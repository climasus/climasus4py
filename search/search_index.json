{"config":{"lang":["pt","en","es"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"climasus4py","text":"<p>Workflows r\u00e1pidos com dados SUS e clima para o Brasil \u2014 em Python.</p> <p><code>climasus4py</code> processa microdados DATASUS com DuckDB: downloads autom\u00e1ticos do FTP, cache em Parquet, filtragem por CID-10, agrega\u00e7\u00e3o temporal/geogr\u00e1fica e enriquecimento com dados clim\u00e1ticos e censit\u00e1rios \u2014 tudo em uma API lazy de alto desempenho.</p>"},{"location":"#instalacao-rapida","title":"Instala\u00e7\u00e3o r\u00e1pida","text":"<pre><code>pip install climasus4py\n</code></pre>"},{"location":"#pipeline-em-3-linhas","title":"Pipeline em 3 linhas","text":"<pre><code>import climasus as cs\n\nresult = cs.sus_pipeline(\"SIM-DO\", uf=\"SP\", year=[2021, 2022, 2023],\n                          groups=\"respiratory\", time=\"month\", geo=\"state\")\nresult.df().head()\n</code></pre>"},{"location":"#documentacao-por-idioma","title":"Documenta\u00e7\u00e3o por idioma","text":"<ul> <li>:flag_br: Portugu\u00eas</li> <li>:flag_us: English</li> <li>:flag_es: Espa\u00f1ol</li> </ul>"},{"location":"#links","title":"Links","text":"<ul> <li>Refer\u00eancia da API</li> <li>GitHub</li> <li>PyPI</li> <li>climasus4r (vers\u00e3o R)</li> </ul>"},{"location":"en/","title":"climasus4py \u2014 English Documentation","text":"<p><code>climasus4py</code> is a Python package for analysing Brazilian SUS (public health system) microdata, with built-in support for INMET climate data enrichment. It uses DuckDB as the query engine, keeping the entire pipeline lazy until final materialisation \u2014 allowing you to process full national datasets with low memory usage.</p>"},{"location":"en/#navigation","title":"Navigation","text":"<ul> <li>:books: API Reference \u2014 Auto-generated documentation for all public functions.</li> <li>:house: Home</li> </ul>"},{"location":"en/#quick-example-respiratory-mortality-in-sao-paulo-20202023","title":"Quick example: respiratory mortality in S\u00e3o Paulo (2020\u20132023)","text":"<pre><code>import climasus as cs\n\n# Full pipeline \u2014 auto-download + cache + filter + aggregate\nresult = cs.sus_pipeline(\n    system=\"SIM-DO\",\n    uf=\"SP\",\n    year=[2020, 2021, 2022, 2023],\n    groups=\"respiratory\",      # ICD-10 group: J00-J99\n    age_min=18,                # adults only\n    time=\"month\",              # monthly aggregation\n    geo=\"state\",               # state level\n    lang=\"en\",                 # English column names\n)\n\n# The relation is lazy \u2014 materialises only here:\ndf = result.df()\nprint(df.head(10))\n</code></pre>"},{"location":"en/#available-disease-groups","title":"Available disease groups","text":"Group Description <code>respiratory</code> Respiratory diseases (J00\u2013J99) <code>cardiovascular</code> Cardiovascular diseases <code>dengue</code> Dengue fever (A90, A91) <code>covid19</code> COVID-19 (U07.1, U07.2) <code>diabetes</code> Diabetes mellitus (E10\u2013E14) <code>neoplasms</code> Neoplasms (C00\u2013D48) <code>external_causes</code> External causes (V01\u2013Y98) <code>maternal_causes</code> Maternal causes (O00\u2013O99) <code>malaria</code> Malaria (B50\u2013B54) <code>tuberculosis_respiratory</code> Pulmonary tuberculosis <code>zika_chikungunya</code> Zika and Chikungunya <code>climate_sensitive</code> Climate-sensitive disease bundle"},{"location":"en/#supported-datasus-systems","title":"Supported DATASUS systems","text":"Code System <code>SIM-DO</code> Death Certificates (mortality) <code>SINASC</code> Live Births <code>SIH</code> Hospital Admissions <code>SIA</code> Outpatient Visits"},{"location":"es/","title":"climasus4py \u2014 Documentaci\u00f3n en Espa\u00f1ol","text":"<p><code>climasus4py</code> es un paquete Python para analizar microdatos del Sistema \u00danico de Salud (SUS) de Brasil, con soporte integrado para enriquecimiento con datos clim\u00e1ticos del INMET. Usa DuckDB como motor de consultas, manteniendo todo el pipeline lazy hasta la materializaci\u00f3n final \u2014 lo que permite procesar datos nacionales completos con bajo uso de memoria.</p>"},{"location":"es/#navegacion","title":"Navegaci\u00f3n","text":"<ul> <li>:books: Referencia de API \u2014 Documentaci\u00f3n autogenerada de todas las funciones p\u00fablicas.</li> <li>:house: Inicio</li> </ul>"},{"location":"es/#ejemplo-rapido-mortalidad-respiratoria-en-sp-20202023","title":"Ejemplo r\u00e1pido: mortalidad respiratoria en SP (2020\u20132023)","text":"<pre><code>import climasus as cs\n\n# Pipeline completo \u2014 descarga autom\u00e1tica + cach\u00e9 + filtrado + agregaci\u00f3n\nresult = cs.sus_pipeline(\n    system=\"SIM-DO\",\n    uf=\"SP\",\n    year=[2020, 2021, 2022, 2023],\n    groups=\"respiratory\",      # grupo ICD-10: J00-J99\n    age_min=18,\n    time=\"month\",\n    geo=\"state\",\n    lang=\"es\",                 # nombres de columnas en espa\u00f1ol\n)\n\ndf = result.df()\nprint(df.head(10))\n</code></pre>"},{"location":"es/#grupos-de-enfermedades-disponibles","title":"Grupos de enfermedades disponibles","text":"Grupo Descripci\u00f3n <code>respiratory</code> Enfermedades respiratorias (J00\u2013J99) <code>cardiovascular</code> Enfermedades cardiovasculares <code>dengue</code> Dengue (A90, A91) <code>covid19</code> COVID-19 (U07.1, U07.2) <code>diabetes</code> Diabetes mellitus (E10\u2013E14) <code>neoplasms</code> Neoplasias (C00\u2013D48) <code>external_causes</code> Causas externas (V01\u2013Y98) <code>maternal_causes</code> Causas maternas (O00\u2013O99) <code>malaria</code> Malaria (B50\u2013B54) <code>tuberculosis_respiratory</code> Tuberculosis pulmonar <code>zika_chikungunya</code> Zika y Chikungunya <code>climate_sensitive</code> Conjunto de enfermedades sensibles al clima"},{"location":"pt/","title":"climasus4py \u2014 Documenta\u00e7\u00e3o em Portugu\u00eas","text":"<p><code>climasus4py</code> \u00e9 um pacote Python para an\u00e1lise de microdados do Sistema \u00danico de Sa\u00fade (SUS), com suporte a dados clim\u00e1ticos do INMET. Usa DuckDB como motor de consulta, mantendo o pipeline lazy at\u00e9 a materializa\u00e7\u00e3o final \u2014 o que permite processar anos inteiros de dados nacionais com baixo uso de mem\u00f3ria.</p>"},{"location":"pt/#navegacao","title":"Navega\u00e7\u00e3o","text":"<ul> <li>:books: Refer\u00eancia da API \u2014 Documenta\u00e7\u00e3o gerada automaticamente de todas as fun\u00e7\u00f5es p\u00fablicas.</li> <li>:house: P\u00e1gina inicial</li> </ul>"},{"location":"pt/#exemplo-mortalidade-respiratoria-em-sp-20202023","title":"Exemplo: mortalidade respirat\u00f3ria em SP (2020\u20132023)","text":"<pre><code>import climasus as cs\n\n# Pipeline completo \u2014 download autom\u00e1tico + cache + filtragem + agrega\u00e7\u00e3o\nresult = cs.sus_pipeline(\n    system=\"SIM-DO\",\n    uf=\"SP\",\n    year=[2020, 2021, 2022, 2023],\n    groups=\"respiratory\",      # grupo CID-10: J00-J99\n    age_min=18,                # adultos\n    time=\"month\",              # agrega\u00e7\u00e3o mensal\n    geo=\"state\",               # n\u00edvel estadual\n    lang=\"pt\",                 # nomes de colunas em portugu\u00eas\n)\n\n# A rela\u00e7\u00e3o \u00e9 lazy \u2014 materializa apenas quando necess\u00e1rio\ndf = result.df()\nprint(df.head(10))\n</code></pre> <pre><code>  time_group UF_residencia  contagem\n0    2020-01            SP      1243\n1    2020-02            SP      1189\n...\n</code></pre>"},{"location":"pt/#grupos-de-doencas-disponiveis","title":"Grupos de doen\u00e7as dispon\u00edveis","text":"<p>Os grupos s\u00e3o mantidos em <code>climasus-data</code> e resolvem automaticamente os CID-10 correspondentes:</p> Grupo Descri\u00e7\u00e3o <code>respiratory</code> Doen\u00e7as respirat\u00f3rias (J00\u2013J99) <code>cardiovascular</code> Doen\u00e7as cardiovasculares <code>dengue</code> Dengue (A90, A91) <code>covid19</code> COVID-19 (U07.1, U07.2) <code>diabetes</code> Diabetes mellitus (E10\u2013E14) <code>neoplasms</code> Neoplasias (C00\u2013D48) <code>external_causes</code> Causas externas (V01\u2013Y98) <code>maternal_causes</code> Causas maternas (O00\u2013O99) <code>malaria</code> Mal\u00e1ria (B50\u2013B54) <code>tuberculosis_respiratory</code> Tuberculose pulmonar <code>zika_chikungunya</code> Zika e Chikungunya <code>climate_sensitive</code> Conjunto sens\u00edvel ao clima"},{"location":"pt/#filosofia-do-pacote","title":"Filosofia do pacote","text":"<ul> <li>Lazy por padr\u00e3o: todas as transforma\u00e7\u00f5es retornam <code>duckdb.DuckDBPyRelation</code> \u2014 os dados s\u00f3 s\u00e3o lidos do disco quando voc\u00ea chama <code>.df()</code>, <code>.fetchall()</code> ou exporta.</li> <li>Cache autom\u00e1tico: os arquivos DBC do DATASUS s\u00e3o baixados uma vez e convertidos para Parquet. Execu\u00e7\u00f5es subsequentes reutilizam o cache.</li> <li>Fast path SQL: quando poss\u00edvel, todo o pipeline vira uma \u00fanica query SQL (sem materializa\u00e7\u00e3o intermedi\u00e1ria).</li> <li>Trilingue: nomes de colunas dispon\u00edveis em <code>\"pt\"</code>, <code>\"en\"</code> e <code>\"es\"</code>.</li> </ul>"},{"location":"pt/#sistemas-datasus-suportados","title":"Sistemas DATASUS suportados","text":"C\u00f3digo Sistema <code>SIM-DO</code> Declara\u00e7\u00f5es de \u00d3bito <code>SINASC</code> Nascidos Vivos <code>SIH</code> Interna\u00e7\u00f5es Hospitalares <code>SIA</code> Atendimentos Ambulatoriais"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>climasus<ul> <li>core<ul> <li>aggregate</li> <li>clean</li> <li>engine</li> <li>filter</li> <li>importer</li> <li>pipeline</li> <li>standardize</li> <li>variables</li> </ul> </li> <li>enrichment<ul> <li>census</li> <li>climate</li> <li>fill_gaps</li> <li>spatial</li> </ul> </li> <li>io<ul> <li>cache</li> <li>export</li> </ul> </li> <li>utils<ul> <li>cid</li> <li>data</li> <li>encoding</li> <li>explore</li> <li>quality</li> </ul> </li> </ul> </li> </ul>"},{"location":"reference/climasus/","title":"<code>climasus</code>","text":""},{"location":"reference/climasus/#climasus","title":"climasus","text":"<p>climasus \u2014 Fast SUS and climate data workflows for Brazil.</p> <p>Provides a high-level pipeline for downloading, cleaning, standardising, filtering, aggregating and exporting DATASUS health data, with optional climate and census enrichment.  All heavy lifting is done lazily via DuckDB; results are only materialised when explicitly collected.</p> <p>Typical usage::</p> <pre><code>import climasus as cs\n\nresult = cs.sus_pipeline(\"SIM-DO\", \"SP\", 2022,\n                         groups=\"respiratory\", time=\"month\")\nresult.df().head()\n</code></pre>"},{"location":"reference/climasus/#climasus-functions","title":"Functions","text":""},{"location":"reference/climasus/#climasus.sus_pipeline","title":"sus_pipeline","text":"<pre><code>sus_pipeline(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    *,\n    lang: str = \"en\",\n    groups: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    age_group: str | list[int] | None = None,\n    time: str = \"month\",\n    geo: str = \"state\",\n    epi_week: bool = False,\n    output: str | Path | None = None,\n    cache_dir: str | Path = Path(\"dados/cache\"),\n    verbose: bool = True,\n    **kwargs: Any,\n) -&gt; duckdb.DuckDBPyRelation | pd.DataFrame\n</code></pre> <p>Run the full SUS ETL pipeline: import \u2192 clean \u2192 standardise \u2192 filter \u2192 variables \u2192 aggregate.</p> <p>Main entry point for most users. Mirrors <code>sus_pipeline()</code> from the R package and uses a single-CTE SQL fast path when the requested operations allow it (same optimisation as R <code>rc_a</code>). Falls back to the staged pipeline for complex operations such as custom age groups or epidemiological-week breakdowns.</p> <p>Parameters:</p> Name Type Description Default <code>system</code> <code>str</code> <p>SUS system identifier, e.g. <code>\"SIM-DO\"</code> or <code>\"SINASC\"</code>.</p> required <code>uf</code> <code>str | list[str]</code> <p>State abbreviation(s), <code>\"all\"</code>, or a region name.</p> required <code>year</code> <code>int | list[int]</code> <p>Year(s) to process, e.g. <code>2022</code> or <code>[2020, 2021, 2022]</code>.</p> required <code>lang</code> <code>str</code> <p>Output language for column names \u2014 <code>\"en\"</code> (default), <code>\"pt\"</code>, or <code>\"es\"</code>.</p> <code>'en'</code> <code>groups</code> <code>str | list[str] | None</code> <p>Disease group name(s) or <code>None</code> to include all causes.</p> <code>None</code> <code>age_min</code> <code>int | None</code> <p>Minimum age in years to retain.</p> <code>None</code> <code>age_max</code> <code>int | None</code> <p>Maximum age in years to retain.</p> <code>None</code> <code>age_group</code> <code>str | list[int] | None</code> <p>Age grouping scheme \u2014 <code>\"who\"</code>, <code>\"decadal\"</code>, a custom list of breakpoints, or <code>None</code> to skip.</p> <code>None</code> <code>time</code> <code>str</code> <p>Temporal aggregation granularity \u2014 <code>\"year\"</code>, <code>\"quarter\"</code>, <code>\"month\"</code> (default), <code>\"week\"</code>, or <code>\"day\"</code>.</p> <code>'month'</code> <code>geo</code> <code>str</code> <p>Geographic aggregation level \u2014 <code>\"state\"</code> (default) or <code>\"municipality\"</code>.</p> <code>'state'</code> <code>epi_week</code> <code>bool</code> <p>If <code>True</code>, add an <code>epi_week</code> column (disables fast path).</p> <code>False</code> <code>output</code> <code>str | Path | None</code> <p>Optional file path to export results (parquet / csv / xlsx).</p> <code>None</code> <code>cache_dir</code> <code>str | Path</code> <p>Root directory for the Parquet cache.</p> <code>Path('dados/cache')</code> <code>verbose</code> <code>bool</code> <p>Print progress messages via Rich.</p> <code>True</code> <code>**kwargs</code> <code>Any</code> <p>Additional keyword arguments forwarded to :func:<code>~climasus.core.importer.sus_import</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation | DataFrame</code> <p>Lazy <code>duckdb.DuckDBPyRelation</code> with aggregated results, or a</p> <code>DuckDBPyRelation | DataFrame</code> <p><code>pandas.DataFrame</code> when output forces materialisation.</p> Example <p>import climasus as cs result = cs.sus_pipeline(\"SIM-DO\", \"SP\", 2022, ...                          groups=\"respiratory\", time=\"month\") result.df().head() cs.sus_pipeline(\"SIM-DO\", [\"SP\", \"RJ\"], [2020, 2021], ...                  output=\"output/mortality.parquet\")</p> Source code in <code>src/climasus/core/pipeline.py</code> <pre><code>def sus_pipeline(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    *,\n    lang: str = \"en\",\n    groups: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    age_group: str | list[int] | None = None,\n    time: str = \"month\",\n    geo: str = \"state\",\n    epi_week: bool = False,\n    output: str | Path | None = None,\n    cache_dir: str | Path = Path(\"dados/cache\"),\n    verbose: bool = True,\n    **kwargs: Any,\n) -&gt; duckdb.DuckDBPyRelation | pd.DataFrame:\n    \"\"\"Run the full SUS ETL pipeline: import \u2192 clean \u2192 standardise \u2192 filter \u2192 variables \u2192 aggregate.\n\n    Main entry point for most users. Mirrors ``sus_pipeline()`` from the\n    R package and uses a single-CTE SQL fast path when the requested\n    operations allow it (same optimisation as R ``rc_a``). Falls back to\n    the staged pipeline for complex operations such as custom age groups\n    or epidemiological-week breakdowns.\n\n    Args:\n        system: SUS system identifier, e.g. ``\"SIM-DO\"`` or\n            ``\"SINASC\"``.\n        uf: State abbreviation(s), ``\"all\"``, or a region name.\n        year: Year(s) to process, e.g. ``2022`` or\n            ``[2020, 2021, 2022]``.\n        lang: Output language for column names \\u2014 ``\"en\"`` (default),\n            ``\"pt\"``, or ``\"es\"``.\n        groups: Disease group name(s) or ``None`` to include all causes.\n        age_min: Minimum age in years to retain.\n        age_max: Maximum age in years to retain.\n        age_group: Age grouping scheme \\u2014 ``\"who\"``, ``\"decadal\"``,\n            a custom list of breakpoints, or ``None`` to skip.\n        time: Temporal aggregation granularity \\u2014 ``\"year\"``,\n            ``\"quarter\"``, ``\"month\"`` (default), ``\"week\"``, or\n            ``\"day\"``.\n        geo: Geographic aggregation level \\u2014 ``\"state\"`` (default) or\n            ``\"municipality\"``.\n        epi_week: If ``True``, add an ``epi_week`` column (disables fast\n            path).\n        output: Optional file path to export results\n            (parquet / csv / xlsx).\n        cache_dir: Root directory for the Parquet cache.\n        verbose: Print progress messages via Rich.\n        **kwargs: Additional keyword arguments forwarded to\n            :func:`~climasus.core.importer.sus_import`.\n\n    Returns:\n        Lazy ``duckdb.DuckDBPyRelation`` with aggregated results, or a\n        ``pandas.DataFrame`` when *output* forces materialisation.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; result = cs.sus_pipeline(\"SIM-DO\", \"SP\", 2022,\n        ...                          groups=\"respiratory\", time=\"month\")\n        &gt;&gt;&gt; result.df().head()\n        &gt;&gt;&gt; cs.sus_pipeline(\"SIM-DO\", [\"SP\", \"RJ\"], [2020, 2021],\n        ...                  output=\"output/mortality.parquet\")\n    \"\"\"\n    group_list = [groups] if isinstance(groups, str) else groups\n\n    # Step 1: Import (always needed \u2014 resolves UFs, discovers/downloads parquets)\n    rel = sus_import(system, uf, year, cache_dir=cache_dir, verbose=verbose, **kwargs)\n\n    # --- Try fast path: single CTE query like R rc_a ---\n    if _can_fast_path(age_group, epi_week, time, geo):\n        # Resolve parquet paths from cache\n        from climasus.utils.data import resolve_uf\n\n        ufs = resolve_uf(uf)\n        years = [year] if isinstance(year, int) else list(year)\n        cache_path = Path(cache_dir)\n        parquet_paths = [\n            cache_path / system / f\"{u}_{y}_all.parquet\"\n            for u in ufs\n            for y in years\n        ]\n        parquet_paths = [p for p in parquet_paths if p.is_file()]\n\n        if parquet_paths:\n            sql = _build_fast_sql(\n                parquet_paths, group_list, age_min, age_max, time, geo\n            )\n            if sql:\n                conn = get_connection()\n                try:\n                    result = conn.sql(sql)\n                    if output:\n                        sus_export(result, output)\n                    return result\n                except Exception:\n                    logging.getLogger(__name__).debug(\n                        \"Fast path failed, falling through to staged pipeline\",\n                        exc_info=True,\n                    )\n\n    # --- Staged pipeline (fallback) ---\n    rel = sus_clean(rel)\n    rel = sus_standardize(rel, lang=lang, system=system)\n    rel = sus_filter(rel, groups=group_list, age_min=age_min, age_max=age_max)\n    rel = sus_variables(rel, age_group=age_group, epi_week=epi_week)\n    rel = sus_aggregate(rel, time=time, geo=geo)\n\n    if output:\n        sus_export(rel, output)\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_import","title":"sus_import","text":"<pre><code>sus_import(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    month: int | list[int] | None = None,\n    *,\n    cache: bool = True,\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    timeout: int = 600,\n    verbose: bool = True,\n    path: str | Path | None = None,\n    data: DataFrame | None = None,\n    backend: Literal[\"auto\", \"ftp\", \"pysus\"] = \"auto\",\n) -&gt; \"duckdb.DuckDBPyRelation\"\n</code></pre> <p>Import SUS data and return a lazy DuckDB relation.</p> <p>Supports three input modes:</p> <ol> <li><code>data=</code> \u2014 wrap an existing <code>pandas.DataFrame</code>.</li> <li><code>path=</code> \u2014 read from a local <code>.parquet</code> or <code>.csv</code> file.</li> <li>Default \u2014 download from the DATASUS FTP, convert <code>.dbc</code> to    Parquet and cache locally; subsequent calls read from cache.</li> </ol> <p>When downloading (mode 3), the backend controls which client is used:</p> <ul> <li><code>\"auto\"</code>  \u2014 FTP direct download (no extra deps), falls back to   PySUS if installed.</li> <li><code>\"ftp\"</code>   \u2014 FTP + <code>.dbc</code> reader chain: <code>readdbc</code> \u2192   <code>pyreaddbc</code> \u2192 <code>pysus</code> \u2192 <code>dbc2dbf</code> CLI.</li> <li><code>\"pysus\"</code> \u2014 PySUS high-level API (requires   <code>pip install pysus</code>; needs C compiler on Windows).</li> </ul> <p>Parameters:</p> Name Type Description Default <code>system</code> <code>str</code> <p>SUS system identifier, e.g. <code>\"SIM-DO\"</code>, <code>\"SINASC\"</code>, or <code>\"SIH-RD\"</code>.</p> required <code>uf</code> <code>str | list[str]</code> <p>State abbreviation(s), e.g. <code>\"SP\"</code> or <code>[\"SP\", \"RJ\"]</code>. Use <code>\"all\"</code> for all states or a region name such as <code>\"Sudeste\"</code>.</p> required <code>year</code> <code>int | list[int]</code> <p>Year(s) to import, e.g. <code>2022</code> or <code>[2020, 2021, 2022]</code>.</p> required <code>month</code> <code>int | list[int] | None</code> <p>Month(s) to import (SIH only). <code>None</code> downloads all 12 months.</p> <code>None</code> <code>cache</code> <code>bool</code> <p>If <code>True</code>, skip download when a cached Parquet exists.</p> <code>True</code> <code>cache_dir</code> <code>str | Path</code> <p>Root directory for the Parquet cache.</p> <code>_DEFAULT_CACHE</code> <code>timeout</code> <code>int</code> <p>Download timeout in seconds.</p> <code>600</code> <code>verbose</code> <code>bool</code> <p>Print progress messages via Rich.</p> <code>True</code> <code>path</code> <code>str | Path | None</code> <p>Local file path to use instead of downloading.</p> <code>None</code> <code>data</code> <code>DataFrame | None</code> <p>Existing <code>DataFrame</code> to wrap instead of downloading.</p> <code>None</code> <code>backend</code> <code>Literal['auto', 'ftp', 'pysus']</code> <p>Download backend \u2014 <code>\"auto\"</code>, <code>\"ftp\"</code>, or <code>\"pysus\"</code>.</p> <code>'auto'</code> <p>Returns:</p> Type Description <code>'duckdb.DuckDBPyRelation'</code> <p>Lazy <code>duckdb.DuckDBPyRelation</code> over the imported data.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If no data could be imported (download failed or no cache hit).</p> <code>ValueError</code> <p>If an unsupported file format is supplied via path.</p> <code>ImportError</code> <p>If <code>.dbc</code> reading is attempted but no backend is available.</p> Example <p>import climasus as cs rel = cs.sus_import(\"SIM-DO\", \"SP\", 2022) rel.count() 334303 cs.sus_import(\"SIM-DO\", \"SP\", 2022, ...               path=\"dados/cache/SP_2022.parquet\")</p> Source code in <code>src/climasus/core/importer.py</code> <pre><code>def sus_import(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    month: int | list[int] | None = None,\n    *,\n    cache: bool = True,\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    timeout: int = 600,\n    verbose: bool = True,\n    path: str | Path | None = None,\n    data: pd.DataFrame | None = None,\n    backend: Literal[\"auto\", \"ftp\", \"pysus\"] = \"auto\",\n) -&gt; \"duckdb.DuckDBPyRelation\":\n    \"\"\"Import SUS data and return a lazy DuckDB relation.\n\n    Supports three input modes:\n\n    1. **``data=``** \u2014 wrap an existing ``pandas.DataFrame``.\n    2. **``path=``** \u2014 read from a local ``.parquet`` or ``.csv`` file.\n    3. **Default** \u2014 download from the DATASUS FTP, convert ``.dbc`` to\n       Parquet and cache locally; subsequent calls read from cache.\n\n    When downloading (mode 3), the *backend* controls which client is\n    used:\n\n    - ``\"auto\"``  \u2014 FTP direct download (no extra deps), falls back to\n      PySUS if installed.\n    - ``\"ftp\"``   \u2014 FTP + ``.dbc`` reader chain: ``readdbc`` \u2192\n      ``pyreaddbc`` \u2192 ``pysus`` \u2192 ``dbc2dbf`` CLI.\n    - ``\"pysus\"`` \u2014 PySUS high-level API (requires\n      ``pip install pysus``; needs C compiler on Windows).\n\n    Args:\n        system: SUS system identifier, e.g. ``\"SIM-DO\"``, ``\"SINASC\"``,\n            or ``\"SIH-RD\"``.\n        uf: State abbreviation(s), e.g. ``\"SP\"`` or ``[\"SP\", \"RJ\"]``.\n            Use ``\"all\"`` for all states or a region name such as\n            ``\"Sudeste\"``.\n        year: Year(s) to import, e.g. ``2022`` or\n            ``[2020, 2021, 2022]``.\n        month: Month(s) to import (SIH only). ``None`` downloads all 12\n            months.\n        cache: If ``True``, skip download when a cached Parquet exists.\n        cache_dir: Root directory for the Parquet cache.\n        timeout: Download timeout in seconds.\n        verbose: Print progress messages via Rich.\n        path: Local file path to use instead of downloading.\n        data: Existing ``DataFrame`` to wrap instead of downloading.\n        backend: Download backend \u2014 ``\"auto\"``, ``\"ftp\"``, or\n            ``\"pysus\"``.\n\n    Returns:\n        Lazy ``duckdb.DuckDBPyRelation`` over the imported data.\n\n    Raises:\n        RuntimeError: If no data could be imported (download failed or\n            no cache hit).\n        ValueError: If an unsupported file format is supplied via\n            *path*.\n        ImportError: If ``.dbc`` reading is attempted but no backend is\n            available.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; rel = cs.sus_import(\"SIM-DO\", \"SP\", 2022)\n        &gt;&gt;&gt; rel.count()\n        334303\n        &gt;&gt;&gt; cs.sus_import(\"SIM-DO\", \"SP\", 2022,\n        ...               path=\"dados/cache/SP_2022.parquet\")\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    ufs = resolve_uf(uf)\n    years = [year] if isinstance(year, int) else list(year)\n    months = [month] if isinstance(month, int) else (month or [None])\n\n    parquet_paths: list[Path] = []\n\n    if data is not None:\n        # Mode 1: inline data\n        data = _coerce_datasus_types(data.copy())\n        target = cache_dir / system / f\"inline_{'_'.join(ufs)}_{years[0]}.parquet\"\n        target.parent.mkdir(parents=True, exist_ok=True)\n        pq.write_table(pa.Table.from_pandas(data), target)\n        parquet_paths.append(target)\n\n    elif path is not None:\n        # Mode 2: local file\n        p = Path(path)\n        if p.suffix == \".parquet\":\n            df = pq.read_table(p).to_pandas()\n        elif p.suffix == \".csv\":\n            df = pd.read_csv(p)\n        else:\n            raise ValueError(f\"Unsupported file format: {p.suffix}\")\n        target = cache_dir / system / f\"file_{'_'.join(ufs)}_{years[0]}.parquet\"\n        target.parent.mkdir(parents=True, exist_ok=True)\n        pq.write_table(pa.Table.from_pandas(df), target)\n        parquet_paths.append(target)\n\n    else:\n        # Mode 3: download from DATASUS\n        needed: list[dict] = []\n        for one_uf in ufs:\n            for one_year in years:\n                for one_month in months:\n                    month_str = str(one_month) if one_month else \"all\"\n                    target = (\n                        cache_dir / system / f\"{one_uf}_{one_year}_{month_str}.parquet\"\n                    )\n                    if cache and target.is_file():\n                        parquet_paths.append(target)\n                    else:\n                        needed.append({\n                            \"uf\": one_uf,\n                            \"year\": one_year,\n                            \"month\": one_month,\n                            \"target\": target,\n                        })\n\n        if needed:\n            use_pysus = (\n                backend == \"pysus\"\n                or (backend == \"auto\" and _pysus_available() and system in _PYSUS_SYSTEM_MAP)\n            )\n\n            engine_label = \"PySUS\" if use_pysus else \"FTP\"\n            if verbose:\n                console.print(\n                    f\"[cyan]\u2139[/] Downloading {len(needed)} file(s) via {engine_label}...\"\n                )\n\n            for item in needed:\n                result: Path | None = None\n\n                if use_pysus:\n                    try:\n                        df = _download_pysus(\n                            system, item[\"uf\"], item[\"year\"], item[\"month\"]\n                        )\n                        df = _coerce_datasus_types(df)\n                        item[\"target\"].parent.mkdir(parents=True, exist_ok=True)\n                        pq.write_table(pa.Table.from_pandas(df), item[\"target\"])\n                        result = item[\"target\"]\n                        if verbose:\n                            console.print(\n                                f\"[green]\u2714[/]  {item['uf']}_{item['year']} ({len(df):,} rows)\"\n                            )\n                    except Exception as e:\n                        if verbose:\n                            console.print(\n                                f\"[red]\u2717[/]  {item['uf']}_{item['year']}: {e}\"\n                            )\n                else:\n                    result = _download_and_cache(\n                        system,\n                        item[\"uf\"],\n                        item[\"year\"],\n                        item[\"month\"],\n                        item[\"target\"],\n                        verbose,\n                    )\n\n                if result:\n                    parquet_paths.append(result)\n\n    if not parquet_paths:\n        raise RuntimeError(\"No data imported \u2014 check system/uf/year parameters.\")\n\n    return read_parquets(parquet_paths)\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_clean","title":"sus_clean","text":"<pre><code>sus_clean(\n    rel: DuckDBPyRelation,\n    *,\n    fix_enc: bool = True,\n    dedup: bool = True,\n    dedup_cols: list[str] | None = None,\n    age_range: tuple[int, int] = (0, 120),\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Clean SUS data: deduplicate, fix encoding, and validate age range.</p> <p>All operations remain lazy (DuckDB relation) until materialised. Deduplication uses ROW_NUMBER() over known DATASUS key columns for performance. Age validation decodes the SIM-DO 3-digit coded age field before applying numeric range filters.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to clean.</p> required <code>fix_enc</code> <code>bool</code> <p>If <code>True</code>, schedule encoding fixes (applied at standardisation time when columns are collected).</p> <code>True</code> <code>dedup</code> <code>bool</code> <p>If <code>True</code>, remove duplicate records.</p> <code>True</code> <code>dedup_cols</code> <code>list[str] | None</code> <p>Columns to use for deduplication. If <code>None</code>, uses known DATASUS key columns (faster). Pass <code>[\"*\"]</code> to force a full-row DISTINCT.</p> <code>None</code> <code>age_range</code> <code>tuple[int, int]</code> <p><code>(min_age, max_age)</code> tuple in years. Records with decoded age outside this range are dropped. Handles the DATASUS 3-digit coding scheme for SIM-DO IDADE fields.</p> <code>(0, 120)</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with duplicates removed and invalid ages</p> <code>DuckDBPyRelation</code> <p>filtered out.</p> Example <p>import climasus as cs clean = cs.sus_clean(rel, age_range=(0, 110)) clean = cs.sus_clean(rel, dedup_cols=[\"CONTADOR\"])</p> Source code in <code>src/climasus/core/clean.py</code> <pre><code>def sus_clean(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    fix_enc: bool = True,\n    dedup: bool = True,\n    dedup_cols: list[str] | None = None,\n    age_range: tuple[int, int] = (0, 120),\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Clean SUS data: deduplicate, fix encoding, and validate age range.\n\n    All operations remain lazy (DuckDB relation) until materialised.\n    Deduplication uses ROW_NUMBER() over known DATASUS key columns for\n    performance. Age validation decodes the SIM-DO 3-digit coded age\n    field before applying numeric range filters.\n\n    Args:\n        rel: Lazy DuckDB relation to clean.\n        fix_enc: If ``True``, schedule encoding fixes (applied at\n            standardisation time when columns are collected).\n        dedup: If ``True``, remove duplicate records.\n        dedup_cols: Columns to use for deduplication. If ``None``, uses\n            known DATASUS key columns (faster). Pass ``[\"*\"]`` to force\n            a full-row DISTINCT.\n        age_range: ``(min_age, max_age)`` tuple in years. Records with\n            decoded age outside this range are dropped. Handles the\n            DATASUS 3-digit coding scheme for SIM-DO IDADE fields.\n\n    Returns:\n        Lazy DuckDB relation with duplicates removed and invalid ages\n        filtered out.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; clean = cs.sus_clean(rel, age_range=(0, 110))\n        &gt;&gt;&gt; clean = cs.sus_clean(rel, dedup_cols=[\"CONTADOR\"])\n    \"\"\"\n    columns = schema_columns(rel)\n\n    if dedup:\n        if dedup_cols and dedup_cols != [\"*\"]:\n            key_cols = [c for c in dedup_cols if c in columns]\n        else:\n            # Use key columns for SUS data instead of full-row distinct\n            key_candidates = [\n                \"CONTADOR\", \"NUMERODO\", \"NUMERODN\", \"N_AIH\", \"NU_NOTIFIC\",\n                \"counter\", \"record_id\",\n            ]\n            key_cols = [c for c in key_candidates if c in columns]\n\n        if key_cols:\n            # ROW_NUMBER() dedup \u2014 keeps first row per key, much faster\n            conn = get_connection()\n            partition = \", \".join(f'\"{c}\"' for c in key_cols)\n            all_cols = \", \".join(f'\"{c}\"' for c in columns)\n            # ORDER BY partition keys for deterministic results\n            rel = conn.sql(\n                f\"SELECT {all_cols} FROM (\"\n                f\"  SELECT *, ROW_NUMBER() OVER (PARTITION BY {partition} ORDER BY {partition}) AS __rn\"\n                f\"  FROM rel\"\n                f\") WHERE __rn = 1\"\n            )\n        else:\n            # No key column found \u2014 fall back to full distinct\n            rel = rel.distinct()\n\n    columns = schema_columns(rel)\n\n    # Age validation\n    # DATASUS SIM-DO encodes age as a 3-digit string: 1st digit = unit code,\n    # remaining = value.  Code 4 = years, 3 = months, 2 = days, 1 = hours,\n    # 0 = minutes, 5 = 100+ years.\n    # We decode to age_years before filtering.\n    age_col = None\n    for candidate in (\"IDADE\", \"age\", \"age_years\"):\n        if candidate in columns:\n            age_col = candidate\n            break\n\n    if age_col:\n        lo, hi = age_range\n        conn = get_connection()\n\n        # Decode SIM-DO coded age to years\n        decoded_expr = (\n            f'CASE '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'5\\' '\n            f'    THEN 100 + TRY_CAST(SUBSTR(TRIM(\"{age_col}\"), 2) AS INTEGER) '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'4\\' '\n            f'    THEN TRY_CAST(SUBSTR(TRIM(\"{age_col}\"), 2) AS INTEGER) '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'3\\' '\n            f'    THEN 0  '  # months \u2192 0 years (infant)\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) IN (\\'0\\', \\'1\\', \\'2\\') '\n            f'    THEN 0  '  # minutes/hours/days \u2192 0 years (infant)\n            f'  ELSE TRY_CAST(\"{age_col}\" AS INTEGER) '\n            f'END'\n        )\n\n        rel = conn.sql(\n            f'SELECT *, ({decoded_expr}) AS __age_years FROM rel'\n        )\n        rel = rel.filter(\n            f'__age_years IS NULL '\n            f'OR (__age_years &gt;= {lo} AND __age_years &lt;= {hi})'\n        )\n        # Drop helper column\n        final_cols = [c for c in schema_columns(rel) if c != \"__age_years\"]\n        rel = rel.project(\", \".join(f'\"{c}\"' for c in final_cols))\n\n    # Note: encoding fixes for string columns require materialization\n    # or a UDF. For now, we mark that encoding fix should happen at\n    # standardization time when we collect column subsets.\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_standardize","title":"sus_standardize","text":"<pre><code>sus_standardize(\n    rel: DuckDBPyRelation,\n    *,\n    lang: str = \"en\",\n    system: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Standardize column names, translate labels, and convert date columns.</p> Source code in <code>src/climasus/core/standardize.py</code> <pre><code>def sus_standardize(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    lang: str = \"en\",\n    system: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Standardize column names, translate labels, and convert date columns.\"\"\"\n    columns = schema_columns(rel)\n\n    if system is None:\n        system = detect_system(columns)\n\n    col_map = _load_column_dict(lang)\n    renames = {col: col_map[col] for col in columns if col in col_map}\n\n    if renames:\n        projections = []\n        for col in columns:\n            if col in renames:\n                projections.append(f'\"{col}\" AS \"{renames[col]}\"')\n            else:\n                projections.append(f'\"{col}\"')\n        conn = get_connection()\n        rel = conn.sql(f\"SELECT {', '.join(projections)} FROM rel\")\n\n    # Date conversion (DDMMYYYY -&gt; DATE), robust for both VARCHAR and DATE/TIMESTAMP.\n    # We avoid TRY/EXCEPT to prevent leaving transaction context aborted in DuckDB.\n    new_columns = schema_columns(rel)\n    date_candidates = [\n        \"death_date\",\n        \"date\",\n        \"DTOBITO\",\n        \"DTNASC\",\n        \"admission_date\",\n        \"birth_date\",\n        \"case_conclusion_date\",\n    ]\n\n    for dc in date_candidates:\n        if dc in new_columns:\n            rel = rel.project(\n                \", \".join(\n                    (\n                        f\"COALESCE(\"\n                        f\"TRY_STRPTIME(CAST(\\\"{dc}\\\" AS VARCHAR), '%d%m%Y')::DATE, \"\n                        f\"TRY_CAST(\\\"{dc}\\\" AS DATE)\"\n                        f\") AS \\\"{dc}\\\"\"\n                    )\n                    if c == dc\n                    else f'\"{c}\"'\n                    for c in new_columns\n                )\n            )\n\n    rel = rel.set_alias(system or \"unknown\")\n    return rel\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_filter","title":"sus_filter","text":"<pre><code>sus_filter(\n    rel: DuckDBPyRelation,\n    *,\n    groups: str | list[str] | None = None,\n    codes: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    sex: str | None = None,\n    race: str | list[str] | None = None,\n    uf: str | list[str] | None = None,\n    municipality: str | list[str] | None = None,\n    date_start: str | None = None,\n    date_end: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Filter SUS data by disease groups, demographics, geography, and dates.</p> <p>All predicates are applied as DuckDB <code>WHERE</code> clauses; the relation stays lazy until materialised. CID-10 codes are resolved via <code>codes_for_groups</code> and <code>expand_cid_ranges</code>; geographic and demographic columns are auto-detected from the relation schema.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to filter.</p> required <code>groups</code> <code>str | list[str] | None</code> <p>Named disease group(s) from climasus-data, e.g. <code>\"respiratory\"</code> or <code>[\"cardiovascular\", \"dengue\"]</code>.</p> <code>None</code> <code>codes</code> <code>str | list[str] | None</code> <p>Explicit ICD-10 code(s) or ranges, e.g. <code>[\"J00-J99\", \"A90\"]</code>.</p> <code>None</code> <code>age_min</code> <code>int | None</code> <p>Minimum age in years (inclusive). Decoded from DATASUS coded age when the column is in SIM-DO format.</p> <code>None</code> <code>age_max</code> <code>int | None</code> <p>Maximum age in years (inclusive).</p> <code>None</code> <code>sex</code> <code>str | None</code> <p>Sex code to keep \u2014 <code>\"M\"</code> (male) or <code>\"F\"</code> (female).</p> <code>None</code> <code>race</code> <code>str | list[str] | None</code> <p><code>RACACOR</code> code(s) to keep, e.g. <code>[\"1\", \"4\"]</code>.</p> <code>None</code> <code>uf</code> <code>str | list[str] | None</code> <p>One or more Brazilian state abbreviations, e.g. <code>[\"SP\", \"RJ\"]</code>.</p> <code>None</code> <code>municipality</code> <code>str | list[str] | None</code> <p>Municipality code(s) (IBGE 6-digit), e.g. <code>[\"355030\"]</code>.</p> <code>None</code> <code>date_start</code> <code>str | None</code> <p>Earliest event date (inclusive), ISO format <code>\"YYYY-MM-DD\"</code>.</p> <code>None</code> <code>date_end</code> <code>str | None</code> <p>Latest event date (inclusive), ISO format <code>\"YYYY-MM-DD\"</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with all specified filters applied.</p> Example <p>import climasus as cs filtered = cs.sus_filter(rel, groups=\"respiratory\", ...                          age_min=15, age_max=64, uf=\"SP\") cs.sus_filter(rel, codes=[\"A90\", \"A91\"], sex=\"F\").count()</p> Source code in <code>src/climasus/core/filter.py</code> <pre><code>def sus_filter(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    groups: str | list[str] | None = None,\n    codes: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    sex: str | None = None,\n    race: str | list[str] | None = None,\n    uf: str | list[str] | None = None,\n    municipality: str | list[str] | None = None,\n    date_start: str | None = None,\n    date_end: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Filter SUS data by disease groups, demographics, geography, and dates.\n\n    All predicates are applied as DuckDB ``WHERE`` clauses; the relation\n    stays lazy until materialised. CID-10 codes are resolved via\n    ``codes_for_groups`` and ``expand_cid_ranges``; geographic and\n    demographic columns are auto-detected from the relation schema.\n\n    Args:\n        rel: Lazy DuckDB relation to filter.\n        groups: Named disease group(s) from climasus-data, e.g.\n            ``\"respiratory\"`` or ``[\"cardiovascular\", \"dengue\"]``.\n        codes: Explicit ICD-10 code(s) or ranges, e.g.\n            ``[\"J00-J99\", \"A90\"]``.\n        age_min: Minimum age in years (inclusive). Decoded from DATASUS\n            coded age when the column is in SIM-DO format.\n        age_max: Maximum age in years (inclusive).\n        sex: Sex code to keep \u2014 ``\"M\"`` (male) or ``\"F\"`` (female).\n        race: ``RACACOR`` code(s) to keep, e.g. ``[\"1\", \"4\"]``.\n        uf: One or more Brazilian state abbreviations, e.g.\n            ``[\"SP\", \"RJ\"]``.\n        municipality: Municipality code(s) (IBGE 6-digit), e.g.\n            ``[\"355030\"]``.\n        date_start: Earliest event date (inclusive), ISO format\n            ``\"YYYY-MM-DD\"``.\n        date_end: Latest event date (inclusive), ISO format\n            ``\"YYYY-MM-DD\"``.\n\n    Returns:\n        Lazy DuckDB relation with all specified filters applied.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; filtered = cs.sus_filter(rel, groups=\"respiratory\",\n        ...                          age_min=15, age_max=64, uf=\"SP\")\n        &gt;&gt;&gt; cs.sus_filter(rel, codes=[\"A90\", \"A91\"], sex=\"F\").count()\n    \"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n\n    # --- Disease filtering (CID-10) ---\n    icd_codes: list[str] = []\n    if groups:\n        group_list = [groups] if isinstance(groups, str) else groups\n        icd_codes.extend(codes_for_groups(group_list))\n    if codes:\n        code_list = [codes] if isinstance(codes, str) else codes\n        icd_codes.extend(expand_cid_ranges(code_list))\n\n    if icd_codes:\n        cause_col = detect_cause_column(columns)\n        if cause_col:\n            unique_codes = sorted(set(icd_codes))\n            if len(unique_codes) &lt;= 100:\n                # Direct IN filter\n                codes_str = \", \".join(f\"'{c}'\" for c in unique_codes)\n                rel = rel.filter(f'\"{cause_col}\" IN ({codes_str})')\n            else:\n                # Semi-join via temporary table for large code lists\n                codes_sql = \", \".join(f\"('{c}')\" for c in unique_codes)\n                conn.execute(\n                    f\"CREATE OR REPLACE TEMP TABLE _icd_filter AS \"\n                    f\"SELECT * FROM (VALUES {codes_sql}) AS t(code)\"\n                )\n                rel = conn.sql(\n                    f'SELECT r.* FROM rel r SEMI JOIN _icd_filter f '\n                    f'ON SUBSTR(r.\"{cause_col}\", 1, 3) = f.code'\n                )\n\n    # --- Age filtering ---\n    age_col = detect_age_column(columns)\n    if age_col and (age_min is not None or age_max is not None):\n        decoded = decode_age_sql(age_col)\n        conditions = []\n        if age_min is not None:\n            conditions.append(f'({decoded}) &gt;= {age_min}')\n        if age_max is not None:\n            conditions.append(f'({decoded}) &lt;= {age_max}')\n        rel = rel.filter(\" AND \".join(conditions))\n\n    # --- Sex filtering ---\n    sex_col = detect_sex_column(columns)\n    if sex_col and sex:\n        sex_val = sex.upper()\n        rel = rel.filter(f'\"{sex_col}\" = \\'{sex_val}\\'')\n\n    # --- Race filtering ---\n    if race:\n        race_list = [race] if isinstance(race, str) else race\n        for candidate in (\"RACACOR\", \"race\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{r}'\" for r in race_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- UF filtering ---\n    if uf:\n        uf_list = [uf] if isinstance(uf, str) else uf\n        for candidate in (\"UF\", \"SG_UF\", \"state\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{u.upper()}'\" for u in uf_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- Municipality filtering ---\n    if municipality:\n        muni_list = [municipality] if isinstance(municipality, str) else municipality\n        for candidate in (\"CODMUNRES\", \"municipality_code\", \"ID_MUNICIP\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{m}'\" for m in muni_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- Date range filtering ---\n    if date_start or date_end:\n        for candidate in (\"death_date\", \"date\", \"DTOBITO\", \"DTNASC\"):\n            if candidate in columns:\n                if date_start:\n                    rel = rel.filter(\n                        f'TRY_CAST(\"{candidate}\" AS DATE) &gt;= \\'{date_start}\\''\n                    )\n                if date_end:\n                    rel = rel.filter(\n                        f'TRY_CAST(\"{candidate}\" AS DATE) &lt;= \\'{date_end}\\''\n                    )\n                break\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_variables","title":"sus_variables","text":"<pre><code>sus_variables(\n    rel: DuckDBPyRelation,\n    *,\n    age_group: str | list[int] | None = None,\n    epi_week: bool = False,\n    season: bool = False,\n    quarter: bool = False,\n    month_name: bool = False,\n    day_of_week: bool = False,\n    hemisphere: str = \"south\",\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Create derived variables from SUS data (stays lazy).</p> Source code in <code>src/climasus/core/variables.py</code> <pre><code>def sus_variables(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    age_group: str | list[int] | None = None,\n    epi_week: bool = False,\n    season: bool = False,\n    quarter: bool = False,\n    month_name: bool = False,\n    day_of_week: bool = False,\n    hemisphere: str = \"south\",\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Create derived variables from SUS data (stays lazy).\"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n    projections = [f'\"{c}\"' for c in columns]\n\n    age_col = detect_age_column(columns)\n    if age_col and age_group:\n        if isinstance(age_group, list):\n            breaks = sorted(age_group + [999])\n        else:\n            breaks = _age_breaks_for_preset(age_group)\n\n        case_parts = []\n        decoded = decode_age_sql(age_col)\n        for i in range(len(breaks) - 1):\n            lo, hi = breaks[i], breaks[i + 1] - 1\n            is_last = i == len(breaks) - 2\n            label = f\"{lo}+\" if is_last else f\"{lo}-{hi}\"\n            case_parts.append(\n                f\"WHEN ({decoded}) BETWEEN {lo} AND {hi} \"\n                f\"THEN '{label}'\"\n            )\n        case_sql = f'CASE {\" \".join(case_parts)} ELSE \\'unknown\\' END AS \"age_group\"'\n        projections.append(case_sql)\n\n    date_col = detect_date_column(columns)\n    if date_col:\n        date_cast = f'TRY_CAST(\"{date_col}\" AS DATE)'\n\n        if epi_week:\n            projections.append(f\"STRFTIME({date_cast}, '%Y-W%W') AS epi_week\")\n\n        if season:\n            projections.append(f\"{_season_case_sql(date_cast, hemisphere)} AS season\")\n\n        if quarter:\n            projections.append(f\"'Q' || EXTRACT(QUARTER FROM {date_cast}) AS quarter\")\n\n        if month_name:\n            projections.append(f\"STRFTIME({date_cast}, '%B') AS month_name\")\n\n        if day_of_week:\n            projections.append(f\"STRFTIME({date_cast}, '%A') AS day_of_week\")\n\n    rel = conn.sql(f\"SELECT {', '.join(projections)} FROM rel\")\n    return rel\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_aggregate","title":"sus_aggregate","text":"<pre><code>sus_aggregate(\n    rel: DuckDBPyRelation,\n    *,\n    time: str = \"month\",\n    geo: str = \"state\",\n    extra_groups: list[str] | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Aggregate SUS data by time period and geography.</p> <p>Groups the relation by a temporal expression and a geographic level, counting rows and summarising recognised numeric columns. The relation stays lazy until materialised.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation produced by a previous pipeline step.</p> required <code>time</code> <code>str</code> <p>Temporal granularity \u2014 one of <code>\"year\"</code>, <code>\"quarter\"</code>, <code>\"month\"</code>, <code>\"week\"</code>, or <code>\"day\"</code>.</p> <code>'month'</code> <code>geo</code> <code>str</code> <p>Geographic aggregation level \u2014 one of <code>\"state\"</code>, <code>\"municipality\"</code>, <code>\"region\"</code>, or <code>\"country\"</code>.</p> <code>'state'</code> <code>extra_groups</code> <code>list[str] | None</code> <p>Additional column names to include in the GROUP BY clause, e.g. <code>[\"sex\", \"age_group\"]</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with columns: <code>time_group</code>, the resolved</p> <code>DuckDBPyRelation</code> <p>geo column, any extra group columns, <code>count</code>, and</p> <code>DuckDBPyRelation</code> <p><code>sum_*/mean_*</code> for any recognised numeric columns present.</p> Example <p>import climasus as cs agg = cs.sus_aggregate(rel, time=\"year\", geo=\"state\") agg.df().head() agg_sex = cs.sus_aggregate(rel, time=\"month\", ...                            geo=\"municipality\", ...                            extra_groups=[\"sex\"])</p> Source code in <code>src/climasus/core/aggregate.py</code> <pre><code>def sus_aggregate(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    time: str = \"month\",\n    geo: str = \"state\",\n    extra_groups: list[str] | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Aggregate SUS data by time period and geography.\n\n    Groups the relation by a temporal expression and a geographic level,\n    counting rows and summarising recognised numeric columns. The relation\n    stays lazy until materialised.\n\n    Args:\n        rel: Lazy DuckDB relation produced by a previous pipeline step.\n        time: Temporal granularity \u2014 one of ``\"year\"``, ``\"quarter\"``,\n            ``\"month\"``, ``\"week\"``, or ``\"day\"``.\n        geo: Geographic aggregation level \u2014 one of ``\"state\"``,\n            ``\"municipality\"``, ``\"region\"``, or ``\"country\"``.\n        extra_groups: Additional column names to include in the GROUP BY\n            clause, e.g. ``[\"sex\", \"age_group\"]``.\n\n    Returns:\n        Lazy DuckDB relation with columns: ``time_group``, the resolved\n        geo column, any extra group columns, ``count``, and\n        ``sum_*/mean_*`` for any recognised numeric columns present.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; agg = cs.sus_aggregate(rel, time=\"year\", geo=\"state\")\n        &gt;&gt;&gt; agg.df().head()\n        &gt;&gt;&gt; agg_sex = cs.sus_aggregate(rel, time=\"month\",\n        ...                            geo=\"municipality\",\n        ...                            extra_groups=[\"sex\"])\n    \"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n\n    group_cols: list[str] = []\n\n    # --- Time grouping ---\n    date_col = detect_date_column(columns)\n    if date_col:\n        date_cast = f'TRY_CAST(\"{date_col}\" AS DATE)'\n        time_expr = {\n            \"year\": f\"EXTRACT(YEAR FROM {date_cast})\",\n            \"quarter\": f\"EXTRACT(YEAR FROM {date_cast}) || '-Q' || EXTRACT(QUARTER FROM {date_cast})\",\n            \"month\": f\"STRFTIME({date_cast}, '%Y-%m')\",\n            \"week\": f\"STRFTIME({date_cast}, '%Y-W%W')\",\n            \"day\": f\"CAST({date_cast} AS VARCHAR)\",\n        }.get(time, f\"STRFTIME({date_cast}, '%Y-%m')\")\n\n        time_alias = \"time_group\"\n        group_cols.append(time_alias)\n    else:\n        time_expr = None\n        time_alias = None\n\n    # --- Geo grouping ---\n    geo_col = detect_geo_column(columns, level=geo)\n    if geo_col:\n        group_cols.append(f'\"{geo_col}\"')\n\n    # --- Extra groups ---\n    if extra_groups:\n        for eg in extra_groups:\n            if eg in columns:\n                group_cols.append(f'\"{eg}\"')\n\n    if not group_cols:\n        # No grouping possible \u2014 return count\n        return conn.sql(\"SELECT COUNT(*) AS count FROM rel\")\n\n    # Build SELECT\n    select_parts: list[str] = []\n    if time_expr:\n        select_parts.append(f\"{time_expr} AS {time_alias}\")\n    if geo_col:\n        select_parts.append(f'\"{geo_col}\"')\n    if extra_groups:\n        for eg in extra_groups:\n            if eg in columns:\n                select_parts.append(f'\"{eg}\"')\n\n    select_parts.append(\"COUNT(*) AS count\")\n\n    # Numeric columns \u2192 SUM and MEAN\n    numeric_candidates = [\"count\", \"deaths\", \"admissions\", \"cases\", \"value\"]\n    for nc in numeric_candidates:\n        if nc in columns and nc not in [g.strip('\"') for g in group_cols]:\n            select_parts.append(f'SUM(TRY_CAST(\"{nc}\" AS DOUBLE)) AS sum_{nc}')\n            select_parts.append(f'AVG(TRY_CAST(\"{nc}\" AS DOUBLE)) AS mean_{nc}')\n\n    group_by = \", \".join(group_cols)\n    select = \", \".join(select_parts)\n\n    return conn.sql(f\"SELECT {select} FROM rel GROUP BY {group_by} ORDER BY {group_by}\")\n</code></pre>"},{"location":"reference/climasus/#climasus.collect_arrow","title":"collect_arrow","text":"<pre><code>collect_arrow(rel: DuckDBPyRelation) -&gt; 'pa.Table'\n</code></pre> <p>Materialise a DuckDB relation to a PyArrow Table.</p> <p>Significantly faster than <code>collect()</code> for large datasets (~100\u00d7) since DuckDB transfers data in zero-copy Arrow format. If DuckDB returns a <code>RecordBatchReader</code>, <code>.read_all()</code> is called to normalise the result to a proper <code>pyarrow.Table</code> with <code>.num_rows</code> / <code>.num_columns</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to execute and collect.</p> required <p>Returns:</p> Type Description <code>'pa.Table'</code> <p><code>pyarrow.Table</code> with the full result set.</p> Example <p>import climasus as cs table = cs.collect_arrow(rel) table.num_rows 334303</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def collect_arrow(rel: duckdb.DuckDBPyRelation) -&gt; \"pa.Table\":\n    \"\"\"Materialise a DuckDB relation to a PyArrow Table.\n\n    Significantly faster than ``collect()`` for large datasets (~100\u00d7)\n    since DuckDB transfers data in zero-copy Arrow format. If DuckDB\n    returns a ``RecordBatchReader``, ``.read_all()`` is called to\n    normalise the result to a proper ``pyarrow.Table`` with\n    ``.num_rows`` / ``.num_columns``.\n\n    Args:\n        rel: Lazy DuckDB relation to execute and collect.\n\n    Returns:\n        ``pyarrow.Table`` with the full result set.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; table = cs.collect_arrow(rel)\n        &gt;&gt;&gt; table.num_rows\n        334303\n    \"\"\"\n    result = rel.arrow()\n    # Some DuckDB versions return RecordBatchReader instead of Table\n    if hasattr(result, \"read_all\"):\n        return result.read_all()\n    return result\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_export","title":"sus_export","text":"<pre><code>sus_export(\n    data: DuckDBPyRelation | DataFrame,\n    path: str | Path,\n    *,\n    fmt: str | None = None,\n    overwrite: bool = True,\n    compress: str = \"snappy\",\n) -&gt; Path\n</code></pre> <p>Export data to Parquet, CSV, or Excel.</p> <p>Accepts lazy DuckDB relations and <code>pandas.DataFrame</code>. For relations, uses DuckDB <code>COPY TO</code> which avoids Python-side materialisation for parquet and CSV formats (significantly faster than <code>write_parquet</code> / <code>write_csv</code>).</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Data to export \u2014 a lazy <code>DuckDBPyRelation</code> or a <code>pandas.DataFrame</code>.</p> required <code>path</code> <code>str | Path</code> <p>Destination file path. The format is inferred from the extension unless fmt is specified explicitly.</p> required <code>fmt</code> <code>str | None</code> <p>Output format override \u2014 <code>\"parquet\"</code>, <code>\"csv\"</code>, or <code>\"xlsx\"</code> / <code>\"excel\"</code>. If <code>None</code>, inferred from path.</p> <code>None</code> <code>overwrite</code> <code>bool</code> <p>If <code>False</code>, raise <code>FileExistsError</code> when path already exists. Defaults to <code>True</code>.</p> <code>True</code> <code>compress</code> <code>str</code> <p>Parquet compression codec \u2014 <code>\"snappy\"</code> (default), <code>\"zstd\"</code>, <code>\"gzip\"</code>, or <code>\"none\"</code>.</p> <code>'snappy'</code> <p>Returns:</p> Type Description <code>Path</code> <p>Resolved <code>pathlib.Path</code> of the written file.</p> <p>Raises:</p> Type Description <code>FileExistsError</code> <p>If path exists and overwrite is <code>False</code>.</p> <code>ValueError</code> <p>If fmt (or the inferred extension) is not supported.</p> <code>ImportError</code> <p>If Excel export is requested but <code>openpyxl</code> is not installed.</p> Example <p>import climasus as cs cs.sus_export(rel, \"output/mortality_2022.parquet\") PosixPath('output/mortality_2022.parquet') cs.sus_export(rel, \"output/data.csv\")</p> Source code in <code>src/climasus/io/export.py</code> <pre><code>def sus_export(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    path: str | Path,\n    *,\n    fmt: str | None = None,\n    overwrite: bool = True,\n    compress: str = \"snappy\",\n) -&gt; Path:\n    \"\"\"Export data to Parquet, CSV, or Excel.\n\n    Accepts lazy DuckDB relations and ``pandas.DataFrame``. For\n    relations, uses DuckDB ``COPY TO`` which avoids Python-side\n    materialisation for parquet and CSV formats (significantly faster\n    than ``write_parquet`` / ``write_csv``).\n\n    Args:\n        data: Data to export \u2014 a lazy ``DuckDBPyRelation`` or a\n            ``pandas.DataFrame``.\n        path: Destination file path. The format is inferred from the\n            extension unless *fmt* is specified explicitly.\n        fmt: Output format override \u2014 ``\"parquet\"``, ``\"csv\"``, or\n            ``\"xlsx\"`` / ``\"excel\"``. If ``None``, inferred from\n            *path*.\n        overwrite: If ``False``, raise ``FileExistsError`` when *path*\n            already exists. Defaults to ``True``.\n        compress: Parquet compression codec \u2014 ``\"snappy\"`` (default),\n            ``\"zstd\"``, ``\"gzip\"``, or ``\"none\"``.\n\n    Returns:\n        Resolved ``pathlib.Path`` of the written file.\n\n    Raises:\n        FileExistsError: If *path* exists and *overwrite* is ``False``.\n        ValueError: If *fmt* (or the inferred extension) is not\n            supported.\n        ImportError: If Excel export is requested but ``openpyxl`` is\n            not installed.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.sus_export(rel, \"output/mortality_2022.parquet\")\n        PosixPath('output/mortality_2022.parquet')\n        &gt;&gt;&gt; cs.sus_export(rel, \"output/data.csv\")\n    \"\"\"\n    path = Path(path)\n    path.parent.mkdir(parents=True, exist_ok=True)\n\n    if fmt is None:\n        fmt = path.suffix.lstrip(\".\").lower()\n\n    if not overwrite and path.exists():\n        raise FileExistsError(f\"File already exists: {path}\")\n\n    if fmt == \"parquet\":\n        if is_relation(data):\n            _copy_to(data, path, \"PARQUET\", f\"COMPRESSION '{compress}'\")\n        else:\n            pq.write_table(\n                pa.Table.from_pandas(data),\n                path,\n                compression=compress,\n            )\n\n    elif fmt == \"csv\":\n        if is_relation(data):\n            _copy_to(data, path, \"CSV\", \"HEADER TRUE\")\n        else:\n            data.to_csv(path, index=False)\n\n    elif fmt in (\"xlsx\", \"excel\"):\n        df = collect(data) if is_relation(data) else data\n        try:\n            df.to_excel(path, index=False, engine=\"openpyxl\")\n        except ImportError:\n            raise ImportError(\"Install openpyxl: pip install climasus4py[excel]\")\n\n    else:\n        raise ValueError(f\"Unsupported format: {fmt}. Use parquet, csv, or xlsx.\")\n\n    return path\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_cache_info","title":"sus_cache_info","text":"<pre><code>sus_cache_info(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n) -&gt; pd.DataFrame\n</code></pre> <p>List all cached Parquet files with metadata.</p> <p>Walks cache_dir recursively and collects file-level statistics for every <code>.parquet</code> file found.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>str | Path</code> <p>Root directory of the Parquet cache. Defaults to <code>\"dados/cache\"</code>.</p> <code>_DEFAULT_CACHE</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with columns: <code>file</code> (basename),</p> <code>DataFrame</code> <p><code>path</code> (full path string), <code>size_mb</code> (file size in MB),</p> <code>DataFrame</code> <p>and <code>modified</code> (ISO-format last-modified timestamp).</p> Example <p>import climasus as cs info = cs.sus_cache_info() info.sort_values(\"size_mb\", ascending=False).head()</p> Source code in <code>src/climasus/io/cache.py</code> <pre><code>def sus_cache_info(cache_dir: str | Path = _DEFAULT_CACHE) -&gt; pd.DataFrame:\n    \"\"\"List all cached Parquet files with metadata.\n\n    Walks *cache_dir* recursively and collects file-level statistics\n    for every ``.parquet`` file found.\n\n    Args:\n        cache_dir: Root directory of the Parquet cache. Defaults to\n            ``\"dados/cache\"``.\n\n    Returns:\n        ``pandas.DataFrame`` with columns: ``file`` (basename),\n        ``path`` (full path string), ``size_mb`` (file size in MB),\n        and ``modified`` (ISO-format last-modified timestamp).\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; info = cs.sus_cache_info()\n        &gt;&gt;&gt; info.sort_values(\"size_mb\", ascending=False).head()\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    records = []\n\n    if cache_dir.is_dir():\n        for pq_file in cache_dir.rglob(\"*.parquet\"):\n            stat = pq_file.stat()\n            records.append({\n                \"file\": pq_file.name,\n                \"path\": str(pq_file),\n                \"size_mb\": round(stat.st_size / (1024 * 1024), 2),\n                \"modified\": datetime.fromtimestamp(stat.st_mtime).isoformat(timespec=\"seconds\"),\n            })\n\n    return pd.DataFrame(records)\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_cache_clear","title":"sus_cache_clear","text":"<pre><code>sus_cache_clear(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    *,\n    system: str | None = None,\n    uf: str | None = None,\n    before: str | None = None,\n) -&gt; int\n</code></pre> <p>Delete cached Parquet files matching the given filters.</p> <p>All filters are applied as AND conditions. Files that satisfy every specified criterion are permanently deleted.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>str | Path</code> <p>Root directory of the Parquet cache.</p> <code>_DEFAULT_CACHE</code> <code>system</code> <code>str | None</code> <p>If provided, only delete files whose parent directory name equals this system identifier (e.g. <code>\"SIM-DO\"</code>).</p> <code>None</code> <code>uf</code> <code>str | None</code> <p>If provided, only delete files whose stem starts with <code>\"{UF}_\"</code> (case-insensitive match).</p> <code>None</code> <code>before</code> <code>str | None</code> <p>ISO date string (<code>\"YYYY-MM-DD\"</code>). Only files last-modified before this date are deleted.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of files deleted.</p> Example <p>import climasus as cs cs.sus_cache_clear(system=\"SIM-DO\", uf=\"SP\") 3 cs.sus_cache_clear(before=\"2024-01-01\")</p> Source code in <code>src/climasus/io/cache.py</code> <pre><code>def sus_cache_clear(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    *,\n    system: str | None = None,\n    uf: str | None = None,\n    before: str | None = None,\n) -&gt; int:\n    \"\"\"Delete cached Parquet files matching the given filters.\n\n    All filters are applied as AND conditions. Files that satisfy every\n    specified criterion are permanently deleted.\n\n    Args:\n        cache_dir: Root directory of the Parquet cache.\n        system: If provided, only delete files whose parent directory\n            name equals this system identifier (e.g. ``\"SIM-DO\"``).\n        uf: If provided, only delete files whose stem starts with\n            ``\"{UF}_\"`` (case-insensitive match).\n        before: ISO date string (``\"YYYY-MM-DD\"``). Only files\n            last-modified before this date are deleted.\n\n    Returns:\n        Number of files deleted.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.sus_cache_clear(system=\"SIM-DO\", uf=\"SP\")\n        3\n        &gt;&gt;&gt; cs.sus_cache_clear(before=\"2024-01-01\")\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    count = 0\n\n    if not cache_dir.is_dir():\n        return 0\n\n    for pq_file in cache_dir.rglob(\"*.parquet\"):\n        # Filter by system (subdirectory name)\n        if system and pq_file.parent.name != system:\n            continue\n        # Filter by UF (filename starts with UF_)\n        if uf and not pq_file.stem.startswith(uf.upper() + \"_\"):\n            continue\n        # Filter by modification date\n        if before:\n            mod_date = datetime.fromtimestamp(pq_file.stat().st_mtime)\n            cutoff = datetime.fromisoformat(before)\n            if mod_date &gt;= cutoff:\n                continue\n\n        pq_file.unlink()\n        count += 1\n\n    return count\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_climate","title":"sus_climate","text":"<pre><code>sus_climate(\n    data: DuckDBPyRelation | DataFrame,\n    climate: DataFrame,\n    *,\n    time_window: int = 0,\n    lags: list[int] | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Join health data with climate observations by municipality and date.</p> <p>Materialises the health data before joining (returns a <code>pandas.DataFrame</code>). The join uses the auto-detected municipality and date columns from data matched against <code>municipality_code</code> and <code>date</code> in climate. Lag columns are added by shifting the climate <code>date</code> forward by the specified number of days.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>climate</code> <code>DataFrame</code> <p><code>DataFrame</code> with columns <code>municipality_code</code>, <code>date</code>, and one or more climate variable columns.</p> required <code>time_window</code> <code>int</code> <p>Days before the health event to aggregate climate (currently unused; reserved for future windowed aggregation). <code>0</code> means same-day join.</p> <code>0</code> <code>lags</code> <code>list[int] | None</code> <p>List of lag offsets in days. For each value n, climate variables are added as <code>{col}_lag{n}d</code> columns aligned to the observation date.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with climate columns left-joined to the</p> <code>DataFrame</code> <p>health data, plus any requested lag columns.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no municipality or date column is found in data.</p> Example <p>import climasus as cs df = cs.sus_climate(rel, climate_df, lags=[7, 14]) df.filter(like=\"_lag7d\").columns</p> Source code in <code>src/climasus/enrichment/climate.py</code> <pre><code>def sus_climate(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    climate: pd.DataFrame,\n    *,\n    time_window: int = 0,\n    lags: list[int] | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Join health data with climate observations by municipality and date.\n\n    Materialises the health data before joining (returns a\n    ``pandas.DataFrame``). The join uses the auto-detected municipality\n    and date columns from *data* matched against ``municipality_code``\n    and ``date`` in *climate*. Lag columns are added by shifting the\n    climate ``date`` forward by the specified number of days.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        climate: ``DataFrame`` with columns ``municipality_code``,\n            ``date``, and one or more climate variable columns.\n        time_window: Days before the health event to aggregate climate\n            (currently unused; reserved for future windowed aggregation).\n            ``0`` means same-day join.\n        lags: List of lag offsets in days. For each value *n*, climate\n            variables are added as ``{col}_lag{n}d`` columns aligned to\n            the observation date.\n\n    Returns:\n        ``pandas.DataFrame`` with climate columns left-joined to the\n        health data, plus any requested lag columns.\n\n    Raises:\n        ValueError: If no municipality or date column is found in\n            *data*.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; df = cs.sus_climate(rel, climate_df, lags=[7, 14])\n        &gt;&gt;&gt; df.filter(like=\"_lag7d\").columns\n    \"\"\"\n    conn = get_connection()\n\n    # Materialize health data if needed\n    if isinstance(data, duckdb.DuckDBPyRelation):\n        health_df = collect(data)\n    else:\n        health_df = data\n\n    columns = list(health_df.columns)\n    geo_col = detect_geo_column(columns, level=\"municipality\")\n    date_col = detect_date_column(columns)\n\n    if not geo_col or not date_col:\n        raise ValueError(\n            \"Health data must have a municipality_code and date column for climate join.\"\n        )\n\n    # Ensure date columns are datetime\n    health_df[date_col] = pd.to_datetime(health_df[date_col], errors=\"coerce\")\n    climate[\"date\"] = pd.to_datetime(climate[\"date\"], errors=\"coerce\")\n\n    # Join on municipality + date\n    result = health_df.merge(\n        climate,\n        left_on=[geo_col, date_col],\n        right_on=[\"municipality_code\", \"date\"],\n        how=\"left\",\n        suffixes=(\"\", \"_clim\"),\n    )\n\n    # Add lags if requested\n    # Lag means climate N days BEFORE the health event, so we ADD lag_days\n    # to the climate date to align it with the future health event date.\n    if lags:\n        climate_cols = [c for c in climate.columns if c not in (\"municipality_code\", \"date\")]\n        for lag_days in lags:\n            lagged = climate.copy()\n            lagged[\"date\"] = lagged[\"date\"] + pd.Timedelta(days=lag_days)\n            lag_suffix = f\"_lag{lag_days}d\"\n            lagged = lagged.rename(columns={c: f\"{c}{lag_suffix}\" for c in climate_cols})\n            result = result.merge(\n                lagged,\n                left_on=[geo_col, date_col],\n                right_on=[\"municipality_code\", \"date\"],\n                how=\"left\",\n                suffixes=(\"\", lag_suffix),\n            )\n\n    return result\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_spatial","title":"sus_spatial","text":"<pre><code>sus_spatial(\n    data: DuckDBPyRelation | DataFrame,\n    *,\n    shapefile: \"gpd.GeoDataFrame | None\" = None,\n    geo_level: str = \"municipality\",\n    join_type: str = \"left\",\n) -&gt; \"gpd.GeoDataFrame\"\n</code></pre> <p>Join health data with Brazilian shapefiles.</p> <p>Materialises the health data before joining (returns a <code>geopandas.GeoDataFrame</code>). When shapefile is <code>None</code>, geometries are fetched automatically via <code>geobr</code>. The join key is the auto-detected geographic column in data matched against the corresponding identifier column in the shapefile.</p> <p>Requires <code>pip install climasus4py[spatial]</code>.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>shapefile</code> <code>'gpd.GeoDataFrame | None'</code> <p><code>GeoDataFrame</code> with geometries (e.g. from geobr). Auto-loaded via <code>geobr</code> when <code>None</code>.</p> <code>None</code> <code>geo_level</code> <code>str</code> <p>Geographic level of the join \u2014 <code>\"municipality\"</code> (default), <code>\"state\"</code>, or <code>\"region\"</code>.</p> <code>'municipality'</code> <code>join_type</code> <code>str</code> <p>Merge strategy \u2014 <code>\"left\"</code> (keep all health rows) or <code>\"inner\"</code> (keep only matched rows).</p> <code>'left'</code> <p>Returns:</p> Type Description <code>'gpd.GeoDataFrame'</code> <p><code>geopandas.GeoDataFrame</code> with health data and shapefile</p> <code>'gpd.GeoDataFrame'</code> <p>geometry merged on the appropriate code column.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>geopandas</code> or <code>geobr</code> are not installed.</p> <code>ValueError</code> <p>If no suitable geographic column is found in data or in the shapefile.</p> Example <p>import climasus as cs gdf = cs.sus_spatial(rel, geo_level=\"state\") gdf.plot(column=\"count\") cs.sus_spatial(rel, shapefile=custom_shp, ...               geo_level=\"municipality\")</p> Source code in <code>src/climasus/enrichment/spatial.py</code> <pre><code>def sus_spatial(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    *,\n    shapefile: \"gpd.GeoDataFrame | None\" = None,\n    geo_level: str = \"municipality\",\n    join_type: str = \"left\",\n) -&gt; \"gpd.GeoDataFrame\":\n    \"\"\"Join health data with Brazilian shapefiles.\n\n    Materialises the health data before joining (returns a\n    ``geopandas.GeoDataFrame``). When *shapefile* is ``None``,\n    geometries are fetched automatically via ``geobr``. The join key is\n    the auto-detected geographic column in *data* matched against the\n    corresponding identifier column in the shapefile.\n\n    Requires ``pip install climasus4py[spatial]``.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        shapefile: ``GeoDataFrame`` with geometries (e.g. from geobr).\n            Auto-loaded via ``geobr`` when ``None``.\n        geo_level: Geographic level of the join \u2014 ``\"municipality\"``\n            (default), ``\"state\"``, or ``\"region\"``.\n        join_type: Merge strategy \u2014 ``\"left\"`` (keep all health rows)\n            or ``\"inner\"`` (keep only matched rows).\n\n    Returns:\n        ``geopandas.GeoDataFrame`` with health data and shapefile\n        geometry merged on the appropriate code column.\n\n    Raises:\n        ImportError: If ``geopandas`` or ``geobr`` are not installed.\n        ValueError: If no suitable geographic column is found in *data*\n            or in the shapefile.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; gdf = cs.sus_spatial(rel, geo_level=\"state\")\n        &gt;&gt;&gt; gdf.plot(column=\"count\")\n        &gt;&gt;&gt; cs.sus_spatial(rel, shapefile=custom_shp,\n        ...               geo_level=\"municipality\")\n    \"\"\"\n    try:\n        import geopandas as gpd\n    except ImportError:\n        raise ImportError(\"Install spatial extras: pip install climasus4py[spatial]\")\n\n    # Materialize\n    df = collect(data) if isinstance(data, duckdb.DuckDBPyRelation) else data\n\n    # Auto-load shapefile via geobr\n    if shapefile is None:\n        try:\n            import geobr\n        except ImportError:\n            raise ImportError(\n                \"Provide a shapefile or install geobr: pip install climasus4py[spatial]\"\n            )\n\n        if geo_level == \"state\":\n            shapefile = geobr.read_state()\n        elif geo_level == \"region\":\n            shapefile = geobr.read_region()\n        else:\n            shapefile = geobr.read_municipality()\n\n    # Determine join column\n    from climasus.utils.data import detect_geo_column\n    join_col = detect_geo_column(list(df.columns), level=geo_level)\n\n    if not join_col:\n        raise ValueError(f\"No {geo_level}-level column found in data.\")\n\n    # Find matching column in shapefile\n    shape_candidates = {\n        \"municipality\": [\"code_muni\", \"CD_MUN\"],\n        \"state\": [\"abbrev_state\", \"SIGLA_UF\"],\n        \"region\": [\"name_region\", \"NM_REGIAO\"],\n    }\n    shape_col = None\n    for c in shape_candidates.get(geo_level, []):\n        if c in shapefile.columns:\n            shape_col = c\n            break\n\n    if not shape_col:\n        raise ValueError(f\"Cannot find join column in shapefile for level={geo_level}\")\n\n    # Cast join columns to string for compatibility\n    df[join_col] = df[join_col].astype(str)\n    shapefile[shape_col] = shapefile[shape_col].astype(str)\n\n    how = \"left\" if join_type == \"left\" else \"inner\"\n    result = shapefile.merge(df, left_on=shape_col, right_on=join_col, how=how)\n\n    return result\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_census","title":"sus_census","text":"<pre><code>sus_census(\n    data: DuckDBPyRelation | DataFrame,\n    census: DataFrame | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Join health data with census indicators.</p> <p>Materialises the health data before joining (returns a <code>pandas.DataFrame</code>). The join is performed on the auto-detected municipality column in data matched against the <code>municipality_code</code> column in census.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>census</code> <code>DataFrame | None</code> <p><code>DataFrame</code> with a <code>municipality_code</code> column plus census indicator columns. Pass <code>None</code> to attempt auto-loading (not yet implemented \u2014 raises <code>NotImplementedError</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with census columns left-joined to the</p> <code>DataFrame</code> <p>health data.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If census is <code>None</code> (auto-loading not yet implemented).</p> <code>ValueError</code> <p>If no municipality column is found in data, or if census lacks the required <code>municipality_code</code> column.</p> Example <p>import climasus as cs df = cs.sus_census(rel, census=ibge_df) df.columns.tolist() [..., 'pop_total', 'hdi']</p> Source code in <code>src/climasus/enrichment/census.py</code> <pre><code>def sus_census(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    census: pd.DataFrame | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Join health data with census indicators.\n\n    Materialises the health data before joining (returns a\n    ``pandas.DataFrame``). The join is performed on the auto-detected\n    municipality column in *data* matched against the\n    ``municipality_code`` column in *census*.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        census: ``DataFrame`` with a ``municipality_code`` column plus\n            census indicator columns. Pass ``None`` to attempt\n            auto-loading (not yet implemented \u2014 raises\n            ``NotImplementedError``).\n\n    Returns:\n        ``pandas.DataFrame`` with census columns left-joined to the\n        health data.\n\n    Raises:\n        NotImplementedError: If *census* is ``None`` (auto-loading not\n            yet implemented).\n        ValueError: If no municipality column is found in *data*, or if\n            *census* lacks the required ``municipality_code`` column.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; df = cs.sus_census(rel, census=ibge_df)\n        &gt;&gt;&gt; df.columns.tolist()\n        [..., 'pop_total', 'hdi']\n    \"\"\"\n    # Materialize\n    df = collect(data) if isinstance(data, duckdb.DuckDBPyRelation) else data\n\n    if census is None:\n        raise NotImplementedError(\n            \"Auto-loading census data not yet implemented in Python. \"\n            \"Provide a census DataFrame with municipality_code column.\"\n        )\n\n    join_col = detect_geo_column(list(df.columns), level=\"municipality\")\n    if not join_col:\n        raise ValueError(\"No municipality column found in health data.\")\n\n    df[join_col] = df[join_col].astype(str)\n    if \"municipality_code\" in census.columns:\n        census[\"municipality_code\"] = census[\"municipality_code\"].astype(str)\n        result = df.merge(census, left_on=join_col, right_on=\"municipality_code\", how=\"left\")\n    else:\n        raise ValueError(\"Census data must have a 'municipality_code' column.\")\n\n    return result\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_fill_gaps","title":"sus_fill_gaps","text":"<pre><code>sus_fill_gaps(\n    data: DataFrame,\n    *,\n    method: str = \"linear\",\n    group_col: str = \"municipality_code\",\n    date_col: str = \"date\",\n    max_gap: int | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Fill gaps in climate time series by interpolation or ML imputation.</p> <p>Operates on <code>pandas.DataFrame</code> (in-memory). Applies the chosen method independently within each group defined by group_col.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p><code>DataFrame</code> with climate time series. Must contain at least date_col, group_col, and numeric variable columns.</p> required <code>method</code> <code>str</code> <p>Gap-filling strategy:</p> <ul> <li><code>\"linear\"</code> \u2014 linear interpolation between valid values.</li> <li><code>\"spline\"</code> \u2014 cubic spline (requires \u2265 4 valid points per   group; falls back to linear otherwise).</li> <li><code>\"locf\"</code> \u2014 last observation carried forward   (forward fill).</li> <li><code>\"xgboost\"</code> \u2014 gradient-boosted tree imputation;   requires <code>pip install climasus4py[ml]</code>.</li> </ul> <code>'linear'</code> <code>group_col</code> <code>str</code> <p>Column name used to partition the series (e.g. <code>\"municipality_code\"</code> or a station identifier).</p> <code>'municipality_code'</code> <code>date_col</code> <code>str</code> <p>Column containing the observation date.</p> <code>'date'</code> <code>max_gap</code> <code>int | None</code> <p>Maximum number of consecutive <code>NaN</code> values to fill. <code>None</code> fills all gaps.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Copy of data with missing numeric values filled according to</p> <code>DataFrame</code> <p>method.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If method is <code>\"xgboost\"</code> and <code>xgboost</code> is not installed.</p> <code>ValueError</code> <p>If method is not one of the supported values.</p> Example <p>import climasus as cs filled = cs.sus_fill_gaps(climate_df, method=\"linear\") filled = cs.sus_fill_gaps(climate_df, method=\"locf\", max_gap=3) cs.sus_fill_gaps(climate_df, method=\"xgboost\", ...                  group_col=\"station_id\")</p> Source code in <code>src/climasus/enrichment/fill_gaps.py</code> <pre><code>def sus_fill_gaps(\n    data: pd.DataFrame,\n    *,\n    method: str = \"linear\",\n    group_col: str = \"municipality_code\",\n    date_col: str = \"date\",\n    max_gap: int | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Fill gaps in climate time series by interpolation or ML imputation.\n\n    Operates on ``pandas.DataFrame`` (in-memory). Applies the chosen\n    method independently within each group defined by *group_col*.\n\n    Args:\n        data: ``DataFrame`` with climate time series. Must contain at\n            least *date_col*, *group_col*, and numeric variable columns.\n        method: Gap-filling strategy:\n\n            - ``\"linear\"`` \u2014 linear interpolation between valid values.\n            - ``\"spline\"`` \u2014 cubic spline (requires \u2265 4 valid points per\n              group; falls back to linear otherwise).\n            - ``\"locf\"`` \u2014 last observation carried forward\n              (forward fill).\n            - ``\"xgboost\"`` \u2014 gradient-boosted tree imputation;\n              requires ``pip install climasus4py[ml]``.\n\n        group_col: Column name used to partition the series (e.g.\n            ``\"municipality_code\"`` or a station identifier).\n        date_col: Column containing the observation date.\n        max_gap: Maximum number of consecutive ``NaN`` values to fill.\n            ``None`` fills all gaps.\n\n    Returns:\n        Copy of *data* with missing numeric values filled according to\n        *method*.\n\n    Raises:\n        ImportError: If *method* is ``\"xgboost\"`` and ``xgboost`` is\n            not installed.\n        ValueError: If *method* is not one of the supported values.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; filled = cs.sus_fill_gaps(climate_df, method=\"linear\")\n        &gt;&gt;&gt; filled = cs.sus_fill_gaps(climate_df, method=\"locf\", max_gap=3)\n        &gt;&gt;&gt; cs.sus_fill_gaps(climate_df, method=\"xgboost\",\n        ...                  group_col=\"station_id\")\n    \"\"\"\n    data = data.copy()\n    data[date_col] = pd.to_datetime(data[date_col], errors=\"coerce\")\n\n    numeric_cols = data.select_dtypes(include=[np.number]).columns.tolist()\n    numeric_cols = [c for c in numeric_cols if c != group_col]\n\n    if method == \"linear\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.interpolate(method=\"linear\", limit=max_gap)\n        )\n\n    elif method == \"spline\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.interpolate(method=\"spline\", order=3, limit=max_gap)\n            if s.notna().sum() &gt;= 4 else s.interpolate(method=\"linear\", limit=max_gap)\n        )\n\n    elif method == \"locf\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.ffill(limit=max_gap)\n        )\n\n    elif method == \"xgboost\":\n        try:\n            import xgboost as xgb\n        except ImportError:\n            raise ImportError(\"Install ML extras: pip install climasus4py[ml]\")\n\n        # XGBoost imputation: train on non-missing, predict missing\n        for col in numeric_cols:\n            for _name, group in data.groupby(group_col):\n                mask = group[col].isna()\n                if not mask.any() or mask.all():\n                    continue\n\n                # Features: other numeric columns + temporal\n                feat_cols = [c for c in numeric_cols if c != col]\n                group_feat = group[feat_cols].copy()\n                group_feat[\"_day_of_year\"] = group[date_col].dt.dayofyear\n\n                train_mask = ~mask\n                if train_mask.sum() &lt; 5:\n                    continue\n\n                model = xgb.XGBRegressor(n_estimators=50, max_depth=3, verbosity=0)\n                model.fit(\n                    group_feat.loc[train_mask].fillna(0),\n                    group.loc[train_mask, col],\n                )\n                predictions = model.predict(group_feat.loc[mask].fillna(0))\n                data.loc[group.index[mask], col] = predictions\n\n    else:\n        raise ValueError(f\"Unknown method: {method}. Use linear, spline, locf, or xgboost.\")\n\n    return data\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_explore","title":"sus_explore","text":"<pre><code>sus_explore(what: str = 'systems') -&gt; dict\n</code></pre> <p>Browse climasus-data metadata interactively.</p> <p>Returns the requested metadata dictionary from climasus-data, useful for discovering available systems, disease groups, regions, and state codes before building a pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>what</code> <code>str</code> <p>Topic to browse. Accepted values:</p> <ul> <li><code>\"systems\"</code> \u2014 available SUS systems and their   properties.</li> <li><code>\"groups\"</code> \u2014 disease group definitions (core and   climate-sensitive).</li> <li><code>\"regions\"</code> \u2014 Brazilian macro-regions and member   states.</li> <li><code>\"uf\"</code> \u2014 2-letter state codes and names.</li> </ul> <code>'systems'</code> <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with the requested metadata. Structure depends on</p> <code>dict</code> <p>the what argument.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If what is not one of the accepted values.</p> Example <p>import climasus as cs systems = cs.sus_explore(\"systems\") list(systems.keys())[:3] cs.sus_explore(\"groups\")[\"climate_sensitive\"]</p> Source code in <code>src/climasus/utils/explore.py</code> <pre><code>def sus_explore(\n    what: str = \"systems\",\n) -&gt; dict:\n    \"\"\"Browse climasus-data metadata interactively.\n\n    Returns the requested metadata dictionary from climasus-data,\n    useful for discovering available systems, disease groups, regions,\n    and state codes before building a pipeline.\n\n    Args:\n        what: Topic to browse. Accepted values:\n\n            - ``\"systems\"`` \u2014 available SUS systems and their\n              properties.\n            - ``\"groups\"`` \u2014 disease group definitions (core and\n              climate-sensitive).\n            - ``\"regions\"`` \u2014 Brazilian macro-regions and member\n              states.\n            - ``\"uf\"`` \u2014 2-letter state codes and names.\n\n    Returns:\n        Dictionary with the requested metadata. Structure depends on\n        the *what* argument.\n\n    Raises:\n        ValueError: If *what* is not one of the accepted values.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; systems = cs.sus_explore(\"systems\")\n        &gt;&gt;&gt; list(systems.keys())[:3]\n        &gt;&gt;&gt; cs.sus_explore(\"groups\")[\"climate_sensitive\"]\n    \"\"\"\n    if what == \"systems\":\n        return load_systems()\n\n    elif what == \"groups\":\n        core = load_json(\"disease_groups/core.json\")\n        sensitive = load_json(\"disease_groups/climate_sensitive.json\")\n        return {\"core\": core, \"climate_sensitive\": sensitive}\n\n    elif what == \"regions\":\n        return load_regions()\n\n    elif what == \"uf\":\n        return load_uf_codes()\n\n    else:\n        raise ValueError(f\"Unknown topic: {what}. Use systems, groups, regions, or uf.\")\n</code></pre>"},{"location":"reference/climasus/#climasus.sus_quality","title":"sus_quality","text":"<pre><code>sus_quality(data: DuckDBPyRelation | DataFrame) -&gt; dict\n</code></pre> <p>Calculate data quality metrics for a SUS dataset.</p> <p>Computes row count, column count, and per-column completeness rate (percentage of non-null values). Works with both lazy DuckDB relations and materialised <code>pandas.DataFrame</code> objects.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Dataset to profile \u2014 a lazy <code>DuckDBPyRelation</code> or a <code>pandas.DataFrame</code>.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with the following keys:</p> <code>dict</code> <ul> <li><code>total_rows</code> (int): total number of rows.</li> </ul> <code>dict</code> <ul> <li><code>total_cols</code> (int): total number of columns.</li> </ul> <code>dict</code> <ul> <li><code>completeness</code> (dict[str, float]): per-column mapping of column name to percentage of non-null values (0\u2013100).</li> </ul> Example <p>import climasus as cs metrics = cs.sus_quality(rel) metrics[\"total_rows\"] 334303 isinstance(metrics.get(\"completeness\"), dict) True</p> Source code in <code>src/climasus/utils/quality.py</code> <pre><code>def sus_quality(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n) -&gt; dict:\n    \"\"\"Calculate data quality metrics for a SUS dataset.\n\n    Computes row count, column count, and per-column completeness rate\n    (percentage of non-null values). Works with both lazy DuckDB\n    relations and materialised ``pandas.DataFrame`` objects.\n\n    Args:\n        data: Dataset to profile \u2014 a lazy ``DuckDBPyRelation`` or a\n            ``pandas.DataFrame``.\n\n    Returns:\n        Dictionary with the following keys:\n\n        - `total_rows` (int): total number of rows.\n        - `total_cols` (int): total number of columns.\n        - `completeness` (dict[str, float]): per-column mapping\n            of column name to percentage of non-null values (0\u2013100).\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; metrics = cs.sus_quality(rel)\n        &gt;&gt;&gt; metrics[\"total_rows\"]\n        334303\n        &gt;&gt;&gt; isinstance(metrics.get(\"completeness\"), dict)\n        True\n    \"\"\"\n    conn = get_connection()\n\n    if is_relation(data):\n        columns = data.columns\n        total_rows = data.aggregate(\"count(*)\").fetchone()[0]\n\n        completeness = {}\n        for col in columns:\n            non_null = conn.sql(\n                f'SELECT COUNT(\"{col}\") FROM data WHERE \"{col}\" IS NOT NULL'\n            ).fetchone()[0]\n            completeness[col] = round(non_null / max(total_rows, 1) * 100, 1)\n    else:\n        total_rows = len(data)\n        columns = list(data.columns)\n        completeness = {\n            col: round(data[col].notna().sum() / max(total_rows, 1) * 100, 1)\n            for col in columns\n        }\n\n    return {\n        \"total_rows\": total_rows,\n        \"total_cols\": len(columns),\n        \"completeness\": completeness,\n    }\n</code></pre>"},{"location":"reference/climasus/#climasus.update_climasus_data","title":"update_climasus_data","text":"<pre><code>update_climasus_data(\n    repo_url: str = \"https://github.com/climasus/climasus-data.git\",\n    target_dir: str | None = None,\n    branch: str = \"main\",\n) -&gt; None\n</code></pre> <p>Baixa ou atualiza o reposit\u00f3rio climasus-data localmente.</p> <p>Normalmente n\u00e3o \u00e9 necess\u00e1rio quando <code>climasus-data</code> est\u00e1 instalado como pacote Python. \u00datil durante desenvolvimento ou para for\u00e7ar atualiza\u00e7\u00e3o dos arquivos de refer\u00eancia offline.</p> <p>Parameters:</p> Name Type Description Default <code>repo_url</code> <code>str</code> <p>URL do reposit\u00f3rio Git a clonar.</p> <code>'https://github.com/climasus/climasus-data.git'</code> <code>target_dir</code> <code>str | None</code> <p>Diret\u00f3rio de destino. Se <code>None</code>, usa a vari\u00e1vel de ambiente <code>CLIMASUS_DATA_DIR</code> ou o diret\u00f3rio do pacote instalado.</p> <code>None</code> <code>branch</code> <code>str</code> <p>Branch Git a clonar/atualizar. Padr\u00e3o: <code>\"main\"</code>.</p> <code>'main'</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Se target_dir existe mas n\u00e3o cont\u00e9m <code>manifest.json</code> (prote\u00e7\u00e3o contra remo\u00e7\u00e3o acidental).</p> <code>CalledProcessError</code> <p>Se o comando <code>git</code> falhar.</p> Example <p>import climasus as cs cs.update_climasus_data() Clonando climasus-data em ... climasus-data atualizado com sucesso.</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def update_climasus_data(repo_url: str = \"https://github.com/climasus/climasus-data.git\", target_dir: str | None = None, branch: str = \"main\") -&gt; None:\n    \"\"\"Baixa ou atualiza o reposit\u00f3rio climasus-data localmente.\n\n    Normalmente n\u00e3o \u00e9 necess\u00e1rio quando ``climasus-data`` est\u00e1 instalado\n    como pacote Python. \u00datil durante desenvolvimento ou para for\u00e7ar\n    atualiza\u00e7\u00e3o dos arquivos de refer\u00eancia offline.\n\n    Args:\n        repo_url: URL do reposit\u00f3rio Git a clonar.\n        target_dir: Diret\u00f3rio de destino. Se ``None``, usa a vari\u00e1vel de\n            ambiente ``CLIMASUS_DATA_DIR`` ou o diret\u00f3rio do pacote\n            instalado.\n        branch: Branch Git a clonar/atualizar. Padr\u00e3o: ``\"main\"``.\n\n    Raises:\n        RuntimeError: Se *target_dir* existe mas n\u00e3o cont\u00e9m\n            ``manifest.json`` (prote\u00e7\u00e3o contra remo\u00e7\u00e3o acidental).\n        subprocess.CalledProcessError: Se o comando ``git`` falhar.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.update_climasus_data()\n        Clonando climasus-data em ...\n        climasus-data atualizado com sucesso.\n    \"\"\"\n    if target_dir is None:\n        env = os.environ.get(\"CLIMASUS_DATA_DIR\")\n        if env:\n            target_dir = env\n        else:\n            # Tenta usar o diret\u00f3rio do pacote instalado\n            try:\n                target_dir = str(climasus_data.data_root())\n            except FileNotFoundError:\n                target_dir = str(Path(__file__).resolve().parent.parent.parent / \"climasus-data\")\n\n    target = Path(target_dir)\n    if target.exists() and (target / \".git\").is_dir():\n        # J\u00e1 existe: git pull\n        print(f\"Atualizando climasus-data em {target}...\")\n        subprocess.run([\"git\", \"-C\", str(target), \"pull\", \"origin\", branch], check=True)\n    elif target.exists():\n        # Existe mas n\u00e3o \u00e9 git: remove e clona\n        # Safety check: only remove if it looks like a climasus-data directory\n        if not (target / \"manifest.json\").is_file():\n            raise RuntimeError(\n                f\"Diret\u00f3rio {target} n\u00e3o parece ser climasus-data \"\n                f\"(manifest.json n\u00e3o encontrado). Remo\u00e7\u00e3o abortada por seguran\u00e7a.\"\n            )\n        print(f\"Removendo diret\u00f3rio existente e clonando climasus-data em {target}...\")\n        shutil.rmtree(target)\n        subprocess.run([\"git\", \"clone\", \"--depth\", \"1\", \"-b\", branch, repo_url, str(target)], check=True)\n    else:\n        # N\u00e3o existe: clona\n        print(f\"Clonando climasus-data em {target}...\")\n        subprocess.run([\"git\", \"clone\", \"--depth\", \"1\", \"-b\", branch, repo_url, str(target)], check=True)\n    print(\"climasus-data atualizado com sucesso.\")\n</code></pre>"},{"location":"reference/climasus/core/","title":"<code>climasus.core</code>","text":""},{"location":"reference/climasus/core/#climasus.core","title":"climasus.core","text":"<p>Subpacote core \u2014 importa\u00e7\u00e3o, limpeza, padroniza\u00e7\u00e3o, filtragem, vari\u00e1veis, agrega\u00e7\u00e3o e pipeline ETL.</p>"},{"location":"reference/climasus/core/aggregate/","title":"<code>climasus.core.aggregate</code>","text":""},{"location":"reference/climasus/core/aggregate/#climasus.core.aggregate","title":"climasus.core.aggregate","text":"<p>Aggregation \u2014 group by time/geography and summarise.</p> <p>Mirrors R: aggregate.R</p>"},{"location":"reference/climasus/core/aggregate/#climasus.core.aggregate-functions","title":"Functions","text":""},{"location":"reference/climasus/core/aggregate/#climasus.core.aggregate.sus_aggregate","title":"sus_aggregate","text":"<pre><code>sus_aggregate(\n    rel: DuckDBPyRelation,\n    *,\n    time: str = \"month\",\n    geo: str = \"state\",\n    extra_groups: list[str] | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Aggregate SUS data by time period and geography.</p> <p>Groups the relation by a temporal expression and a geographic level, counting rows and summarising recognised numeric columns. The relation stays lazy until materialised.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation produced by a previous pipeline step.</p> required <code>time</code> <code>str</code> <p>Temporal granularity \u2014 one of <code>\"year\"</code>, <code>\"quarter\"</code>, <code>\"month\"</code>, <code>\"week\"</code>, or <code>\"day\"</code>.</p> <code>'month'</code> <code>geo</code> <code>str</code> <p>Geographic aggregation level \u2014 one of <code>\"state\"</code>, <code>\"municipality\"</code>, <code>\"region\"</code>, or <code>\"country\"</code>.</p> <code>'state'</code> <code>extra_groups</code> <code>list[str] | None</code> <p>Additional column names to include in the GROUP BY clause, e.g. <code>[\"sex\", \"age_group\"]</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with columns: <code>time_group</code>, the resolved</p> <code>DuckDBPyRelation</code> <p>geo column, any extra group columns, <code>count</code>, and</p> <code>DuckDBPyRelation</code> <p><code>sum_*/mean_*</code> for any recognised numeric columns present.</p> Example <p>import climasus as cs agg = cs.sus_aggregate(rel, time=\"year\", geo=\"state\") agg.df().head() agg_sex = cs.sus_aggregate(rel, time=\"month\", ...                            geo=\"municipality\", ...                            extra_groups=[\"sex\"])</p> Source code in <code>src/climasus/core/aggregate.py</code> <pre><code>def sus_aggregate(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    time: str = \"month\",\n    geo: str = \"state\",\n    extra_groups: list[str] | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Aggregate SUS data by time period and geography.\n\n    Groups the relation by a temporal expression and a geographic level,\n    counting rows and summarising recognised numeric columns. The relation\n    stays lazy until materialised.\n\n    Args:\n        rel: Lazy DuckDB relation produced by a previous pipeline step.\n        time: Temporal granularity \u2014 one of ``\"year\"``, ``\"quarter\"``,\n            ``\"month\"``, ``\"week\"``, or ``\"day\"``.\n        geo: Geographic aggregation level \u2014 one of ``\"state\"``,\n            ``\"municipality\"``, ``\"region\"``, or ``\"country\"``.\n        extra_groups: Additional column names to include in the GROUP BY\n            clause, e.g. ``[\"sex\", \"age_group\"]``.\n\n    Returns:\n        Lazy DuckDB relation with columns: ``time_group``, the resolved\n        geo column, any extra group columns, ``count``, and\n        ``sum_*/mean_*`` for any recognised numeric columns present.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; agg = cs.sus_aggregate(rel, time=\"year\", geo=\"state\")\n        &gt;&gt;&gt; agg.df().head()\n        &gt;&gt;&gt; agg_sex = cs.sus_aggregate(rel, time=\"month\",\n        ...                            geo=\"municipality\",\n        ...                            extra_groups=[\"sex\"])\n    \"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n\n    group_cols: list[str] = []\n\n    # --- Time grouping ---\n    date_col = detect_date_column(columns)\n    if date_col:\n        date_cast = f'TRY_CAST(\"{date_col}\" AS DATE)'\n        time_expr = {\n            \"year\": f\"EXTRACT(YEAR FROM {date_cast})\",\n            \"quarter\": f\"EXTRACT(YEAR FROM {date_cast}) || '-Q' || EXTRACT(QUARTER FROM {date_cast})\",\n            \"month\": f\"STRFTIME({date_cast}, '%Y-%m')\",\n            \"week\": f\"STRFTIME({date_cast}, '%Y-W%W')\",\n            \"day\": f\"CAST({date_cast} AS VARCHAR)\",\n        }.get(time, f\"STRFTIME({date_cast}, '%Y-%m')\")\n\n        time_alias = \"time_group\"\n        group_cols.append(time_alias)\n    else:\n        time_expr = None\n        time_alias = None\n\n    # --- Geo grouping ---\n    geo_col = detect_geo_column(columns, level=geo)\n    if geo_col:\n        group_cols.append(f'\"{geo_col}\"')\n\n    # --- Extra groups ---\n    if extra_groups:\n        for eg in extra_groups:\n            if eg in columns:\n                group_cols.append(f'\"{eg}\"')\n\n    if not group_cols:\n        # No grouping possible \u2014 return count\n        return conn.sql(\"SELECT COUNT(*) AS count FROM rel\")\n\n    # Build SELECT\n    select_parts: list[str] = []\n    if time_expr:\n        select_parts.append(f\"{time_expr} AS {time_alias}\")\n    if geo_col:\n        select_parts.append(f'\"{geo_col}\"')\n    if extra_groups:\n        for eg in extra_groups:\n            if eg in columns:\n                select_parts.append(f'\"{eg}\"')\n\n    select_parts.append(\"COUNT(*) AS count\")\n\n    # Numeric columns \u2192 SUM and MEAN\n    numeric_candidates = [\"count\", \"deaths\", \"admissions\", \"cases\", \"value\"]\n    for nc in numeric_candidates:\n        if nc in columns and nc not in [g.strip('\"') for g in group_cols]:\n            select_parts.append(f'SUM(TRY_CAST(\"{nc}\" AS DOUBLE)) AS sum_{nc}')\n            select_parts.append(f'AVG(TRY_CAST(\"{nc}\" AS DOUBLE)) AS mean_{nc}')\n\n    group_by = \", \".join(group_cols)\n    select = \", \".join(select_parts)\n\n    return conn.sql(f\"SELECT {select} FROM rel GROUP BY {group_by} ORDER BY {group_by}\")\n</code></pre>"},{"location":"reference/climasus/core/clean/","title":"<code>climasus.core.clean</code>","text":""},{"location":"reference/climasus/core/clean/#climasus.core.clean","title":"climasus.core.clean","text":"<p>Data cleaning \u2014 deduplication and encoding fixes.</p> <p>Mirrors R: clean.R</p>"},{"location":"reference/climasus/core/clean/#climasus.core.clean-functions","title":"Functions","text":""},{"location":"reference/climasus/core/clean/#climasus.core.clean.sus_clean","title":"sus_clean","text":"<pre><code>sus_clean(\n    rel: DuckDBPyRelation,\n    *,\n    fix_enc: bool = True,\n    dedup: bool = True,\n    dedup_cols: list[str] | None = None,\n    age_range: tuple[int, int] = (0, 120),\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Clean SUS data: deduplicate, fix encoding, and validate age range.</p> <p>All operations remain lazy (DuckDB relation) until materialised. Deduplication uses ROW_NUMBER() over known DATASUS key columns for performance. Age validation decodes the SIM-DO 3-digit coded age field before applying numeric range filters.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to clean.</p> required <code>fix_enc</code> <code>bool</code> <p>If <code>True</code>, schedule encoding fixes (applied at standardisation time when columns are collected).</p> <code>True</code> <code>dedup</code> <code>bool</code> <p>If <code>True</code>, remove duplicate records.</p> <code>True</code> <code>dedup_cols</code> <code>list[str] | None</code> <p>Columns to use for deduplication. If <code>None</code>, uses known DATASUS key columns (faster). Pass <code>[\"*\"]</code> to force a full-row DISTINCT.</p> <code>None</code> <code>age_range</code> <code>tuple[int, int]</code> <p><code>(min_age, max_age)</code> tuple in years. Records with decoded age outside this range are dropped. Handles the DATASUS 3-digit coding scheme for SIM-DO IDADE fields.</p> <code>(0, 120)</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with duplicates removed and invalid ages</p> <code>DuckDBPyRelation</code> <p>filtered out.</p> Example <p>import climasus as cs clean = cs.sus_clean(rel, age_range=(0, 110)) clean = cs.sus_clean(rel, dedup_cols=[\"CONTADOR\"])</p> Source code in <code>src/climasus/core/clean.py</code> <pre><code>def sus_clean(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    fix_enc: bool = True,\n    dedup: bool = True,\n    dedup_cols: list[str] | None = None,\n    age_range: tuple[int, int] = (0, 120),\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Clean SUS data: deduplicate, fix encoding, and validate age range.\n\n    All operations remain lazy (DuckDB relation) until materialised.\n    Deduplication uses ROW_NUMBER() over known DATASUS key columns for\n    performance. Age validation decodes the SIM-DO 3-digit coded age\n    field before applying numeric range filters.\n\n    Args:\n        rel: Lazy DuckDB relation to clean.\n        fix_enc: If ``True``, schedule encoding fixes (applied at\n            standardisation time when columns are collected).\n        dedup: If ``True``, remove duplicate records.\n        dedup_cols: Columns to use for deduplication. If ``None``, uses\n            known DATASUS key columns (faster). Pass ``[\"*\"]`` to force\n            a full-row DISTINCT.\n        age_range: ``(min_age, max_age)`` tuple in years. Records with\n            decoded age outside this range are dropped. Handles the\n            DATASUS 3-digit coding scheme for SIM-DO IDADE fields.\n\n    Returns:\n        Lazy DuckDB relation with duplicates removed and invalid ages\n        filtered out.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; clean = cs.sus_clean(rel, age_range=(0, 110))\n        &gt;&gt;&gt; clean = cs.sus_clean(rel, dedup_cols=[\"CONTADOR\"])\n    \"\"\"\n    columns = schema_columns(rel)\n\n    if dedup:\n        if dedup_cols and dedup_cols != [\"*\"]:\n            key_cols = [c for c in dedup_cols if c in columns]\n        else:\n            # Use key columns for SUS data instead of full-row distinct\n            key_candidates = [\n                \"CONTADOR\", \"NUMERODO\", \"NUMERODN\", \"N_AIH\", \"NU_NOTIFIC\",\n                \"counter\", \"record_id\",\n            ]\n            key_cols = [c for c in key_candidates if c in columns]\n\n        if key_cols:\n            # ROW_NUMBER() dedup \u2014 keeps first row per key, much faster\n            conn = get_connection()\n            partition = \", \".join(f'\"{c}\"' for c in key_cols)\n            all_cols = \", \".join(f'\"{c}\"' for c in columns)\n            # ORDER BY partition keys for deterministic results\n            rel = conn.sql(\n                f\"SELECT {all_cols} FROM (\"\n                f\"  SELECT *, ROW_NUMBER() OVER (PARTITION BY {partition} ORDER BY {partition}) AS __rn\"\n                f\"  FROM rel\"\n                f\") WHERE __rn = 1\"\n            )\n        else:\n            # No key column found \u2014 fall back to full distinct\n            rel = rel.distinct()\n\n    columns = schema_columns(rel)\n\n    # Age validation\n    # DATASUS SIM-DO encodes age as a 3-digit string: 1st digit = unit code,\n    # remaining = value.  Code 4 = years, 3 = months, 2 = days, 1 = hours,\n    # 0 = minutes, 5 = 100+ years.\n    # We decode to age_years before filtering.\n    age_col = None\n    for candidate in (\"IDADE\", \"age\", \"age_years\"):\n        if candidate in columns:\n            age_col = candidate\n            break\n\n    if age_col:\n        lo, hi = age_range\n        conn = get_connection()\n\n        # Decode SIM-DO coded age to years\n        decoded_expr = (\n            f'CASE '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'5\\' '\n            f'    THEN 100 + TRY_CAST(SUBSTR(TRIM(\"{age_col}\"), 2) AS INTEGER) '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'4\\' '\n            f'    THEN TRY_CAST(SUBSTR(TRIM(\"{age_col}\"), 2) AS INTEGER) '\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) = \\'3\\' '\n            f'    THEN 0  '  # months \u2192 0 years (infant)\n            f'  WHEN LENGTH(TRIM(\"{age_col}\")) = 3 AND SUBSTR(TRIM(\"{age_col}\"), 1, 1) IN (\\'0\\', \\'1\\', \\'2\\') '\n            f'    THEN 0  '  # minutes/hours/days \u2192 0 years (infant)\n            f'  ELSE TRY_CAST(\"{age_col}\" AS INTEGER) '\n            f'END'\n        )\n\n        rel = conn.sql(\n            f'SELECT *, ({decoded_expr}) AS __age_years FROM rel'\n        )\n        rel = rel.filter(\n            f'__age_years IS NULL '\n            f'OR (__age_years &gt;= {lo} AND __age_years &lt;= {hi})'\n        )\n        # Drop helper column\n        final_cols = [c for c in schema_columns(rel) if c != \"__age_years\"]\n        rel = rel.project(\", \".join(f'\"{c}\"' for c in final_cols))\n\n    # Note: encoding fixes for string columns require materialization\n    # or a UDF. For now, we mark that encoding fix should happen at\n    # standardization time when we collect column subsets.\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/core/engine/","title":"<code>climasus.core.engine</code>","text":""},{"location":"reference/climasus/core/engine/#climasus.core.engine","title":"climasus.core.engine","text":"<p>DuckDB engine management.</p> <p>Mirrors R: engine.R \u2014 lazy evaluation via DuckDB instead of duckplyr.</p>"},{"location":"reference/climasus/core/engine/#climasus.core.engine-functions","title":"Functions","text":""},{"location":"reference/climasus/core/engine/#climasus.core.engine.get_connection","title":"get_connection","text":"<pre><code>get_connection() -&gt; duckdb.DuckDBPyConnection\n</code></pre> <p>Return the shared DuckDB in-memory connection, creating it if needed.</p> <p>Uses a module-level singleton so all pipeline steps share the same connection and can reference each other's views and temporary tables.</p> <p>Returns:</p> Type Description <code>DuckDBPyConnection</code> <p>An open <code>duckdb.DuckDBPyConnection</code> backed by <code>:memory:</code>.</p> Example <p>conn = get_connection() conn.sql(\"SELECT 1 AS x\").df()</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def get_connection() -&gt; duckdb.DuckDBPyConnection:\n    \"\"\"Return the shared DuckDB in-memory connection, creating it if needed.\n\n    Uses a module-level singleton so all pipeline steps share the same\n    connection and can reference each other's views and temporary tables.\n\n    Returns:\n        An open ``duckdb.DuckDBPyConnection`` backed by ``:memory:``.\n\n    Example:\n        &gt;&gt;&gt; conn = get_connection()\n        &gt;&gt;&gt; conn.sql(\"SELECT 1 AS x\").df()\n    \"\"\"\n    global _conn\n    if _conn is None:\n        _conn = duckdb.connect(\":memory:\")\n    return _conn\n</code></pre>"},{"location":"reference/climasus/core/engine/#climasus.core.engine.read_parquets","title":"read_parquets","text":"<pre><code>read_parquets(\n    paths: list[str | Path],\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Read one or more Parquet files as a lazy DuckDB relation.</p> <p>Python equivalent of <code>duckplyr::read_parquet_duckdb()</code> in R. No data is loaded into memory until <code>.df()</code> or <code>.fetchdf()</code> is called. Multiple files are combined with <code>union_by_name=True</code> to handle schema differences across years (e.g. a new column that did not exist in a prior year).</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>list[str | Path]</code> <p>One or more paths to <code>.parquet</code> files.</p> required <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy <code>duckdb.DuckDBPyRelation</code> over the given files.</p> Example <p>rel = read_parquets([\"dados/cache/SIM-DO/SP_2022_all.parquet\"]) rel.columns</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def read_parquets(paths: list[str | Path]) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Read one or more Parquet files as a lazy DuckDB relation.\n\n    Python equivalent of ``duckplyr::read_parquet_duckdb()`` in R.\n    No data is loaded into memory until ``.df()`` or ``.fetchdf()`` is\n    called. Multiple files are combined with ``union_by_name=True`` to\n    handle schema differences across years (e.g. a new column that\n    did not exist in a prior year).\n\n    Args:\n        paths: One or more paths to ``.parquet`` files.\n\n    Returns:\n        Lazy ``duckdb.DuckDBPyRelation`` over the given files.\n\n    Example:\n        &gt;&gt;&gt; rel = read_parquets([\"dados/cache/SIM-DO/SP_2022_all.parquet\"])\n        &gt;&gt;&gt; rel.columns\n    \"\"\"\n    conn = get_connection()\n    str_paths = [str(p) for p in paths]\n    if len(str_paths) == 1:\n        return conn.read_parquet(str_paths[0])\n    return conn.read_parquet(str_paths, union_by_name=True)\n</code></pre>"},{"location":"reference/climasus/core/engine/#climasus.core.engine.is_relation","title":"is_relation","text":"<pre><code>is_relation(obj: object) -&gt; bool\n</code></pre> <p>Check whether an object is a lazy DuckDB relation.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>object</code> <p>Any Python object to test.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if obj is a <code>duckdb.DuckDBPyRelation</code>, <code>False</code></p> <code>bool</code> <p>otherwise.</p> Example <p>is_relation(conn.sql(\"SELECT 1\")) True is_relation(pd.DataFrame()) False</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def is_relation(obj: object) -&gt; bool:\n    \"\"\"Check whether an object is a lazy DuckDB relation.\n\n    Args:\n        obj: Any Python object to test.\n\n    Returns:\n        ``True`` if *obj* is a ``duckdb.DuckDBPyRelation``, ``False``\n        otherwise.\n\n    Example:\n        &gt;&gt;&gt; is_relation(conn.sql(\"SELECT 1\"))\n        True\n        &gt;&gt;&gt; is_relation(pd.DataFrame())\n        False\n    \"\"\"\n    return isinstance(obj, duckdb.DuckDBPyRelation)\n</code></pre>"},{"location":"reference/climasus/core/engine/#climasus.core.engine.collect","title":"collect","text":"<pre><code>collect(rel: DuckDBPyRelation) -&gt; 'pd.DataFrame'\n</code></pre> <p>Materialise a DuckDB relation to a pandas DataFrame.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to execute and collect.</p> required <p>Returns:</p> Type Description <code>'pd.DataFrame'</code> <p><code>pandas.DataFrame</code> with the full result set in memory.</p> Example <p>df = collect(rel) type(df)  Source code in <code>src/climasus/core/engine.py</code> <pre><code>def collect(rel: duckdb.DuckDBPyRelation) -&gt; \"pd.DataFrame\":\n    \"\"\"Materialise a DuckDB relation to a pandas DataFrame.\n\n    Args:\n        rel: Lazy DuckDB relation to execute and collect.\n\n    Returns:\n        ``pandas.DataFrame`` with the full result set in memory.\n\n    Example:\n        &gt;&gt;&gt; df = collect(rel)\n        &gt;&gt;&gt; type(df)\n        &lt;class 'pandas.core.frame.DataFrame'&gt;\n    \"\"\"\n    return rel.df()\n</code></pre>"},{"location":"reference/climasus/core/engine/#climasus.core.engine.collect_arrow","title":"collect_arrow","text":"<pre><code>collect_arrow(rel: DuckDBPyRelation) -&gt; 'pa.Table'\n</code></pre> <p>Materialise a DuckDB relation to a PyArrow Table.</p> <p>Significantly faster than <code>collect()</code> for large datasets (~100\u00d7) since DuckDB transfers data in zero-copy Arrow format. If DuckDB returns a <code>RecordBatchReader</code>, <code>.read_all()</code> is called to normalise the result to a proper <code>pyarrow.Table</code> with <code>.num_rows</code> / <code>.num_columns</code>.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to execute and collect.</p> required <p>Returns:</p> Type Description <code>'pa.Table'</code> <p><code>pyarrow.Table</code> with the full result set.</p> Example <p>import climasus as cs table = cs.collect_arrow(rel) table.num_rows 334303</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def collect_arrow(rel: duckdb.DuckDBPyRelation) -&gt; \"pa.Table\":\n    \"\"\"Materialise a DuckDB relation to a PyArrow Table.\n\n    Significantly faster than ``collect()`` for large datasets (~100\u00d7)\n    since DuckDB transfers data in zero-copy Arrow format. If DuckDB\n    returns a ``RecordBatchReader``, ``.read_all()`` is called to\n    normalise the result to a proper ``pyarrow.Table`` with\n    ``.num_rows`` / ``.num_columns``.\n\n    Args:\n        rel: Lazy DuckDB relation to execute and collect.\n\n    Returns:\n        ``pyarrow.Table`` with the full result set.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; table = cs.collect_arrow(rel)\n        &gt;&gt;&gt; table.num_rows\n        334303\n    \"\"\"\n    result = rel.arrow()\n    # Some DuckDB versions return RecordBatchReader instead of Table\n    if hasattr(result, \"read_all\"):\n        return result.read_all()\n    return result\n</code></pre>"},{"location":"reference/climasus/core/engine/#climasus.core.engine.schema_columns","title":"schema_columns","text":"<pre><code>schema_columns(rel: DuckDBPyRelation) -&gt; list[str]\n</code></pre> <p>Return column names of a DuckDB relation without materialising data.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of column name strings in schema order.</p> Example <p>schema_columns(rel) ['DTOBITO', 'CAUSABAS', 'IDADE', ...]</p> Source code in <code>src/climasus/core/engine.py</code> <pre><code>def schema_columns(rel: duckdb.DuckDBPyRelation) -&gt; list[str]:\n    \"\"\"Return column names of a DuckDB relation without materialising data.\n\n    Args:\n        rel: Lazy DuckDB relation.\n\n    Returns:\n        List of column name strings in schema order.\n\n    Example:\n        &gt;&gt;&gt; schema_columns(rel)\n        ['DTOBITO', 'CAUSABAS', 'IDADE', ...]\n    \"\"\"\n    return rel.columns\n</code></pre>"},{"location":"reference/climasus/core/filter/","title":"<code>climasus.core.filter</code>","text":""},{"location":"reference/climasus/core/filter/#climasus.core.filter","title":"climasus.core.filter","text":"<p>Disease and demographic filtering.</p> <p>Mirrors R: filter.R + utils-cid.R</p>"},{"location":"reference/climasus/core/filter/#climasus.core.filter-functions","title":"Functions","text":""},{"location":"reference/climasus/core/filter/#climasus.core.filter.sus_filter","title":"sus_filter","text":"<pre><code>sus_filter(\n    rel: DuckDBPyRelation,\n    *,\n    groups: str | list[str] | None = None,\n    codes: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    sex: str | None = None,\n    race: str | list[str] | None = None,\n    uf: str | list[str] | None = None,\n    municipality: str | list[str] | None = None,\n    date_start: str | None = None,\n    date_end: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Filter SUS data by disease groups, demographics, geography, and dates.</p> <p>All predicates are applied as DuckDB <code>WHERE</code> clauses; the relation stays lazy until materialised. CID-10 codes are resolved via <code>codes_for_groups</code> and <code>expand_cid_ranges</code>; geographic and demographic columns are auto-detected from the relation schema.</p> <p>Parameters:</p> Name Type Description Default <code>rel</code> <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation to filter.</p> required <code>groups</code> <code>str | list[str] | None</code> <p>Named disease group(s) from climasus-data, e.g. <code>\"respiratory\"</code> or <code>[\"cardiovascular\", \"dengue\"]</code>.</p> <code>None</code> <code>codes</code> <code>str | list[str] | None</code> <p>Explicit ICD-10 code(s) or ranges, e.g. <code>[\"J00-J99\", \"A90\"]</code>.</p> <code>None</code> <code>age_min</code> <code>int | None</code> <p>Minimum age in years (inclusive). Decoded from DATASUS coded age when the column is in SIM-DO format.</p> <code>None</code> <code>age_max</code> <code>int | None</code> <p>Maximum age in years (inclusive).</p> <code>None</code> <code>sex</code> <code>str | None</code> <p>Sex code to keep \u2014 <code>\"M\"</code> (male) or <code>\"F\"</code> (female).</p> <code>None</code> <code>race</code> <code>str | list[str] | None</code> <p><code>RACACOR</code> code(s) to keep, e.g. <code>[\"1\", \"4\"]</code>.</p> <code>None</code> <code>uf</code> <code>str | list[str] | None</code> <p>One or more Brazilian state abbreviations, e.g. <code>[\"SP\", \"RJ\"]</code>.</p> <code>None</code> <code>municipality</code> <code>str | list[str] | None</code> <p>Municipality code(s) (IBGE 6-digit), e.g. <code>[\"355030\"]</code>.</p> <code>None</code> <code>date_start</code> <code>str | None</code> <p>Earliest event date (inclusive), ISO format <code>\"YYYY-MM-DD\"</code>.</p> <code>None</code> <code>date_end</code> <code>str | None</code> <p>Latest event date (inclusive), ISO format <code>\"YYYY-MM-DD\"</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation</code> <p>Lazy DuckDB relation with all specified filters applied.</p> Example <p>import climasus as cs filtered = cs.sus_filter(rel, groups=\"respiratory\", ...                          age_min=15, age_max=64, uf=\"SP\") cs.sus_filter(rel, codes=[\"A90\", \"A91\"], sex=\"F\").count()</p> Source code in <code>src/climasus/core/filter.py</code> <pre><code>def sus_filter(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    groups: str | list[str] | None = None,\n    codes: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    sex: str | None = None,\n    race: str | list[str] | None = None,\n    uf: str | list[str] | None = None,\n    municipality: str | list[str] | None = None,\n    date_start: str | None = None,\n    date_end: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Filter SUS data by disease groups, demographics, geography, and dates.\n\n    All predicates are applied as DuckDB ``WHERE`` clauses; the relation\n    stays lazy until materialised. CID-10 codes are resolved via\n    ``codes_for_groups`` and ``expand_cid_ranges``; geographic and\n    demographic columns are auto-detected from the relation schema.\n\n    Args:\n        rel: Lazy DuckDB relation to filter.\n        groups: Named disease group(s) from climasus-data, e.g.\n            ``\"respiratory\"`` or ``[\"cardiovascular\", \"dengue\"]``.\n        codes: Explicit ICD-10 code(s) or ranges, e.g.\n            ``[\"J00-J99\", \"A90\"]``.\n        age_min: Minimum age in years (inclusive). Decoded from DATASUS\n            coded age when the column is in SIM-DO format.\n        age_max: Maximum age in years (inclusive).\n        sex: Sex code to keep \u2014 ``\"M\"`` (male) or ``\"F\"`` (female).\n        race: ``RACACOR`` code(s) to keep, e.g. ``[\"1\", \"4\"]``.\n        uf: One or more Brazilian state abbreviations, e.g.\n            ``[\"SP\", \"RJ\"]``.\n        municipality: Municipality code(s) (IBGE 6-digit), e.g.\n            ``[\"355030\"]``.\n        date_start: Earliest event date (inclusive), ISO format\n            ``\"YYYY-MM-DD\"``.\n        date_end: Latest event date (inclusive), ISO format\n            ``\"YYYY-MM-DD\"``.\n\n    Returns:\n        Lazy DuckDB relation with all specified filters applied.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; filtered = cs.sus_filter(rel, groups=\"respiratory\",\n        ...                          age_min=15, age_max=64, uf=\"SP\")\n        &gt;&gt;&gt; cs.sus_filter(rel, codes=[\"A90\", \"A91\"], sex=\"F\").count()\n    \"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n\n    # --- Disease filtering (CID-10) ---\n    icd_codes: list[str] = []\n    if groups:\n        group_list = [groups] if isinstance(groups, str) else groups\n        icd_codes.extend(codes_for_groups(group_list))\n    if codes:\n        code_list = [codes] if isinstance(codes, str) else codes\n        icd_codes.extend(expand_cid_ranges(code_list))\n\n    if icd_codes:\n        cause_col = detect_cause_column(columns)\n        if cause_col:\n            unique_codes = sorted(set(icd_codes))\n            if len(unique_codes) &lt;= 100:\n                # Direct IN filter\n                codes_str = \", \".join(f\"'{c}'\" for c in unique_codes)\n                rel = rel.filter(f'\"{cause_col}\" IN ({codes_str})')\n            else:\n                # Semi-join via temporary table for large code lists\n                codes_sql = \", \".join(f\"('{c}')\" for c in unique_codes)\n                conn.execute(\n                    f\"CREATE OR REPLACE TEMP TABLE _icd_filter AS \"\n                    f\"SELECT * FROM (VALUES {codes_sql}) AS t(code)\"\n                )\n                rel = conn.sql(\n                    f'SELECT r.* FROM rel r SEMI JOIN _icd_filter f '\n                    f'ON SUBSTR(r.\"{cause_col}\", 1, 3) = f.code'\n                )\n\n    # --- Age filtering ---\n    age_col = detect_age_column(columns)\n    if age_col and (age_min is not None or age_max is not None):\n        decoded = decode_age_sql(age_col)\n        conditions = []\n        if age_min is not None:\n            conditions.append(f'({decoded}) &gt;= {age_min}')\n        if age_max is not None:\n            conditions.append(f'({decoded}) &lt;= {age_max}')\n        rel = rel.filter(\" AND \".join(conditions))\n\n    # --- Sex filtering ---\n    sex_col = detect_sex_column(columns)\n    if sex_col and sex:\n        sex_val = sex.upper()\n        rel = rel.filter(f'\"{sex_col}\" = \\'{sex_val}\\'')\n\n    # --- Race filtering ---\n    if race:\n        race_list = [race] if isinstance(race, str) else race\n        for candidate in (\"RACACOR\", \"race\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{r}'\" for r in race_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- UF filtering ---\n    if uf:\n        uf_list = [uf] if isinstance(uf, str) else uf\n        for candidate in (\"UF\", \"SG_UF\", \"state\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{u.upper()}'\" for u in uf_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- Municipality filtering ---\n    if municipality:\n        muni_list = [municipality] if isinstance(municipality, str) else municipality\n        for candidate in (\"CODMUNRES\", \"municipality_code\", \"ID_MUNICIP\"):\n            if candidate in columns:\n                vals = \", \".join(f\"'{m}'\" for m in muni_list)\n                rel = rel.filter(f'\"{candidate}\" IN ({vals})')\n                break\n\n    # --- Date range filtering ---\n    if date_start or date_end:\n        for candidate in (\"death_date\", \"date\", \"DTOBITO\", \"DTNASC\"):\n            if candidate in columns:\n                if date_start:\n                    rel = rel.filter(\n                        f'TRY_CAST(\"{candidate}\" AS DATE) &gt;= \\'{date_start}\\''\n                    )\n                if date_end:\n                    rel = rel.filter(\n                        f'TRY_CAST(\"{candidate}\" AS DATE) &lt;= \\'{date_end}\\''\n                    )\n                break\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/core/importer/","title":"<code>climasus.core.importer</code>","text":""},{"location":"reference/climasus/core/importer/#climasus.core.importer","title":"climasus.core.importer","text":"<p>Data import \u2014 download from DATASUS and cache as parquet.</p> <p>Mirrors R: import.R + download-aria2c.R Preferred reader: readdbc (pure Python, no C compiler). Fallback chain: readdbc \u2192 pyreaddbc \u2192 pysus \u2192 dbc2dbf CLI.</p>"},{"location":"reference/climasus/core/importer/#climasus.core.importer-functions","title":"Functions","text":""},{"location":"reference/climasus/core/importer/#climasus.core.importer.sus_import","title":"sus_import","text":"<pre><code>sus_import(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    month: int | list[int] | None = None,\n    *,\n    cache: bool = True,\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    timeout: int = 600,\n    verbose: bool = True,\n    path: str | Path | None = None,\n    data: DataFrame | None = None,\n    backend: Literal[\"auto\", \"ftp\", \"pysus\"] = \"auto\",\n) -&gt; \"duckdb.DuckDBPyRelation\"\n</code></pre> <p>Import SUS data and return a lazy DuckDB relation.</p> <p>Supports three input modes:</p> <ol> <li><code>data=</code> \u2014 wrap an existing <code>pandas.DataFrame</code>.</li> <li><code>path=</code> \u2014 read from a local <code>.parquet</code> or <code>.csv</code> file.</li> <li>Default \u2014 download from the DATASUS FTP, convert <code>.dbc</code> to    Parquet and cache locally; subsequent calls read from cache.</li> </ol> <p>When downloading (mode 3), the backend controls which client is used:</p> <ul> <li><code>\"auto\"</code>  \u2014 FTP direct download (no extra deps), falls back to   PySUS if installed.</li> <li><code>\"ftp\"</code>   \u2014 FTP + <code>.dbc</code> reader chain: <code>readdbc</code> \u2192   <code>pyreaddbc</code> \u2192 <code>pysus</code> \u2192 <code>dbc2dbf</code> CLI.</li> <li><code>\"pysus\"</code> \u2014 PySUS high-level API (requires   <code>pip install pysus</code>; needs C compiler on Windows).</li> </ul> <p>Parameters:</p> Name Type Description Default <code>system</code> <code>str</code> <p>SUS system identifier, e.g. <code>\"SIM-DO\"</code>, <code>\"SINASC\"</code>, or <code>\"SIH-RD\"</code>.</p> required <code>uf</code> <code>str | list[str]</code> <p>State abbreviation(s), e.g. <code>\"SP\"</code> or <code>[\"SP\", \"RJ\"]</code>. Use <code>\"all\"</code> for all states or a region name such as <code>\"Sudeste\"</code>.</p> required <code>year</code> <code>int | list[int]</code> <p>Year(s) to import, e.g. <code>2022</code> or <code>[2020, 2021, 2022]</code>.</p> required <code>month</code> <code>int | list[int] | None</code> <p>Month(s) to import (SIH only). <code>None</code> downloads all 12 months.</p> <code>None</code> <code>cache</code> <code>bool</code> <p>If <code>True</code>, skip download when a cached Parquet exists.</p> <code>True</code> <code>cache_dir</code> <code>str | Path</code> <p>Root directory for the Parquet cache.</p> <code>_DEFAULT_CACHE</code> <code>timeout</code> <code>int</code> <p>Download timeout in seconds.</p> <code>600</code> <code>verbose</code> <code>bool</code> <p>Print progress messages via Rich.</p> <code>True</code> <code>path</code> <code>str | Path | None</code> <p>Local file path to use instead of downloading.</p> <code>None</code> <code>data</code> <code>DataFrame | None</code> <p>Existing <code>DataFrame</code> to wrap instead of downloading.</p> <code>None</code> <code>backend</code> <code>Literal['auto', 'ftp', 'pysus']</code> <p>Download backend \u2014 <code>\"auto\"</code>, <code>\"ftp\"</code>, or <code>\"pysus\"</code>.</p> <code>'auto'</code> <p>Returns:</p> Type Description <code>'duckdb.DuckDBPyRelation'</code> <p>Lazy <code>duckdb.DuckDBPyRelation</code> over the imported data.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If no data could be imported (download failed or no cache hit).</p> <code>ValueError</code> <p>If an unsupported file format is supplied via path.</p> <code>ImportError</code> <p>If <code>.dbc</code> reading is attempted but no backend is available.</p> Example <p>import climasus as cs rel = cs.sus_import(\"SIM-DO\", \"SP\", 2022) rel.count() 334303 cs.sus_import(\"SIM-DO\", \"SP\", 2022, ...               path=\"dados/cache/SP_2022.parquet\")</p> Source code in <code>src/climasus/core/importer.py</code> <pre><code>def sus_import(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    month: int | list[int] | None = None,\n    *,\n    cache: bool = True,\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    timeout: int = 600,\n    verbose: bool = True,\n    path: str | Path | None = None,\n    data: pd.DataFrame | None = None,\n    backend: Literal[\"auto\", \"ftp\", \"pysus\"] = \"auto\",\n) -&gt; \"duckdb.DuckDBPyRelation\":\n    \"\"\"Import SUS data and return a lazy DuckDB relation.\n\n    Supports three input modes:\n\n    1. **``data=``** \u2014 wrap an existing ``pandas.DataFrame``.\n    2. **``path=``** \u2014 read from a local ``.parquet`` or ``.csv`` file.\n    3. **Default** \u2014 download from the DATASUS FTP, convert ``.dbc`` to\n       Parquet and cache locally; subsequent calls read from cache.\n\n    When downloading (mode 3), the *backend* controls which client is\n    used:\n\n    - ``\"auto\"``  \u2014 FTP direct download (no extra deps), falls back to\n      PySUS if installed.\n    - ``\"ftp\"``   \u2014 FTP + ``.dbc`` reader chain: ``readdbc`` \u2192\n      ``pyreaddbc`` \u2192 ``pysus`` \u2192 ``dbc2dbf`` CLI.\n    - ``\"pysus\"`` \u2014 PySUS high-level API (requires\n      ``pip install pysus``; needs C compiler on Windows).\n\n    Args:\n        system: SUS system identifier, e.g. ``\"SIM-DO\"``, ``\"SINASC\"``,\n            or ``\"SIH-RD\"``.\n        uf: State abbreviation(s), e.g. ``\"SP\"`` or ``[\"SP\", \"RJ\"]``.\n            Use ``\"all\"`` for all states or a region name such as\n            ``\"Sudeste\"``.\n        year: Year(s) to import, e.g. ``2022`` or\n            ``[2020, 2021, 2022]``.\n        month: Month(s) to import (SIH only). ``None`` downloads all 12\n            months.\n        cache: If ``True``, skip download when a cached Parquet exists.\n        cache_dir: Root directory for the Parquet cache.\n        timeout: Download timeout in seconds.\n        verbose: Print progress messages via Rich.\n        path: Local file path to use instead of downloading.\n        data: Existing ``DataFrame`` to wrap instead of downloading.\n        backend: Download backend \u2014 ``\"auto\"``, ``\"ftp\"``, or\n            ``\"pysus\"``.\n\n    Returns:\n        Lazy ``duckdb.DuckDBPyRelation`` over the imported data.\n\n    Raises:\n        RuntimeError: If no data could be imported (download failed or\n            no cache hit).\n        ValueError: If an unsupported file format is supplied via\n            *path*.\n        ImportError: If ``.dbc`` reading is attempted but no backend is\n            available.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; rel = cs.sus_import(\"SIM-DO\", \"SP\", 2022)\n        &gt;&gt;&gt; rel.count()\n        334303\n        &gt;&gt;&gt; cs.sus_import(\"SIM-DO\", \"SP\", 2022,\n        ...               path=\"dados/cache/SP_2022.parquet\")\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    ufs = resolve_uf(uf)\n    years = [year] if isinstance(year, int) else list(year)\n    months = [month] if isinstance(month, int) else (month or [None])\n\n    parquet_paths: list[Path] = []\n\n    if data is not None:\n        # Mode 1: inline data\n        data = _coerce_datasus_types(data.copy())\n        target = cache_dir / system / f\"inline_{'_'.join(ufs)}_{years[0]}.parquet\"\n        target.parent.mkdir(parents=True, exist_ok=True)\n        pq.write_table(pa.Table.from_pandas(data), target)\n        parquet_paths.append(target)\n\n    elif path is not None:\n        # Mode 2: local file\n        p = Path(path)\n        if p.suffix == \".parquet\":\n            df = pq.read_table(p).to_pandas()\n        elif p.suffix == \".csv\":\n            df = pd.read_csv(p)\n        else:\n            raise ValueError(f\"Unsupported file format: {p.suffix}\")\n        target = cache_dir / system / f\"file_{'_'.join(ufs)}_{years[0]}.parquet\"\n        target.parent.mkdir(parents=True, exist_ok=True)\n        pq.write_table(pa.Table.from_pandas(df), target)\n        parquet_paths.append(target)\n\n    else:\n        # Mode 3: download from DATASUS\n        needed: list[dict] = []\n        for one_uf in ufs:\n            for one_year in years:\n                for one_month in months:\n                    month_str = str(one_month) if one_month else \"all\"\n                    target = (\n                        cache_dir / system / f\"{one_uf}_{one_year}_{month_str}.parquet\"\n                    )\n                    if cache and target.is_file():\n                        parquet_paths.append(target)\n                    else:\n                        needed.append({\n                            \"uf\": one_uf,\n                            \"year\": one_year,\n                            \"month\": one_month,\n                            \"target\": target,\n                        })\n\n        if needed:\n            use_pysus = (\n                backend == \"pysus\"\n                or (backend == \"auto\" and _pysus_available() and system in _PYSUS_SYSTEM_MAP)\n            )\n\n            engine_label = \"PySUS\" if use_pysus else \"FTP\"\n            if verbose:\n                console.print(\n                    f\"[cyan]\u2139[/] Downloading {len(needed)} file(s) via {engine_label}...\"\n                )\n\n            for item in needed:\n                result: Path | None = None\n\n                if use_pysus:\n                    try:\n                        df = _download_pysus(\n                            system, item[\"uf\"], item[\"year\"], item[\"month\"]\n                        )\n                        df = _coerce_datasus_types(df)\n                        item[\"target\"].parent.mkdir(parents=True, exist_ok=True)\n                        pq.write_table(pa.Table.from_pandas(df), item[\"target\"])\n                        result = item[\"target\"]\n                        if verbose:\n                            console.print(\n                                f\"[green]\u2714[/]  {item['uf']}_{item['year']} ({len(df):,} rows)\"\n                            )\n                    except Exception as e:\n                        if verbose:\n                            console.print(\n                                f\"[red]\u2717[/]  {item['uf']}_{item['year']}: {e}\"\n                            )\n                else:\n                    result = _download_and_cache(\n                        system,\n                        item[\"uf\"],\n                        item[\"year\"],\n                        item[\"month\"],\n                        item[\"target\"],\n                        verbose,\n                    )\n\n                if result:\n                    parquet_paths.append(result)\n\n    if not parquet_paths:\n        raise RuntimeError(\"No data imported \u2014 check system/uf/year parameters.\")\n\n    return read_parquets(parquet_paths)\n</code></pre>"},{"location":"reference/climasus/core/pipeline/","title":"<code>climasus.core.pipeline</code>","text":""},{"location":"reference/climasus/core/pipeline/#climasus.core.pipeline","title":"climasus.core.pipeline","text":"<p>Main pipeline \u2014 orchestrates the full ETL chain.</p> <p>Mirrors R: pipeline.R + pipeline-fast.R</p>"},{"location":"reference/climasus/core/pipeline/#climasus.core.pipeline-functions","title":"Functions","text":""},{"location":"reference/climasus/core/pipeline/#climasus.core.pipeline.sus_pipeline","title":"sus_pipeline","text":"<pre><code>sus_pipeline(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    *,\n    lang: str = \"en\",\n    groups: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    age_group: str | list[int] | None = None,\n    time: str = \"month\",\n    geo: str = \"state\",\n    epi_week: bool = False,\n    output: str | Path | None = None,\n    cache_dir: str | Path = Path(\"dados/cache\"),\n    verbose: bool = True,\n    **kwargs: Any,\n) -&gt; duckdb.DuckDBPyRelation | pd.DataFrame\n</code></pre> <p>Run the full SUS ETL pipeline: import \u2192 clean \u2192 standardise \u2192 filter \u2192 variables \u2192 aggregate.</p> <p>Main entry point for most users. Mirrors <code>sus_pipeline()</code> from the R package and uses a single-CTE SQL fast path when the requested operations allow it (same optimisation as R <code>rc_a</code>). Falls back to the staged pipeline for complex operations such as custom age groups or epidemiological-week breakdowns.</p> <p>Parameters:</p> Name Type Description Default <code>system</code> <code>str</code> <p>SUS system identifier, e.g. <code>\"SIM-DO\"</code> or <code>\"SINASC\"</code>.</p> required <code>uf</code> <code>str | list[str]</code> <p>State abbreviation(s), <code>\"all\"</code>, or a region name.</p> required <code>year</code> <code>int | list[int]</code> <p>Year(s) to process, e.g. <code>2022</code> or <code>[2020, 2021, 2022]</code>.</p> required <code>lang</code> <code>str</code> <p>Output language for column names \u2014 <code>\"en\"</code> (default), <code>\"pt\"</code>, or <code>\"es\"</code>.</p> <code>'en'</code> <code>groups</code> <code>str | list[str] | None</code> <p>Disease group name(s) or <code>None</code> to include all causes.</p> <code>None</code> <code>age_min</code> <code>int | None</code> <p>Minimum age in years to retain.</p> <code>None</code> <code>age_max</code> <code>int | None</code> <p>Maximum age in years to retain.</p> <code>None</code> <code>age_group</code> <code>str | list[int] | None</code> <p>Age grouping scheme \u2014 <code>\"who\"</code>, <code>\"decadal\"</code>, a custom list of breakpoints, or <code>None</code> to skip.</p> <code>None</code> <code>time</code> <code>str</code> <p>Temporal aggregation granularity \u2014 <code>\"year\"</code>, <code>\"quarter\"</code>, <code>\"month\"</code> (default), <code>\"week\"</code>, or <code>\"day\"</code>.</p> <code>'month'</code> <code>geo</code> <code>str</code> <p>Geographic aggregation level \u2014 <code>\"state\"</code> (default) or <code>\"municipality\"</code>.</p> <code>'state'</code> <code>epi_week</code> <code>bool</code> <p>If <code>True</code>, add an <code>epi_week</code> column (disables fast path).</p> <code>False</code> <code>output</code> <code>str | Path | None</code> <p>Optional file path to export results (parquet / csv / xlsx).</p> <code>None</code> <code>cache_dir</code> <code>str | Path</code> <p>Root directory for the Parquet cache.</p> <code>Path('dados/cache')</code> <code>verbose</code> <code>bool</code> <p>Print progress messages via Rich.</p> <code>True</code> <code>**kwargs</code> <code>Any</code> <p>Additional keyword arguments forwarded to :func:<code>~climasus.core.importer.sus_import</code>.</p> <code>{}</code> <p>Returns:</p> Type Description <code>DuckDBPyRelation | DataFrame</code> <p>Lazy <code>duckdb.DuckDBPyRelation</code> with aggregated results, or a</p> <code>DuckDBPyRelation | DataFrame</code> <p><code>pandas.DataFrame</code> when output forces materialisation.</p> Example <p>import climasus as cs result = cs.sus_pipeline(\"SIM-DO\", \"SP\", 2022, ...                          groups=\"respiratory\", time=\"month\") result.df().head() cs.sus_pipeline(\"SIM-DO\", [\"SP\", \"RJ\"], [2020, 2021], ...                  output=\"output/mortality.parquet\")</p> Source code in <code>src/climasus/core/pipeline.py</code> <pre><code>def sus_pipeline(\n    system: str,\n    uf: str | list[str],\n    year: int | list[int],\n    *,\n    lang: str = \"en\",\n    groups: str | list[str] | None = None,\n    age_min: int | None = None,\n    age_max: int | None = None,\n    age_group: str | list[int] | None = None,\n    time: str = \"month\",\n    geo: str = \"state\",\n    epi_week: bool = False,\n    output: str | Path | None = None,\n    cache_dir: str | Path = Path(\"dados/cache\"),\n    verbose: bool = True,\n    **kwargs: Any,\n) -&gt; duckdb.DuckDBPyRelation | pd.DataFrame:\n    \"\"\"Run the full SUS ETL pipeline: import \u2192 clean \u2192 standardise \u2192 filter \u2192 variables \u2192 aggregate.\n\n    Main entry point for most users. Mirrors ``sus_pipeline()`` from the\n    R package and uses a single-CTE SQL fast path when the requested\n    operations allow it (same optimisation as R ``rc_a``). Falls back to\n    the staged pipeline for complex operations such as custom age groups\n    or epidemiological-week breakdowns.\n\n    Args:\n        system: SUS system identifier, e.g. ``\"SIM-DO\"`` or\n            ``\"SINASC\"``.\n        uf: State abbreviation(s), ``\"all\"``, or a region name.\n        year: Year(s) to process, e.g. ``2022`` or\n            ``[2020, 2021, 2022]``.\n        lang: Output language for column names \\u2014 ``\"en\"`` (default),\n            ``\"pt\"``, or ``\"es\"``.\n        groups: Disease group name(s) or ``None`` to include all causes.\n        age_min: Minimum age in years to retain.\n        age_max: Maximum age in years to retain.\n        age_group: Age grouping scheme \\u2014 ``\"who\"``, ``\"decadal\"``,\n            a custom list of breakpoints, or ``None`` to skip.\n        time: Temporal aggregation granularity \\u2014 ``\"year\"``,\n            ``\"quarter\"``, ``\"month\"`` (default), ``\"week\"``, or\n            ``\"day\"``.\n        geo: Geographic aggregation level \\u2014 ``\"state\"`` (default) or\n            ``\"municipality\"``.\n        epi_week: If ``True``, add an ``epi_week`` column (disables fast\n            path).\n        output: Optional file path to export results\n            (parquet / csv / xlsx).\n        cache_dir: Root directory for the Parquet cache.\n        verbose: Print progress messages via Rich.\n        **kwargs: Additional keyword arguments forwarded to\n            :func:`~climasus.core.importer.sus_import`.\n\n    Returns:\n        Lazy ``duckdb.DuckDBPyRelation`` with aggregated results, or a\n        ``pandas.DataFrame`` when *output* forces materialisation.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; result = cs.sus_pipeline(\"SIM-DO\", \"SP\", 2022,\n        ...                          groups=\"respiratory\", time=\"month\")\n        &gt;&gt;&gt; result.df().head()\n        &gt;&gt;&gt; cs.sus_pipeline(\"SIM-DO\", [\"SP\", \"RJ\"], [2020, 2021],\n        ...                  output=\"output/mortality.parquet\")\n    \"\"\"\n    group_list = [groups] if isinstance(groups, str) else groups\n\n    # Step 1: Import (always needed \u2014 resolves UFs, discovers/downloads parquets)\n    rel = sus_import(system, uf, year, cache_dir=cache_dir, verbose=verbose, **kwargs)\n\n    # --- Try fast path: single CTE query like R rc_a ---\n    if _can_fast_path(age_group, epi_week, time, geo):\n        # Resolve parquet paths from cache\n        from climasus.utils.data import resolve_uf\n\n        ufs = resolve_uf(uf)\n        years = [year] if isinstance(year, int) else list(year)\n        cache_path = Path(cache_dir)\n        parquet_paths = [\n            cache_path / system / f\"{u}_{y}_all.parquet\"\n            for u in ufs\n            for y in years\n        ]\n        parquet_paths = [p for p in parquet_paths if p.is_file()]\n\n        if parquet_paths:\n            sql = _build_fast_sql(\n                parquet_paths, group_list, age_min, age_max, time, geo\n            )\n            if sql:\n                conn = get_connection()\n                try:\n                    result = conn.sql(sql)\n                    if output:\n                        sus_export(result, output)\n                    return result\n                except Exception:\n                    logging.getLogger(__name__).debug(\n                        \"Fast path failed, falling through to staged pipeline\",\n                        exc_info=True,\n                    )\n\n    # --- Staged pipeline (fallback) ---\n    rel = sus_clean(rel)\n    rel = sus_standardize(rel, lang=lang, system=system)\n    rel = sus_filter(rel, groups=group_list, age_min=age_min, age_max=age_max)\n    rel = sus_variables(rel, age_group=age_group, epi_week=epi_week)\n    rel = sus_aggregate(rel, time=time, geo=geo)\n\n    if output:\n        sus_export(rel, output)\n\n    return rel\n</code></pre>"},{"location":"reference/climasus/core/standardize/","title":"<code>climasus.core.standardize</code>","text":""},{"location":"reference/climasus/core/standardize/#climasus.core.standardize","title":"climasus.core.standardize","text":"<p>Data standardization - column renaming, type conversion, translations.</p> <p>Mirrors R: standardize.R</p>"},{"location":"reference/climasus/core/standardize/#climasus.core.standardize-functions","title":"Functions","text":""},{"location":"reference/climasus/core/standardize/#climasus.core.standardize.sus_standardize","title":"sus_standardize","text":"<pre><code>sus_standardize(\n    rel: DuckDBPyRelation,\n    *,\n    lang: str = \"en\",\n    system: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Standardize column names, translate labels, and convert date columns.</p> Source code in <code>src/climasus/core/standardize.py</code> <pre><code>def sus_standardize(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    lang: str = \"en\",\n    system: str | None = None,\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Standardize column names, translate labels, and convert date columns.\"\"\"\n    columns = schema_columns(rel)\n\n    if system is None:\n        system = detect_system(columns)\n\n    col_map = _load_column_dict(lang)\n    renames = {col: col_map[col] for col in columns if col in col_map}\n\n    if renames:\n        projections = []\n        for col in columns:\n            if col in renames:\n                projections.append(f'\"{col}\" AS \"{renames[col]}\"')\n            else:\n                projections.append(f'\"{col}\"')\n        conn = get_connection()\n        rel = conn.sql(f\"SELECT {', '.join(projections)} FROM rel\")\n\n    # Date conversion (DDMMYYYY -&gt; DATE), robust for both VARCHAR and DATE/TIMESTAMP.\n    # We avoid TRY/EXCEPT to prevent leaving transaction context aborted in DuckDB.\n    new_columns = schema_columns(rel)\n    date_candidates = [\n        \"death_date\",\n        \"date\",\n        \"DTOBITO\",\n        \"DTNASC\",\n        \"admission_date\",\n        \"birth_date\",\n        \"case_conclusion_date\",\n    ]\n\n    for dc in date_candidates:\n        if dc in new_columns:\n            rel = rel.project(\n                \", \".join(\n                    (\n                        f\"COALESCE(\"\n                        f\"TRY_STRPTIME(CAST(\\\"{dc}\\\" AS VARCHAR), '%d%m%Y')::DATE, \"\n                        f\"TRY_CAST(\\\"{dc}\\\" AS DATE)\"\n                        f\") AS \\\"{dc}\\\"\"\n                    )\n                    if c == dc\n                    else f'\"{c}\"'\n                    for c in new_columns\n                )\n            )\n\n    rel = rel.set_alias(system or \"unknown\")\n    return rel\n</code></pre>"},{"location":"reference/climasus/core/variables/","title":"<code>climasus.core.variables</code>","text":""},{"location":"reference/climasus/core/variables/#climasus.core.variables","title":"climasus.core.variables","text":"<p>Derived variable creation - age groups, epi weeks, seasons.</p> <p>Mirrors R: variables.R</p>"},{"location":"reference/climasus/core/variables/#climasus.core.variables-functions","title":"Functions","text":""},{"location":"reference/climasus/core/variables/#climasus.core.variables.sus_variables","title":"sus_variables","text":"<pre><code>sus_variables(\n    rel: DuckDBPyRelation,\n    *,\n    age_group: str | list[int] | None = None,\n    epi_week: bool = False,\n    season: bool = False,\n    quarter: bool = False,\n    month_name: bool = False,\n    day_of_week: bool = False,\n    hemisphere: str = \"south\",\n) -&gt; duckdb.DuckDBPyRelation\n</code></pre> <p>Create derived variables from SUS data (stays lazy).</p> Source code in <code>src/climasus/core/variables.py</code> <pre><code>def sus_variables(\n    rel: duckdb.DuckDBPyRelation,\n    *,\n    age_group: str | list[int] | None = None,\n    epi_week: bool = False,\n    season: bool = False,\n    quarter: bool = False,\n    month_name: bool = False,\n    day_of_week: bool = False,\n    hemisphere: str = \"south\",\n) -&gt; duckdb.DuckDBPyRelation:\n    \"\"\"Create derived variables from SUS data (stays lazy).\"\"\"\n    columns = schema_columns(rel)\n    conn = get_connection()\n    projections = [f'\"{c}\"' for c in columns]\n\n    age_col = detect_age_column(columns)\n    if age_col and age_group:\n        if isinstance(age_group, list):\n            breaks = sorted(age_group + [999])\n        else:\n            breaks = _age_breaks_for_preset(age_group)\n\n        case_parts = []\n        decoded = decode_age_sql(age_col)\n        for i in range(len(breaks) - 1):\n            lo, hi = breaks[i], breaks[i + 1] - 1\n            is_last = i == len(breaks) - 2\n            label = f\"{lo}+\" if is_last else f\"{lo}-{hi}\"\n            case_parts.append(\n                f\"WHEN ({decoded}) BETWEEN {lo} AND {hi} \"\n                f\"THEN '{label}'\"\n            )\n        case_sql = f'CASE {\" \".join(case_parts)} ELSE \\'unknown\\' END AS \"age_group\"'\n        projections.append(case_sql)\n\n    date_col = detect_date_column(columns)\n    if date_col:\n        date_cast = f'TRY_CAST(\"{date_col}\" AS DATE)'\n\n        if epi_week:\n            projections.append(f\"STRFTIME({date_cast}, '%Y-W%W') AS epi_week\")\n\n        if season:\n            projections.append(f\"{_season_case_sql(date_cast, hemisphere)} AS season\")\n\n        if quarter:\n            projections.append(f\"'Q' || EXTRACT(QUARTER FROM {date_cast}) AS quarter\")\n\n        if month_name:\n            projections.append(f\"STRFTIME({date_cast}, '%B') AS month_name\")\n\n        if day_of_week:\n            projections.append(f\"STRFTIME({date_cast}, '%A') AS day_of_week\")\n\n    rel = conn.sql(f\"SELECT {', '.join(projections)} FROM rel\")\n    return rel\n</code></pre>"},{"location":"reference/climasus/enrichment/","title":"<code>climasus.enrichment</code>","text":""},{"location":"reference/climasus/enrichment/#climasus.enrichment","title":"climasus.enrichment","text":"<p>Subpacote enrichment \u2014 enriquecimento com dados clim\u00e1ticos, censo, shapefile e preenchimento de lacunas.</p>"},{"location":"reference/climasus/enrichment/census/","title":"<code>climasus.enrichment.census</code>","text":""},{"location":"reference/climasus/enrichment/census/#climasus.enrichment.census","title":"climasus.enrichment.census","text":"<p>Census data enrichment.</p> <p>Mirrors R: census.R</p>"},{"location":"reference/climasus/enrichment/census/#climasus.enrichment.census-functions","title":"Functions","text":""},{"location":"reference/climasus/enrichment/census/#climasus.enrichment.census.sus_census","title":"sus_census","text":"<pre><code>sus_census(\n    data: DuckDBPyRelation | DataFrame,\n    census: DataFrame | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Join health data with census indicators.</p> <p>Materialises the health data before joining (returns a <code>pandas.DataFrame</code>). The join is performed on the auto-detected municipality column in data matched against the <code>municipality_code</code> column in census.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>census</code> <code>DataFrame | None</code> <p><code>DataFrame</code> with a <code>municipality_code</code> column plus census indicator columns. Pass <code>None</code> to attempt auto-loading (not yet implemented \u2014 raises <code>NotImplementedError</code>).</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with census columns left-joined to the</p> <code>DataFrame</code> <p>health data.</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If census is <code>None</code> (auto-loading not yet implemented).</p> <code>ValueError</code> <p>If no municipality column is found in data, or if census lacks the required <code>municipality_code</code> column.</p> Example <p>import climasus as cs df = cs.sus_census(rel, census=ibge_df) df.columns.tolist() [..., 'pop_total', 'hdi']</p> Source code in <code>src/climasus/enrichment/census.py</code> <pre><code>def sus_census(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    census: pd.DataFrame | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Join health data with census indicators.\n\n    Materialises the health data before joining (returns a\n    ``pandas.DataFrame``). The join is performed on the auto-detected\n    municipality column in *data* matched against the\n    ``municipality_code`` column in *census*.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        census: ``DataFrame`` with a ``municipality_code`` column plus\n            census indicator columns. Pass ``None`` to attempt\n            auto-loading (not yet implemented \u2014 raises\n            ``NotImplementedError``).\n\n    Returns:\n        ``pandas.DataFrame`` with census columns left-joined to the\n        health data.\n\n    Raises:\n        NotImplementedError: If *census* is ``None`` (auto-loading not\n            yet implemented).\n        ValueError: If no municipality column is found in *data*, or if\n            *census* lacks the required ``municipality_code`` column.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; df = cs.sus_census(rel, census=ibge_df)\n        &gt;&gt;&gt; df.columns.tolist()\n        [..., 'pop_total', 'hdi']\n    \"\"\"\n    # Materialize\n    df = collect(data) if isinstance(data, duckdb.DuckDBPyRelation) else data\n\n    if census is None:\n        raise NotImplementedError(\n            \"Auto-loading census data not yet implemented in Python. \"\n            \"Provide a census DataFrame with municipality_code column.\"\n        )\n\n    join_col = detect_geo_column(list(df.columns), level=\"municipality\")\n    if not join_col:\n        raise ValueError(\"No municipality column found in health data.\")\n\n    df[join_col] = df[join_col].astype(str)\n    if \"municipality_code\" in census.columns:\n        census[\"municipality_code\"] = census[\"municipality_code\"].astype(str)\n        result = df.merge(census, left_on=join_col, right_on=\"municipality_code\", how=\"left\")\n    else:\n        raise ValueError(\"Census data must have a 'municipality_code' column.\")\n\n    return result\n</code></pre>"},{"location":"reference/climasus/enrichment/climate/","title":"<code>climasus.enrichment.climate</code>","text":""},{"location":"reference/climasus/enrichment/climate/#climasus.enrichment.climate","title":"climasus.enrichment.climate","text":"<p>Climate data enrichment \u2014 join health + climate data.</p> <p>Mirrors R: climate.R</p>"},{"location":"reference/climasus/enrichment/climate/#climasus.enrichment.climate-functions","title":"Functions","text":""},{"location":"reference/climasus/enrichment/climate/#climasus.enrichment.climate.sus_climate","title":"sus_climate","text":"<pre><code>sus_climate(\n    data: DuckDBPyRelation | DataFrame,\n    climate: DataFrame,\n    *,\n    time_window: int = 0,\n    lags: list[int] | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Join health data with climate observations by municipality and date.</p> <p>Materialises the health data before joining (returns a <code>pandas.DataFrame</code>). The join uses the auto-detected municipality and date columns from data matched against <code>municipality_code</code> and <code>date</code> in climate. Lag columns are added by shifting the climate <code>date</code> forward by the specified number of days.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>climate</code> <code>DataFrame</code> <p><code>DataFrame</code> with columns <code>municipality_code</code>, <code>date</code>, and one or more climate variable columns.</p> required <code>time_window</code> <code>int</code> <p>Days before the health event to aggregate climate (currently unused; reserved for future windowed aggregation). <code>0</code> means same-day join.</p> <code>0</code> <code>lags</code> <code>list[int] | None</code> <p>List of lag offsets in days. For each value n, climate variables are added as <code>{col}_lag{n}d</code> columns aligned to the observation date.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with climate columns left-joined to the</p> <code>DataFrame</code> <p>health data, plus any requested lag columns.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If no municipality or date column is found in data.</p> Example <p>import climasus as cs df = cs.sus_climate(rel, climate_df, lags=[7, 14]) df.filter(like=\"_lag7d\").columns</p> Source code in <code>src/climasus/enrichment/climate.py</code> <pre><code>def sus_climate(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    climate: pd.DataFrame,\n    *,\n    time_window: int = 0,\n    lags: list[int] | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Join health data with climate observations by municipality and date.\n\n    Materialises the health data before joining (returns a\n    ``pandas.DataFrame``). The join uses the auto-detected municipality\n    and date columns from *data* matched against ``municipality_code``\n    and ``date`` in *climate*. Lag columns are added by shifting the\n    climate ``date`` forward by the specified number of days.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        climate: ``DataFrame`` with columns ``municipality_code``,\n            ``date``, and one or more climate variable columns.\n        time_window: Days before the health event to aggregate climate\n            (currently unused; reserved for future windowed aggregation).\n            ``0`` means same-day join.\n        lags: List of lag offsets in days. For each value *n*, climate\n            variables are added as ``{col}_lag{n}d`` columns aligned to\n            the observation date.\n\n    Returns:\n        ``pandas.DataFrame`` with climate columns left-joined to the\n        health data, plus any requested lag columns.\n\n    Raises:\n        ValueError: If no municipality or date column is found in\n            *data*.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; df = cs.sus_climate(rel, climate_df, lags=[7, 14])\n        &gt;&gt;&gt; df.filter(like=\"_lag7d\").columns\n    \"\"\"\n    conn = get_connection()\n\n    # Materialize health data if needed\n    if isinstance(data, duckdb.DuckDBPyRelation):\n        health_df = collect(data)\n    else:\n        health_df = data\n\n    columns = list(health_df.columns)\n    geo_col = detect_geo_column(columns, level=\"municipality\")\n    date_col = detect_date_column(columns)\n\n    if not geo_col or not date_col:\n        raise ValueError(\n            \"Health data must have a municipality_code and date column for climate join.\"\n        )\n\n    # Ensure date columns are datetime\n    health_df[date_col] = pd.to_datetime(health_df[date_col], errors=\"coerce\")\n    climate[\"date\"] = pd.to_datetime(climate[\"date\"], errors=\"coerce\")\n\n    # Join on municipality + date\n    result = health_df.merge(\n        climate,\n        left_on=[geo_col, date_col],\n        right_on=[\"municipality_code\", \"date\"],\n        how=\"left\",\n        suffixes=(\"\", \"_clim\"),\n    )\n\n    # Add lags if requested\n    # Lag means climate N days BEFORE the health event, so we ADD lag_days\n    # to the climate date to align it with the future health event date.\n    if lags:\n        climate_cols = [c for c in climate.columns if c not in (\"municipality_code\", \"date\")]\n        for lag_days in lags:\n            lagged = climate.copy()\n            lagged[\"date\"] = lagged[\"date\"] + pd.Timedelta(days=lag_days)\n            lag_suffix = f\"_lag{lag_days}d\"\n            lagged = lagged.rename(columns={c: f\"{c}{lag_suffix}\" for c in climate_cols})\n            result = result.merge(\n                lagged,\n                left_on=[geo_col, date_col],\n                right_on=[\"municipality_code\", \"date\"],\n                how=\"left\",\n                suffixes=(\"\", lag_suffix),\n            )\n\n    return result\n</code></pre>"},{"location":"reference/climasus/enrichment/fill_gaps/","title":"<code>climasus.enrichment.fill_gaps</code>","text":""},{"location":"reference/climasus/enrichment/fill_gaps/#climasus.enrichment.fill_gaps","title":"climasus.enrichment.fill_gaps","text":"<p>Climate gap filling \u2014 interpolation and ML-based imputation.</p> <p>Mirrors R: fill_gaps.R</p>"},{"location":"reference/climasus/enrichment/fill_gaps/#climasus.enrichment.fill_gaps-functions","title":"Functions","text":""},{"location":"reference/climasus/enrichment/fill_gaps/#climasus.enrichment.fill_gaps.sus_fill_gaps","title":"sus_fill_gaps","text":"<pre><code>sus_fill_gaps(\n    data: DataFrame,\n    *,\n    method: str = \"linear\",\n    group_col: str = \"municipality_code\",\n    date_col: str = \"date\",\n    max_gap: int | None = None,\n) -&gt; pd.DataFrame\n</code></pre> <p>Fill gaps in climate time series by interpolation or ML imputation.</p> <p>Operates on <code>pandas.DataFrame</code> (in-memory). Applies the chosen method independently within each group defined by group_col.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DataFrame</code> <p><code>DataFrame</code> with climate time series. Must contain at least date_col, group_col, and numeric variable columns.</p> required <code>method</code> <code>str</code> <p>Gap-filling strategy:</p> <ul> <li><code>\"linear\"</code> \u2014 linear interpolation between valid values.</li> <li><code>\"spline\"</code> \u2014 cubic spline (requires \u2265 4 valid points per   group; falls back to linear otherwise).</li> <li><code>\"locf\"</code> \u2014 last observation carried forward   (forward fill).</li> <li><code>\"xgboost\"</code> \u2014 gradient-boosted tree imputation;   requires <code>pip install climasus4py[ml]</code>.</li> </ul> <code>'linear'</code> <code>group_col</code> <code>str</code> <p>Column name used to partition the series (e.g. <code>\"municipality_code\"</code> or a station identifier).</p> <code>'municipality_code'</code> <code>date_col</code> <code>str</code> <p>Column containing the observation date.</p> <code>'date'</code> <code>max_gap</code> <code>int | None</code> <p>Maximum number of consecutive <code>NaN</code> values to fill. <code>None</code> fills all gaps.</p> <code>None</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>Copy of data with missing numeric values filled according to</p> <code>DataFrame</code> <p>method.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If method is <code>\"xgboost\"</code> and <code>xgboost</code> is not installed.</p> <code>ValueError</code> <p>If method is not one of the supported values.</p> Example <p>import climasus as cs filled = cs.sus_fill_gaps(climate_df, method=\"linear\") filled = cs.sus_fill_gaps(climate_df, method=\"locf\", max_gap=3) cs.sus_fill_gaps(climate_df, method=\"xgboost\", ...                  group_col=\"station_id\")</p> Source code in <code>src/climasus/enrichment/fill_gaps.py</code> <pre><code>def sus_fill_gaps(\n    data: pd.DataFrame,\n    *,\n    method: str = \"linear\",\n    group_col: str = \"municipality_code\",\n    date_col: str = \"date\",\n    max_gap: int | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Fill gaps in climate time series by interpolation or ML imputation.\n\n    Operates on ``pandas.DataFrame`` (in-memory). Applies the chosen\n    method independently within each group defined by *group_col*.\n\n    Args:\n        data: ``DataFrame`` with climate time series. Must contain at\n            least *date_col*, *group_col*, and numeric variable columns.\n        method: Gap-filling strategy:\n\n            - ``\"linear\"`` \u2014 linear interpolation between valid values.\n            - ``\"spline\"`` \u2014 cubic spline (requires \u2265 4 valid points per\n              group; falls back to linear otherwise).\n            - ``\"locf\"`` \u2014 last observation carried forward\n              (forward fill).\n            - ``\"xgboost\"`` \u2014 gradient-boosted tree imputation;\n              requires ``pip install climasus4py[ml]``.\n\n        group_col: Column name used to partition the series (e.g.\n            ``\"municipality_code\"`` or a station identifier).\n        date_col: Column containing the observation date.\n        max_gap: Maximum number of consecutive ``NaN`` values to fill.\n            ``None`` fills all gaps.\n\n    Returns:\n        Copy of *data* with missing numeric values filled according to\n        *method*.\n\n    Raises:\n        ImportError: If *method* is ``\"xgboost\"`` and ``xgboost`` is\n            not installed.\n        ValueError: If *method* is not one of the supported values.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; filled = cs.sus_fill_gaps(climate_df, method=\"linear\")\n        &gt;&gt;&gt; filled = cs.sus_fill_gaps(climate_df, method=\"locf\", max_gap=3)\n        &gt;&gt;&gt; cs.sus_fill_gaps(climate_df, method=\"xgboost\",\n        ...                  group_col=\"station_id\")\n    \"\"\"\n    data = data.copy()\n    data[date_col] = pd.to_datetime(data[date_col], errors=\"coerce\")\n\n    numeric_cols = data.select_dtypes(include=[np.number]).columns.tolist()\n    numeric_cols = [c for c in numeric_cols if c != group_col]\n\n    if method == \"linear\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.interpolate(method=\"linear\", limit=max_gap)\n        )\n\n    elif method == \"spline\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.interpolate(method=\"spline\", order=3, limit=max_gap)\n            if s.notna().sum() &gt;= 4 else s.interpolate(method=\"linear\", limit=max_gap)\n        )\n\n    elif method == \"locf\":\n        data[numeric_cols] = data.groupby(group_col)[numeric_cols].transform(\n            lambda s: s.ffill(limit=max_gap)\n        )\n\n    elif method == \"xgboost\":\n        try:\n            import xgboost as xgb\n        except ImportError:\n            raise ImportError(\"Install ML extras: pip install climasus4py[ml]\")\n\n        # XGBoost imputation: train on non-missing, predict missing\n        for col in numeric_cols:\n            for _name, group in data.groupby(group_col):\n                mask = group[col].isna()\n                if not mask.any() or mask.all():\n                    continue\n\n                # Features: other numeric columns + temporal\n                feat_cols = [c for c in numeric_cols if c != col]\n                group_feat = group[feat_cols].copy()\n                group_feat[\"_day_of_year\"] = group[date_col].dt.dayofyear\n\n                train_mask = ~mask\n                if train_mask.sum() &lt; 5:\n                    continue\n\n                model = xgb.XGBRegressor(n_estimators=50, max_depth=3, verbosity=0)\n                model.fit(\n                    group_feat.loc[train_mask].fillna(0),\n                    group.loc[train_mask, col],\n                )\n                predictions = model.predict(group_feat.loc[mask].fillna(0))\n                data.loc[group.index[mask], col] = predictions\n\n    else:\n        raise ValueError(f\"Unknown method: {method}. Use linear, spline, locf, or xgboost.\")\n\n    return data\n</code></pre>"},{"location":"reference/climasus/enrichment/spatial/","title":"<code>climasus.enrichment.spatial</code>","text":""},{"location":"reference/climasus/enrichment/spatial/#climasus.enrichment.spatial","title":"climasus.enrichment.spatial","text":"<p>Spatial enrichment \u2014 join with Brazilian shapefiles.</p> <p>Mirrors R: spatial.R</p>"},{"location":"reference/climasus/enrichment/spatial/#climasus.enrichment.spatial-functions","title":"Functions","text":""},{"location":"reference/climasus/enrichment/spatial/#climasus.enrichment.spatial.sus_spatial","title":"sus_spatial","text":"<pre><code>sus_spatial(\n    data: DuckDBPyRelation | DataFrame,\n    *,\n    shapefile: \"gpd.GeoDataFrame | None\" = None,\n    geo_level: str = \"municipality\",\n    join_type: str = \"left\",\n) -&gt; \"gpd.GeoDataFrame\"\n</code></pre> <p>Join health data with Brazilian shapefiles.</p> <p>Materialises the health data before joining (returns a <code>geopandas.GeoDataFrame</code>). When shapefile is <code>None</code>, geometries are fetched automatically via <code>geobr</code>. The join key is the auto-detected geographic column in data matched against the corresponding identifier column in the shapefile.</p> <p>Requires <code>pip install climasus4py[spatial]</code>.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Health data as a lazy DuckDB relation or <code>DataFrame</code>.</p> required <code>shapefile</code> <code>'gpd.GeoDataFrame | None'</code> <p><code>GeoDataFrame</code> with geometries (e.g. from geobr). Auto-loaded via <code>geobr</code> when <code>None</code>.</p> <code>None</code> <code>geo_level</code> <code>str</code> <p>Geographic level of the join \u2014 <code>\"municipality\"</code> (default), <code>\"state\"</code>, or <code>\"region\"</code>.</p> <code>'municipality'</code> <code>join_type</code> <code>str</code> <p>Merge strategy \u2014 <code>\"left\"</code> (keep all health rows) or <code>\"inner\"</code> (keep only matched rows).</p> <code>'left'</code> <p>Returns:</p> Type Description <code>'gpd.GeoDataFrame'</code> <p><code>geopandas.GeoDataFrame</code> with health data and shapefile</p> <code>'gpd.GeoDataFrame'</code> <p>geometry merged on the appropriate code column.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If <code>geopandas</code> or <code>geobr</code> are not installed.</p> <code>ValueError</code> <p>If no suitable geographic column is found in data or in the shapefile.</p> Example <p>import climasus as cs gdf = cs.sus_spatial(rel, geo_level=\"state\") gdf.plot(column=\"count\") cs.sus_spatial(rel, shapefile=custom_shp, ...               geo_level=\"municipality\")</p> Source code in <code>src/climasus/enrichment/spatial.py</code> <pre><code>def sus_spatial(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    *,\n    shapefile: \"gpd.GeoDataFrame | None\" = None,\n    geo_level: str = \"municipality\",\n    join_type: str = \"left\",\n) -&gt; \"gpd.GeoDataFrame\":\n    \"\"\"Join health data with Brazilian shapefiles.\n\n    Materialises the health data before joining (returns a\n    ``geopandas.GeoDataFrame``). When *shapefile* is ``None``,\n    geometries are fetched automatically via ``geobr``. The join key is\n    the auto-detected geographic column in *data* matched against the\n    corresponding identifier column in the shapefile.\n\n    Requires ``pip install climasus4py[spatial]``.\n\n    Args:\n        data: Health data as a lazy DuckDB relation or ``DataFrame``.\n        shapefile: ``GeoDataFrame`` with geometries (e.g. from geobr).\n            Auto-loaded via ``geobr`` when ``None``.\n        geo_level: Geographic level of the join \u2014 ``\"municipality\"``\n            (default), ``\"state\"``, or ``\"region\"``.\n        join_type: Merge strategy \u2014 ``\"left\"`` (keep all health rows)\n            or ``\"inner\"`` (keep only matched rows).\n\n    Returns:\n        ``geopandas.GeoDataFrame`` with health data and shapefile\n        geometry merged on the appropriate code column.\n\n    Raises:\n        ImportError: If ``geopandas`` or ``geobr`` are not installed.\n        ValueError: If no suitable geographic column is found in *data*\n            or in the shapefile.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; gdf = cs.sus_spatial(rel, geo_level=\"state\")\n        &gt;&gt;&gt; gdf.plot(column=\"count\")\n        &gt;&gt;&gt; cs.sus_spatial(rel, shapefile=custom_shp,\n        ...               geo_level=\"municipality\")\n    \"\"\"\n    try:\n        import geopandas as gpd\n    except ImportError:\n        raise ImportError(\"Install spatial extras: pip install climasus4py[spatial]\")\n\n    # Materialize\n    df = collect(data) if isinstance(data, duckdb.DuckDBPyRelation) else data\n\n    # Auto-load shapefile via geobr\n    if shapefile is None:\n        try:\n            import geobr\n        except ImportError:\n            raise ImportError(\n                \"Provide a shapefile or install geobr: pip install climasus4py[spatial]\"\n            )\n\n        if geo_level == \"state\":\n            shapefile = geobr.read_state()\n        elif geo_level == \"region\":\n            shapefile = geobr.read_region()\n        else:\n            shapefile = geobr.read_municipality()\n\n    # Determine join column\n    from climasus.utils.data import detect_geo_column\n    join_col = detect_geo_column(list(df.columns), level=geo_level)\n\n    if not join_col:\n        raise ValueError(f\"No {geo_level}-level column found in data.\")\n\n    # Find matching column in shapefile\n    shape_candidates = {\n        \"municipality\": [\"code_muni\", \"CD_MUN\"],\n        \"state\": [\"abbrev_state\", \"SIGLA_UF\"],\n        \"region\": [\"name_region\", \"NM_REGIAO\"],\n    }\n    shape_col = None\n    for c in shape_candidates.get(geo_level, []):\n        if c in shapefile.columns:\n            shape_col = c\n            break\n\n    if not shape_col:\n        raise ValueError(f\"Cannot find join column in shapefile for level={geo_level}\")\n\n    # Cast join columns to string for compatibility\n    df[join_col] = df[join_col].astype(str)\n    shapefile[shape_col] = shapefile[shape_col].astype(str)\n\n    how = \"left\" if join_type == \"left\" else \"inner\"\n    result = shapefile.merge(df, left_on=shape_col, right_on=join_col, how=how)\n\n    return result\n</code></pre>"},{"location":"reference/climasus/io/","title":"<code>climasus.io</code>","text":""},{"location":"reference/climasus/io/#climasus.io","title":"climasus.io","text":"<p>Subpacote io \u2014 exporta\u00e7\u00e3o de dados e gerenciamento de cache Parquet.</p>"},{"location":"reference/climasus/io/cache/","title":"<code>climasus.io.cache</code>","text":""},{"location":"reference/climasus/io/cache/#climasus.io.cache","title":"climasus.io.cache","text":"<p>Cache management \u2014 inspect and clear cached parquet files.</p> <p>Mirrors R: cache.R</p>"},{"location":"reference/climasus/io/cache/#climasus.io.cache-functions","title":"Functions","text":""},{"location":"reference/climasus/io/cache/#climasus.io.cache.sus_cache_info","title":"sus_cache_info","text":"<pre><code>sus_cache_info(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n) -&gt; pd.DataFrame\n</code></pre> <p>List all cached Parquet files with metadata.</p> <p>Walks cache_dir recursively and collects file-level statistics for every <code>.parquet</code> file found.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>str | Path</code> <p>Root directory of the Parquet cache. Defaults to <code>\"dados/cache\"</code>.</p> <code>_DEFAULT_CACHE</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p><code>pandas.DataFrame</code> with columns: <code>file</code> (basename),</p> <code>DataFrame</code> <p><code>path</code> (full path string), <code>size_mb</code> (file size in MB),</p> <code>DataFrame</code> <p>and <code>modified</code> (ISO-format last-modified timestamp).</p> Example <p>import climasus as cs info = cs.sus_cache_info() info.sort_values(\"size_mb\", ascending=False).head()</p> Source code in <code>src/climasus/io/cache.py</code> <pre><code>def sus_cache_info(cache_dir: str | Path = _DEFAULT_CACHE) -&gt; pd.DataFrame:\n    \"\"\"List all cached Parquet files with metadata.\n\n    Walks *cache_dir* recursively and collects file-level statistics\n    for every ``.parquet`` file found.\n\n    Args:\n        cache_dir: Root directory of the Parquet cache. Defaults to\n            ``\"dados/cache\"``.\n\n    Returns:\n        ``pandas.DataFrame`` with columns: ``file`` (basename),\n        ``path`` (full path string), ``size_mb`` (file size in MB),\n        and ``modified`` (ISO-format last-modified timestamp).\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; info = cs.sus_cache_info()\n        &gt;&gt;&gt; info.sort_values(\"size_mb\", ascending=False).head()\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    records = []\n\n    if cache_dir.is_dir():\n        for pq_file in cache_dir.rglob(\"*.parquet\"):\n            stat = pq_file.stat()\n            records.append({\n                \"file\": pq_file.name,\n                \"path\": str(pq_file),\n                \"size_mb\": round(stat.st_size / (1024 * 1024), 2),\n                \"modified\": datetime.fromtimestamp(stat.st_mtime).isoformat(timespec=\"seconds\"),\n            })\n\n    return pd.DataFrame(records)\n</code></pre>"},{"location":"reference/climasus/io/cache/#climasus.io.cache.sus_cache_clear","title":"sus_cache_clear","text":"<pre><code>sus_cache_clear(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    *,\n    system: str | None = None,\n    uf: str | None = None,\n    before: str | None = None,\n) -&gt; int\n</code></pre> <p>Delete cached Parquet files matching the given filters.</p> <p>All filters are applied as AND conditions. Files that satisfy every specified criterion are permanently deleted.</p> <p>Parameters:</p> Name Type Description Default <code>cache_dir</code> <code>str | Path</code> <p>Root directory of the Parquet cache.</p> <code>_DEFAULT_CACHE</code> <code>system</code> <code>str | None</code> <p>If provided, only delete files whose parent directory name equals this system identifier (e.g. <code>\"SIM-DO\"</code>).</p> <code>None</code> <code>uf</code> <code>str | None</code> <p>If provided, only delete files whose stem starts with <code>\"{UF}_\"</code> (case-insensitive match).</p> <code>None</code> <code>before</code> <code>str | None</code> <p>ISO date string (<code>\"YYYY-MM-DD\"</code>). Only files last-modified before this date are deleted.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of files deleted.</p> Example <p>import climasus as cs cs.sus_cache_clear(system=\"SIM-DO\", uf=\"SP\") 3 cs.sus_cache_clear(before=\"2024-01-01\")</p> Source code in <code>src/climasus/io/cache.py</code> <pre><code>def sus_cache_clear(\n    cache_dir: str | Path = _DEFAULT_CACHE,\n    *,\n    system: str | None = None,\n    uf: str | None = None,\n    before: str | None = None,\n) -&gt; int:\n    \"\"\"Delete cached Parquet files matching the given filters.\n\n    All filters are applied as AND conditions. Files that satisfy every\n    specified criterion are permanently deleted.\n\n    Args:\n        cache_dir: Root directory of the Parquet cache.\n        system: If provided, only delete files whose parent directory\n            name equals this system identifier (e.g. ``\"SIM-DO\"``).\n        uf: If provided, only delete files whose stem starts with\n            ``\"{UF}_\"`` (case-insensitive match).\n        before: ISO date string (``\"YYYY-MM-DD\"``). Only files\n            last-modified before this date are deleted.\n\n    Returns:\n        Number of files deleted.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.sus_cache_clear(system=\"SIM-DO\", uf=\"SP\")\n        3\n        &gt;&gt;&gt; cs.sus_cache_clear(before=\"2024-01-01\")\n    \"\"\"\n    cache_dir = Path(cache_dir)\n    count = 0\n\n    if not cache_dir.is_dir():\n        return 0\n\n    for pq_file in cache_dir.rglob(\"*.parquet\"):\n        # Filter by system (subdirectory name)\n        if system and pq_file.parent.name != system:\n            continue\n        # Filter by UF (filename starts with UF_)\n        if uf and not pq_file.stem.startswith(uf.upper() + \"_\"):\n            continue\n        # Filter by modification date\n        if before:\n            mod_date = datetime.fromtimestamp(pq_file.stat().st_mtime)\n            cutoff = datetime.fromisoformat(before)\n            if mod_date &gt;= cutoff:\n                continue\n\n        pq_file.unlink()\n        count += 1\n\n    return count\n</code></pre>"},{"location":"reference/climasus/io/export/","title":"<code>climasus.io.export</code>","text":""},{"location":"reference/climasus/io/export/#climasus.io.export","title":"climasus.io.export","text":"<p>Export data to various formats.</p> <p>Mirrors R: export.R \u2014 uses COPY TO for maximum throughput.</p>"},{"location":"reference/climasus/io/export/#climasus.io.export-functions","title":"Functions","text":""},{"location":"reference/climasus/io/export/#climasus.io.export.sus_export","title":"sus_export","text":"<pre><code>sus_export(\n    data: DuckDBPyRelation | DataFrame,\n    path: str | Path,\n    *,\n    fmt: str | None = None,\n    overwrite: bool = True,\n    compress: str = \"snappy\",\n) -&gt; Path\n</code></pre> <p>Export data to Parquet, CSV, or Excel.</p> <p>Accepts lazy DuckDB relations and <code>pandas.DataFrame</code>. For relations, uses DuckDB <code>COPY TO</code> which avoids Python-side materialisation for parquet and CSV formats (significantly faster than <code>write_parquet</code> / <code>write_csv</code>).</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Data to export \u2014 a lazy <code>DuckDBPyRelation</code> or a <code>pandas.DataFrame</code>.</p> required <code>path</code> <code>str | Path</code> <p>Destination file path. The format is inferred from the extension unless fmt is specified explicitly.</p> required <code>fmt</code> <code>str | None</code> <p>Output format override \u2014 <code>\"parquet\"</code>, <code>\"csv\"</code>, or <code>\"xlsx\"</code> / <code>\"excel\"</code>. If <code>None</code>, inferred from path.</p> <code>None</code> <code>overwrite</code> <code>bool</code> <p>If <code>False</code>, raise <code>FileExistsError</code> when path already exists. Defaults to <code>True</code>.</p> <code>True</code> <code>compress</code> <code>str</code> <p>Parquet compression codec \u2014 <code>\"snappy\"</code> (default), <code>\"zstd\"</code>, <code>\"gzip\"</code>, or <code>\"none\"</code>.</p> <code>'snappy'</code> <p>Returns:</p> Type Description <code>Path</code> <p>Resolved <code>pathlib.Path</code> of the written file.</p> <p>Raises:</p> Type Description <code>FileExistsError</code> <p>If path exists and overwrite is <code>False</code>.</p> <code>ValueError</code> <p>If fmt (or the inferred extension) is not supported.</p> <code>ImportError</code> <p>If Excel export is requested but <code>openpyxl</code> is not installed.</p> Example <p>import climasus as cs cs.sus_export(rel, \"output/mortality_2022.parquet\") PosixPath('output/mortality_2022.parquet') cs.sus_export(rel, \"output/data.csv\")</p> Source code in <code>src/climasus/io/export.py</code> <pre><code>def sus_export(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n    path: str | Path,\n    *,\n    fmt: str | None = None,\n    overwrite: bool = True,\n    compress: str = \"snappy\",\n) -&gt; Path:\n    \"\"\"Export data to Parquet, CSV, or Excel.\n\n    Accepts lazy DuckDB relations and ``pandas.DataFrame``. For\n    relations, uses DuckDB ``COPY TO`` which avoids Python-side\n    materialisation for parquet and CSV formats (significantly faster\n    than ``write_parquet`` / ``write_csv``).\n\n    Args:\n        data: Data to export \u2014 a lazy ``DuckDBPyRelation`` or a\n            ``pandas.DataFrame``.\n        path: Destination file path. The format is inferred from the\n            extension unless *fmt* is specified explicitly.\n        fmt: Output format override \u2014 ``\"parquet\"``, ``\"csv\"``, or\n            ``\"xlsx\"`` / ``\"excel\"``. If ``None``, inferred from\n            *path*.\n        overwrite: If ``False``, raise ``FileExistsError`` when *path*\n            already exists. Defaults to ``True``.\n        compress: Parquet compression codec \u2014 ``\"snappy\"`` (default),\n            ``\"zstd\"``, ``\"gzip\"``, or ``\"none\"``.\n\n    Returns:\n        Resolved ``pathlib.Path`` of the written file.\n\n    Raises:\n        FileExistsError: If *path* exists and *overwrite* is ``False``.\n        ValueError: If *fmt* (or the inferred extension) is not\n            supported.\n        ImportError: If Excel export is requested but ``openpyxl`` is\n            not installed.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.sus_export(rel, \"output/mortality_2022.parquet\")\n        PosixPath('output/mortality_2022.parquet')\n        &gt;&gt;&gt; cs.sus_export(rel, \"output/data.csv\")\n    \"\"\"\n    path = Path(path)\n    path.parent.mkdir(parents=True, exist_ok=True)\n\n    if fmt is None:\n        fmt = path.suffix.lstrip(\".\").lower()\n\n    if not overwrite and path.exists():\n        raise FileExistsError(f\"File already exists: {path}\")\n\n    if fmt == \"parquet\":\n        if is_relation(data):\n            _copy_to(data, path, \"PARQUET\", f\"COMPRESSION '{compress}'\")\n        else:\n            pq.write_table(\n                pa.Table.from_pandas(data),\n                path,\n                compression=compress,\n            )\n\n    elif fmt == \"csv\":\n        if is_relation(data):\n            _copy_to(data, path, \"CSV\", \"HEADER TRUE\")\n        else:\n            data.to_csv(path, index=False)\n\n    elif fmt in (\"xlsx\", \"excel\"):\n        df = collect(data) if is_relation(data) else data\n        try:\n            df.to_excel(path, index=False, engine=\"openpyxl\")\n        except ImportError:\n            raise ImportError(\"Install openpyxl: pip install climasus4py[excel]\")\n\n    else:\n        raise ValueError(f\"Unsupported format: {fmt}. Use parquet, csv, or xlsx.\")\n\n    return path\n</code></pre>"},{"location":"reference/climasus/utils/","title":"<code>climasus.utils</code>","text":""},{"location":"reference/climasus/utils/#climasus.utils","title":"climasus.utils","text":"<p>Subpacote utils \u2014 utilit\u00e1rios de dados, CID-10, codifica\u00e7\u00e3o, explora\u00e7\u00e3o e qualidade.</p>"},{"location":"reference/climasus/utils/#climasus.utils-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/cid/","title":"<code>climasus.utils.cid</code>","text":""},{"location":"reference/climasus/utils/cid/#climasus.utils.cid","title":"climasus.utils.cid","text":"<p>CID-10 (ICD-10) code expansion and disease group utilities.</p> <p>Mirrors R: utils-cid.R \u2014 expand_cid_ranges, codes_for_groups.</p>"},{"location":"reference/climasus/utils/cid/#climasus.utils.cid-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/cid/#climasus.utils.cid.expand_cid_range","title":"expand_cid_range","text":"<pre><code>expand_cid_range(start: str, end: str) -&gt; list[str]\n</code></pre> <p>Expand an ICD-10 range into a list of all codes it contains.</p> <p>Handles the standard letter+two-digit-number format (A00\u2013Z99). Both endpoints are inclusive.</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>str</code> <p>Starting ICD-10 code, e.g. <code>\"A00\"</code>.</p> required <code>end</code> <code>str</code> <p>Ending ICD-10 code (inclusive), e.g. <code>\"A09\"</code>.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of ICD-10 code strings spanning from start to end.</p> Example <p>expand_cid_range(\"J00\", \"J06\") ['J00', 'J01', 'J02', 'J03', 'J04', 'J05', 'J06']</p> Source code in <code>src/climasus/utils/cid.py</code> <pre><code>def expand_cid_range(start: str, end: str) -&gt; list[str]:\n    \"\"\"Expand an ICD-10 range into a list of all codes it contains.\n\n    Handles the standard letter+two-digit-number format (A00\u2013Z99).\n    Both endpoints are inclusive.\n\n    Args:\n        start: Starting ICD-10 code, e.g. ``\"A00\"``.\n        end: Ending ICD-10 code (inclusive), e.g. ``\"A09\"``.\n\n    Returns:\n        List of ICD-10 code strings spanning from *start* to *end*.\n\n    Example:\n        &gt;&gt;&gt; expand_cid_range(\"J00\", \"J06\")\n        ['J00', 'J01', 'J02', 'J03', 'J04', 'J05', 'J06']\n    \"\"\"\n    s_letter, s_num = start[0].upper(), int(start[1:])\n    e_letter, e_num = end[0].upper(), int(end[1:])\n\n    codes: list[str] = []\n    for letter in string.ascii_uppercase[\n        string.ascii_uppercase.index(s_letter) : string.ascii_uppercase.index(e_letter) + 1\n    ]:\n        lo = s_num if letter == s_letter else 0\n        hi = e_num if letter == e_letter else 99\n        for num in range(lo, hi + 1):\n            codes.append(f\"{letter}{num:02d}\")\n    return codes\n</code></pre>"},{"location":"reference/climasus/utils/cid/#climasus.utils.cid.expand_cid_ranges","title":"expand_cid_ranges","text":"<pre><code>expand_cid_ranges(codes: list[str]) -&gt; list[str]\n</code></pre> <p>Expand a mixed list of ICD-10 codes, ranges, and letter prefixes.</p> <p>Each item in codes is parsed and expanded:</p> <ul> <li>Single code (e.g. <code>\"A90\"</code>) \u2014 returned as-is.</li> <li>Range (e.g. <code>\"A00-A09\"</code>) \u2014 expanded via   :func:<code>expand_cid_range</code>.</li> <li>Letter prefix (e.g. <code>\"J\"</code>) \u2014 expanded to all codes   <code>J00</code>\u2013<code>J99</code>.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>codes</code> <code>list[str]</code> <p>Mixed list of ICD-10 codes, ranges, and/or letter prefixes.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>Flat list of individual ICD-10 code strings.</p> Example <p>expand_cid_ranges([\"A90\", \"J00-J06\", \"K\"]) ['A90', 'J00', 'J01', ..., 'K00', ..., 'K99']</p> Source code in <code>src/climasus/utils/cid.py</code> <pre><code>def expand_cid_ranges(codes: list[str]) -&gt; list[str]:\n    \"\"\"Expand a mixed list of ICD-10 codes, ranges, and letter prefixes.\n\n    Each item in *codes* is parsed and expanded:\n\n    - **Single code** (e.g. ``\"A90\"``) \u2014 returned as-is.\n    - **Range** (e.g. ``\"A00-A09\"``) \u2014 expanded via\n      :func:`expand_cid_range`.\n    - **Letter prefix** (e.g. ``\"J\"``) \u2014 expanded to all codes\n      ``J00``\u2013``J99``.\n\n    Args:\n        codes: Mixed list of ICD-10 codes, ranges, and/or letter\n            prefixes.\n\n    Returns:\n        Flat list of individual ICD-10 code strings.\n\n    Example:\n        &gt;&gt;&gt; expand_cid_ranges([\"A90\", \"J00-J06\", \"K\"])\n        ['A90', 'J00', 'J01', ..., 'K00', ..., 'K99']\n    \"\"\"\n    expanded: list[str] = []\n    for item in codes:\n        item = item.strip().upper()\n        if \"-\" in item:\n            parts = item.split(\"-\", 1)\n            expanded.extend(expand_cid_range(parts[0], parts[1]))\n        elif len(item) == 1 and item.isalpha():\n            expanded.extend(expand_cid_range(f\"{item}00\", f\"{item}99\"))\n        else:\n            expanded.append(item)\n    return expanded\n</code></pre>"},{"location":"reference/climasus/utils/cid/#climasus.utils.cid.codes_for_groups","title":"codes_for_groups","text":"<pre><code>codes_for_groups(group_names: list[str]) -&gt; list[str]\n</code></pre> <p>Load ICD-10 codes for named disease groups from climasus-data.</p> <p>Searches both <code>disease_groups/core.json</code> and <code>disease_groups/climate_sensitive.json</code>. Matches groups by identifier key or by any language variant of the <code>label</code> field. Supports the flat schema <code>{group_id: {codes: [...]}}</code> used by the current climasus-data release.</p> <p>Parameters:</p> Name Type Description Default <code>group_names</code> <code>list[str]</code> <p>List of group identifiers or labels to look up, e.g. <code>[\"respiratory\", \"dengue\"]</code>.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted, deduplicated list of ICD-10 code strings for all</p> <code>list[str]</code> <p>matched groups.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the required JSON files are not present in the climasus-data directory.</p> Example <p>codes_for_groups([\"respiratory\"]) ['J00', 'J01', ..., 'J99'] len(codes_for_groups([\"cardiovascular\", \"dengue\"]))</p> Source code in <code>src/climasus/utils/cid.py</code> <pre><code>def codes_for_groups(group_names: list[str]) -&gt; list[str]:\n    \"\"\"Load ICD-10 codes for named disease groups from climasus-data.\n\n    Searches both ``disease_groups/core.json`` and\n    ``disease_groups/climate_sensitive.json``. Matches groups by\n    identifier key or by any language variant of the ``label`` field.\n    Supports the flat schema ``{group_id: {codes: [...]}}`` used by\n    the current climasus-data release.\n\n    Args:\n        group_names: List of group identifiers or labels to look up,\n            e.g. ``[\"respiratory\", \"dengue\"]``.\n\n    Returns:\n        Sorted, deduplicated list of ICD-10 code strings for all\n        matched groups.\n\n    Raises:\n        FileNotFoundError: If the required JSON files are not present\n            in the climasus-data directory.\n\n    Example:\n        &gt;&gt;&gt; codes_for_groups([\"respiratory\"])\n        ['J00', 'J01', ..., 'J99']\n        &gt;&gt;&gt; len(codes_for_groups([\"cardiovascular\", \"dengue\"]))\n    \"\"\"\n    all_codes: list[str] = []\n\n    for json_file in (\"disease_groups/core.json\", \"disease_groups/climate_sensitive.json\"):\n        data = load_json(json_file)\n\n        # Flat format: {group_id: {codes: [...]}} (current climasus-data)\n        for group_id, group_data in data.items():\n            if group_id.startswith(\"_\"):\n                continue  # skip _meta\n            if not isinstance(group_data, dict):\n                continue\n            if group_id in group_names:\n                raw = group_data.get(\"codes\", group_data.get(\"icd10_codes\", []))\n                all_codes.extend(expand_cid_ranges(raw))\n                continue\n            # Also match by label\n            label = group_data.get(\"label\", {})\n            if isinstance(label, dict):\n                if any(v in group_names for v in label.values()):\n                    raw = group_data.get(\"codes\", group_data.get(\"icd10_codes\", []))\n                    all_codes.extend(expand_cid_ranges(raw))\n\n    return sorted(set(all_codes))\n</code></pre>"},{"location":"reference/climasus/utils/data/","title":"<code>climasus.utils.data</code>","text":""},{"location":"reference/climasus/utils/data/#climasus.utils.data","title":"climasus.utils.data","text":"<p>Data loading and column detection utilities.</p> <p>Mirrors R: utils-data.R \u2014 JSON loading, column/system detection, UF resolution.</p>"},{"location":"reference/climasus/utils/data/#climasus.utils.data-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/data/#climasus.utils.data.data_path","title":"data_path","text":"<pre><code>data_path(relative: str) -&gt; Path\n</code></pre> <p>Return the absolute path to a file inside the climasus-data directory.</p> <p>Parameters:</p> Name Type Description Default <code>relative</code> <code>str</code> <p>Path relative to the climasus-data root, e.g. <code>\"metadata/sus_systems.json\"</code>.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>Absolute <code>pathlib.Path</code> to the requested file.</p> Example <p>data_path(\"metadata/sus_systems.json\").exists() True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def data_path(relative: str) -&gt; Path:\n    \"\"\"Return the absolute path to a file inside the climasus-data directory.\n\n    Args:\n        relative: Path relative to the climasus-data root, e.g.\n            ``\"metadata/sus_systems.json\"``.\n\n    Returns:\n        Absolute ``pathlib.Path`` to the requested file.\n\n    Example:\n        &gt;&gt;&gt; data_path(\"metadata/sus_systems.json\").exists()\n        True\n    \"\"\"\n    return _find_data_dir() / relative\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.load_json","title":"load_json  <code>cached</code>","text":"<pre><code>load_json(relative: str) -&gt; Any\n</code></pre> <p>Load and cache a JSON file from the climasus-data directory.</p> <p>Results are cached with <code>lru_cache</code> \u2014 the file is read only once per process regardless of how many times this function is called with the same relative path.</p> <p>Parameters:</p> Name Type Description Default <code>relative</code> <code>str</code> <p>Path relative to the climasus-data root, e.g. <code>\"disease_groups/core.json\"</code>.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>Parsed JSON object (<code>dict</code> or <code>list</code>).</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the file does not exist in climasus-data.</p> Example <p>data = load_json(\"metadata/sus_systems.json\") list(data.keys()) ['systems']</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>@lru_cache(maxsize=32)\ndef load_json(relative: str) -&gt; Any:\n    \"\"\"Load and cache a JSON file from the climasus-data directory.\n\n    Results are cached with ``lru_cache`` \u2014 the file is read only once\n    per process regardless of how many times this function is called\n    with the same *relative* path.\n\n    Args:\n        relative: Path relative to the climasus-data root, e.g.\n            ``\"disease_groups/core.json\"``.\n\n    Returns:\n        Parsed JSON object (``dict`` or ``list``).\n\n    Raises:\n        FileNotFoundError: If the file does not exist in climasus-data.\n\n    Example:\n        &gt;&gt;&gt; data = load_json(\"metadata/sus_systems.json\")\n        &gt;&gt;&gt; list(data.keys())\n        ['systems']\n    \"\"\"\n    path = data_path(relative)\n    if not path.is_file():\n        raise FileNotFoundError(f\"Arquivo n\u00e3o encontrado em climasus-data: {relative}\\n\"\n                                \"Certifique-se de que o diret\u00f3rio clonado est\u00e1 presente e atualizado.\")\n    with open(path, encoding=\"utf-8\") as f:\n        return json.load(f)\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.load_datasus_columns_spec","title":"load_datasus_columns_spec  <code>cached</code>","text":"<pre><code>load_datasus_columns_spec() -&gt; dict[str, Any]\n</code></pre> <p>Load DATASUS column specs, with fallback for older climasus-data releases.</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>@lru_cache(maxsize=1)\ndef load_datasus_columns_spec() -&gt; dict[str, Any]:\n    \"\"\"Load DATASUS column specs, with fallback for older climasus-data releases.\"\"\"\n    try:\n        data = load_json(\"metadata/datasus_columns.json\")\n        if isinstance(data, dict):\n            return data\n    except FileNotFoundError:\n        pass\n    return _FALLBACK_DATASUS_COLUMNS.copy()\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.update_climasus_data","title":"update_climasus_data","text":"<pre><code>update_climasus_data(\n    repo_url: str = \"https://github.com/climasus/climasus-data.git\",\n    target_dir: str | None = None,\n    branch: str = \"main\",\n) -&gt; None\n</code></pre> <p>Baixa ou atualiza o reposit\u00f3rio climasus-data localmente.</p> <p>Normalmente n\u00e3o \u00e9 necess\u00e1rio quando <code>climasus-data</code> est\u00e1 instalado como pacote Python. \u00datil durante desenvolvimento ou para for\u00e7ar atualiza\u00e7\u00e3o dos arquivos de refer\u00eancia offline.</p> <p>Parameters:</p> Name Type Description Default <code>repo_url</code> <code>str</code> <p>URL do reposit\u00f3rio Git a clonar.</p> <code>'https://github.com/climasus/climasus-data.git'</code> <code>target_dir</code> <code>str | None</code> <p>Diret\u00f3rio de destino. Se <code>None</code>, usa a vari\u00e1vel de ambiente <code>CLIMASUS_DATA_DIR</code> ou o diret\u00f3rio do pacote instalado.</p> <code>None</code> <code>branch</code> <code>str</code> <p>Branch Git a clonar/atualizar. Padr\u00e3o: <code>\"main\"</code>.</p> <code>'main'</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Se target_dir existe mas n\u00e3o cont\u00e9m <code>manifest.json</code> (prote\u00e7\u00e3o contra remo\u00e7\u00e3o acidental).</p> <code>CalledProcessError</code> <p>Se o comando <code>git</code> falhar.</p> Example <p>import climasus as cs cs.update_climasus_data() Clonando climasus-data em ... climasus-data atualizado com sucesso.</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def update_climasus_data(repo_url: str = \"https://github.com/climasus/climasus-data.git\", target_dir: str | None = None, branch: str = \"main\") -&gt; None:\n    \"\"\"Baixa ou atualiza o reposit\u00f3rio climasus-data localmente.\n\n    Normalmente n\u00e3o \u00e9 necess\u00e1rio quando ``climasus-data`` est\u00e1 instalado\n    como pacote Python. \u00datil durante desenvolvimento ou para for\u00e7ar\n    atualiza\u00e7\u00e3o dos arquivos de refer\u00eancia offline.\n\n    Args:\n        repo_url: URL do reposit\u00f3rio Git a clonar.\n        target_dir: Diret\u00f3rio de destino. Se ``None``, usa a vari\u00e1vel de\n            ambiente ``CLIMASUS_DATA_DIR`` ou o diret\u00f3rio do pacote\n            instalado.\n        branch: Branch Git a clonar/atualizar. Padr\u00e3o: ``\"main\"``.\n\n    Raises:\n        RuntimeError: Se *target_dir* existe mas n\u00e3o cont\u00e9m\n            ``manifest.json`` (prote\u00e7\u00e3o contra remo\u00e7\u00e3o acidental).\n        subprocess.CalledProcessError: Se o comando ``git`` falhar.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; cs.update_climasus_data()\n        Clonando climasus-data em ...\n        climasus-data atualizado com sucesso.\n    \"\"\"\n    if target_dir is None:\n        env = os.environ.get(\"CLIMASUS_DATA_DIR\")\n        if env:\n            target_dir = env\n        else:\n            # Tenta usar o diret\u00f3rio do pacote instalado\n            try:\n                target_dir = str(climasus_data.data_root())\n            except FileNotFoundError:\n                target_dir = str(Path(__file__).resolve().parent.parent.parent / \"climasus-data\")\n\n    target = Path(target_dir)\n    if target.exists() and (target / \".git\").is_dir():\n        # J\u00e1 existe: git pull\n        print(f\"Atualizando climasus-data em {target}...\")\n        subprocess.run([\"git\", \"-C\", str(target), \"pull\", \"origin\", branch], check=True)\n    elif target.exists():\n        # Existe mas n\u00e3o \u00e9 git: remove e clona\n        # Safety check: only remove if it looks like a climasus-data directory\n        if not (target / \"manifest.json\").is_file():\n            raise RuntimeError(\n                f\"Diret\u00f3rio {target} n\u00e3o parece ser climasus-data \"\n                f\"(manifest.json n\u00e3o encontrado). Remo\u00e7\u00e3o abortada por seguran\u00e7a.\"\n            )\n        print(f\"Removendo diret\u00f3rio existente e clonando climasus-data em {target}...\")\n        shutil.rmtree(target)\n        subprocess.run([\"git\", \"clone\", \"--depth\", \"1\", \"-b\", branch, repo_url, str(target)], check=True)\n    else:\n        # N\u00e3o existe: clona\n        print(f\"Clonando climasus-data em {target}...\")\n        subprocess.run([\"git\", \"clone\", \"--depth\", \"1\", \"-b\", branch, repo_url, str(target)], check=True)\n    print(\"climasus-data atualizado com sucesso.\")\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.load_systems","title":"load_systems","text":"<pre><code>load_systems() -&gt; dict\n</code></pre> <p>Load SUS system definitions from climasus-data.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dict mapping system identifiers to their metadata.</p> Example <p>systems = load_systems() \"SIM-DO\" in systems True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def load_systems() -&gt; dict:\n    \"\"\"Load SUS system definitions from climasus-data.\n\n    Returns:\n        Dict mapping system identifiers to their metadata.\n\n    Example:\n        &gt;&gt;&gt; systems = load_systems()\n        &gt;&gt;&gt; \"SIM-DO\" in systems\n        True\n    \"\"\"\n    return load_json(\"metadata/sus_systems.json\")[\"systems\"]\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.load_uf_codes","title":"load_uf_codes","text":"<pre><code>load_uf_codes() -&gt; dict\n</code></pre> <p>Load Brazilian state (UF) codes from climasus-data.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dict mapping 2-letter UF abbreviations to state metadata.</p> Example <p>ufs = load_uf_codes() \"name\" in ufs[\"SP\"] True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def load_uf_codes() -&gt; dict:\n    \"\"\"Load Brazilian state (UF) codes from climasus-data.\n\n    Returns:\n        Dict mapping 2-letter UF abbreviations to state metadata.\n\n    Example:\n        &gt;&gt;&gt; ufs = load_uf_codes()\n        &gt;&gt;&gt; \"name\" in ufs[\"SP\"]\n        True\n    \"\"\"\n    return load_json(\"metadata/uf_codes.json\")[\"states\"]\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.load_regions","title":"load_regions","text":"<pre><code>load_regions() -&gt; dict\n</code></pre> <p>Load Brazilian region definitions from climasus-data.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dict mapping region categories to their region metadata and</p> <code>dict</code> <p>member state lists.</p> Example <p>regions = load_regions() list(regions.keys())</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def load_regions() -&gt; dict:\n    \"\"\"Load Brazilian region definitions from climasus-data.\n\n    Returns:\n        Dict mapping region categories to their region metadata and\n        member state lists.\n\n    Example:\n        &gt;&gt;&gt; regions = load_regions()\n        &gt;&gt;&gt; list(regions.keys())\n    \"\"\"\n    return load_json(\"metadata/regions.json\")[\"categories\"]\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.resolve_uf","title":"resolve_uf","text":"<pre><code>resolve_uf(uf: str | list[str]) -&gt; list[str]\n</code></pre> <p>Resolve a UF specification to a list of 2-letter state codes.</p> <p>Parameters:</p> Name Type Description Default <code>uf</code> <code>str | list[str]</code> <p>A single UF string (e.g. <code>\"SP\"</code>), a list of UFs (e.g. <code>[\"SP\", \"RJ\"]</code>), the special token <code>\"all\"</code> to expand to all 27 states, or a region name (e.g. <code>\"Sudeste\"</code>) to expand to its member states.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>List of upper-case 2-letter UF abbreviations.</p> Example <p>resolve_uf(\"SP\") ['SP'] resolve_uf(\"Sudeste\") ['ES', 'MG', 'RJ', 'SP'] len(resolve_uf(\"all\")) 27</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def resolve_uf(uf: str | list[str]) -&gt; list[str]:\n    \"\"\"Resolve a UF specification to a list of 2-letter state codes.\n\n    Args:\n        uf: A single UF string (e.g. ``\"SP\"``), a list of UFs\n            (e.g. ``[\"SP\", \"RJ\"]``), the special token ``\"all\"`` to\n            expand to all 27 states, or a region name\n            (e.g. ``\"Sudeste\"``) to expand to its member states.\n\n    Returns:\n        List of upper-case 2-letter UF abbreviations.\n\n    Example:\n        &gt;&gt;&gt; resolve_uf(\"SP\")\n        ['SP']\n        &gt;&gt;&gt; resolve_uf(\"Sudeste\")\n        ['ES', 'MG', 'RJ', 'SP']\n        &gt;&gt;&gt; len(resolve_uf(\"all\"))\n        27\n    \"\"\"\n    if isinstance(uf, str):\n        uf_list = [uf]\n    else:\n        uf_list = list(uf)\n\n    if len(uf_list) == 1:\n        token = uf_list[0]\n        if token.lower() == \"all\":\n            return list(load_uf_codes().keys())\n\n        # Check if it's a region name\n        regions = load_regions()\n        for category in regions.values():\n            for region_name, region_data in category.get(\"regions\", {}).items():\n                if token == region_name:\n                    return region_data[\"states\"]\n\n    return [u.upper() for u in uf_list]\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_system","title":"detect_system","text":"<pre><code>detect_system(columns: list[str]) -&gt; str | None\n</code></pre> <p>Detect the SUS system from a list of column names.</p> <p>Uses characteristic columns as signatures: e.g. <code>CAUSABAS</code> and <code>DTOBITO</code> identify SIM-DO; <code>NUMERODN</code> identifies SINASC.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>System identifier string (e.g. <code>\"SIM-DO\"</code>), or <code>None</code> when</p> <code>str | None</code> <p>no known signature is found.</p> Example <p>detect_system([\"CAUSABAS\", \"DTOBITO\", \"IDADE\"]) 'SIM-DO' detect_system([\"UNKNOWN_COL\"]) is None True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_system(columns: list[str]) -&gt; str | None:\n    \"\"\"Detect the SUS system from a list of column names.\n\n    Uses characteristic columns as signatures: e.g. ``CAUSABAS`` and\n    ``DTOBITO`` identify SIM-DO; ``NUMERODN`` identifies SINASC.\n\n    Args:\n        columns: Column names present in the dataset.\n\n    Returns:\n        System identifier string (e.g. ``\"SIM-DO\"``), or ``None`` when\n        no known signature is found.\n\n    Example:\n        &gt;&gt;&gt; detect_system([\"CAUSABAS\", \"DTOBITO\", \"IDADE\"])\n        'SIM-DO'\n        &gt;&gt;&gt; detect_system([\"UNKNOWN_COL\"]) is None\n        True\n    \"\"\"\n    col_set = set(columns)\n    signatures = _load_datasus_columns_json()[\"system_signatures\"]\n    for system, spec in signatures.items():\n        if col_set &amp; set(spec[\"any_of\"]):\n            return system\n    return None\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_date_column","title":"detect_date_column","text":"<pre><code>detect_date_column(columns: list[str]) -&gt; str | None\n</code></pre> <p>Return the first recognised date column from a list of column names.</p> <p>Searches columns for known DATASUS and standardised date column names in priority order: <code>death_date</code>, <code>date</code>, <code>DTOBITO</code>, <code>DTNASC</code>, <code>admission_date</code>.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Matching column name, or <code>None</code> if no date candidate is found.</p> Example <p>detect_date_column([\"DTOBITO\", \"CAUSABAS\", \"IDADE\"]) 'DTOBITO' detect_date_column([\"UNKNOWN\"]) is None True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_date_column(columns: list[str]) -&gt; str | None:\n    \"\"\"Return the first recognised date column from a list of column names.\n\n    Searches *columns* for known DATASUS and standardised date column\n    names in priority order: ``death_date``, ``date``, ``DTOBITO``,\n    ``DTNASC``, ``admission_date``.\n\n    Args:\n        columns: Column names present in the dataset.\n\n    Returns:\n        Matching column name, or ``None`` if no date candidate is found.\n\n    Example:\n        &gt;&gt;&gt; detect_date_column([\"DTOBITO\", \"CAUSABAS\", \"IDADE\"])\n        'DTOBITO'\n        &gt;&gt;&gt; detect_date_column([\"UNKNOWN\"]) is None\n        True\n    \"\"\"\n    return _detect_column(columns, _load_datasus_columns_json()[\"role_priority\"][\"date\"])\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_cause_column","title":"detect_cause_column","text":"<pre><code>detect_cause_column(columns: list[str]) -&gt; str | None\n</code></pre> <p>Return the first recognised ICD-10 cause column from a list of column names.</p> <p>Searches columns for known DATASUS and standardised cause column names in priority order: <code>underlying_cause</code>, <code>cause</code>, <code>CAUSABAS</code>, <code>DIAG_PRINC</code>.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Matching column name, or <code>None</code> if no cause candidate is found.</p> Example <p>detect_cause_column([\"CAUSABAS\", \"DTOBITO\"]) 'CAUSABAS' detect_cause_column([\"OTHER\"]) is None True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_cause_column(columns: list[str]) -&gt; str | None:\n    \"\"\"Return the first recognised ICD-10 cause column from a list of column names.\n\n    Searches *columns* for known DATASUS and standardised cause column\n    names in priority order: ``underlying_cause``, ``cause``,\n    ``CAUSABAS``, ``DIAG_PRINC``.\n\n    Args:\n        columns: Column names present in the dataset.\n\n    Returns:\n        Matching column name, or ``None`` if no cause candidate is found.\n\n    Example:\n        &gt;&gt;&gt; detect_cause_column([\"CAUSABAS\", \"DTOBITO\"])\n        'CAUSABAS'\n        &gt;&gt;&gt; detect_cause_column([\"OTHER\"]) is None\n        True\n    \"\"\"\n    return _detect_column(columns, _load_datasus_columns_json()[\"role_priority\"][\"cause\"])\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_age_column","title":"detect_age_column","text":"<pre><code>detect_age_column(columns: list[str]) -&gt; str | None\n</code></pre> <p>Return the first recognised age column from a list of column names.</p> <p>Searches columns for known DATASUS and standardised age column names in priority order: <code>age</code>, <code>age_years</code>, <code>age_code</code>, <code>IDADE</code>, <code>IDADEMAE</code>.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Matching column name, or <code>None</code> if no age candidate is found.</p> Example <p>detect_age_column([\"IDADE\", \"SEXO\"]) 'IDADE' detect_age_column([\"UNKNOWN\"]) is None True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_age_column(columns: list[str]) -&gt; str | None:\n    \"\"\"Return the first recognised age column from a list of column names.\n\n    Searches *columns* for known DATASUS and standardised age column\n    names in priority order: ``age``, ``age_years``, ``age_code``,\n    ``IDADE``, ``IDADEMAE``.\n\n    Args:\n        columns: Column names present in the dataset.\n\n    Returns:\n        Matching column name, or ``None`` if no age candidate is found.\n\n    Example:\n        &gt;&gt;&gt; detect_age_column([\"IDADE\", \"SEXO\"])\n        'IDADE'\n        &gt;&gt;&gt; detect_age_column([\"UNKNOWN\"]) is None\n        True\n    \"\"\"\n    return _detect_column(columns, _load_datasus_columns_json()[\"role_priority\"][\"age\"])\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_sex_column","title":"detect_sex_column","text":"<pre><code>detect_sex_column(columns: list[str]) -&gt; str | None\n</code></pre> <p>Return the first recognised sex column from a list of column names.</p> <p>Searches columns for known DATASUS and standardised sex column names in priority order: <code>sex</code>, <code>SEXO</code>, <code>CS_SEXO</code>.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Matching column name, or <code>None</code> if no sex candidate is found.</p> Example <p>detect_sex_column([\"SEXO\", \"IDADE\"]) 'SEXO' detect_sex_column([\"UNKNOWN\"]) is None True</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_sex_column(columns: list[str]) -&gt; str | None:\n    \"\"\"Return the first recognised sex column from a list of column names.\n\n    Searches *columns* for known DATASUS and standardised sex column\n    names in priority order: ``sex``, ``SEXO``, ``CS_SEXO``.\n\n    Args:\n        columns: Column names present in the dataset.\n\n    Returns:\n        Matching column name, or ``None`` if no sex candidate is found.\n\n    Example:\n        &gt;&gt;&gt; detect_sex_column([\"SEXO\", \"IDADE\"])\n        'SEXO'\n        &gt;&gt;&gt; detect_sex_column([\"UNKNOWN\"]) is None\n        True\n    \"\"\"\n    return _detect_column(columns, _load_datasus_columns_json()[\"role_priority\"][\"sex\"])\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.decode_age_sql","title":"decode_age_sql","text":"<pre><code>decode_age_sql(age_col: str) -&gt; str\n</code></pre> <p>Return a DuckDB SQL expression that decodes SIM-DO coded age to years.</p> <p>DATASUS SIM-DO encodes age as a 3-digit string where the first digit is a unit code:</p> <ul> <li><code>5xx</code> \u2014 100 + xx years (centenarians, e.g. 501 = 101 years)</li> <li><code>4xx</code> \u2014 xx years       (e.g. 435 = 35 years)</li> <li><code>3xx</code> \u2014 months         (decoded to 0 years)</li> <li><code>2xx</code> \u2014 days           (decoded to 0 years)</li> <li><code>1xx</code> \u2014 hours          (decoded to 0 years)</li> <li><code>0xx</code> \u2014 minutes        (decoded to 0 years)</li> </ul> <p>Parameters:</p> Name Type Description Default <code>age_col</code> <code>str</code> <p>Column name containing the raw DATASUS age code.</p> required <p>Returns:</p> Type Description <code>str</code> <p>SQL <code>CASE</code> expression string that evaluates to an integer</p> <code>str</code> <p>representing age in years.</p> Example <p>expr = decode_age_sql(\"IDADE\") conn.sql(f\"SELECT ({expr}) AS age_years FROM rel\").df()</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def decode_age_sql(age_col: str) -&gt; str:\n    \"\"\"Return a DuckDB SQL expression that decodes SIM-DO coded age to years.\n\n    DATASUS SIM-DO encodes age as a 3-digit string where the first\n    digit is a unit code:\n\n    - ``5xx`` \u2014 100 + xx years (centenarians, e.g. 501 = 101 years)\n    - ``4xx`` \u2014 xx years       (e.g. 435 = 35 years)\n    - ``3xx`` \u2014 months         (decoded to 0 years)\n    - ``2xx`` \u2014 days           (decoded to 0 years)\n    - ``1xx`` \u2014 hours          (decoded to 0 years)\n    - ``0xx`` \u2014 minutes        (decoded to 0 years)\n\n    Args:\n        age_col: Column name containing the raw DATASUS age code.\n\n    Returns:\n        SQL ``CASE`` expression string that evaluates to an integer\n        representing age in years.\n\n    Example:\n        &gt;&gt;&gt; expr = decode_age_sql(\"IDADE\")\n        &gt;&gt;&gt; conn.sql(f\"SELECT ({expr}) AS age_years FROM rel\").df()\n    \"\"\"\n    v = f'TRIM(CAST(\"{age_col}\" AS VARCHAR))'\n    return (\n        f\"CASE\"\n        f\"  WHEN LENGTH({v}) = 3 AND SUBSTR({v}, 1, 1) = '5'\"\n        f\"    THEN 100 + TRY_CAST(SUBSTR({v}, 2) AS INTEGER)\"\n        f\"  WHEN LENGTH({v}) = 3 AND SUBSTR({v}, 1, 1) = '4'\"\n        f\"    THEN TRY_CAST(SUBSTR({v}, 2) AS INTEGER)\"\n        f\"  WHEN LENGTH({v}) = 3 AND SUBSTR({v}, 1, 1) IN ('0', '1', '2', '3')\"\n        f\"    THEN 0\"\n        f\"  ELSE TRY_CAST({v} AS INTEGER)\"\n        f\" END\"\n    )\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.detect_geo_column","title":"detect_geo_column","text":"<pre><code>detect_geo_column(\n    columns: list[str], level: str = \"municipality\"\n) -&gt; str | None\n</code></pre> <p>Return the first recognised geographic column for the requested level.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[str]</code> <p>Column names present in the dataset.</p> required <code>level</code> <code>str</code> <p>Geographic level to detect \u2014 <code>\"municipality\"</code> (default), <code>\"state\"</code>, <code>\"region\"</code>, or <code>\"country\"</code>.</p> <code>'municipality'</code> <p>Returns:</p> Type Description <code>str | None</code> <p>Matching column name, or <code>None</code> if no candidate is found.</p> Example <p>detect_geo_column([\"CODMUNRES\", \"DTOBITO\"]) 'CODMUNRES' detect_geo_column([\"UF\", \"DTOBITO\"], level=\"state\") 'UF'</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def detect_geo_column(columns: list[str], level: str = \"municipality\") -&gt; str | None:\n    \"\"\"Return the first recognised geographic column for the requested level.\n\n    Args:\n        columns: Column names present in the dataset.\n        level: Geographic level to detect \u2014 ``\"municipality\"``\n            (default), ``\"state\"``, ``\"region\"``, or ``\"country\"``.\n\n    Returns:\n        Matching column name, or ``None`` if no candidate is found.\n\n    Example:\n        &gt;&gt;&gt; detect_geo_column([\"CODMUNRES\", \"DTOBITO\"])\n        'CODMUNRES'\n        &gt;&gt;&gt; detect_geo_column([\"UF\", \"DTOBITO\"], level=\"state\")\n        'UF'\n    \"\"\"\n    candidates = {\n        \"municipality\": [\"municipality_code\", \"CODMUNRES\", \"ID_MUNICIP\"],\n        \"state\": [\"state\", \"SG_UF\", \"UF\", \"SG_UF_NOT\"],\n        \"region\": [\"region\"],\n        \"country\": [\"country\"],\n    }\n    return _detect_column(columns, candidates.get(level, []))\n</code></pre>"},{"location":"reference/climasus/utils/data/#climasus.utils.data.system_family","title":"system_family","text":"<pre><code>system_family(system: str) -&gt; str\n</code></pre> <p>Extract the family prefix from a SUS system identifier.</p> <p>Parameters:</p> Name Type Description Default <code>system</code> <code>str</code> <p>Full system name, e.g. <code>\"SIM-DO\"</code> or <code>\"SIH-RD\"</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Family prefix string, e.g. <code>\"SIM\"</code> or <code>\"SIH\"</code>.</p> Example <p>system_family(\"SIM-DO\") 'SIM' system_family(\"SIH-RD\") 'SIH'</p> Source code in <code>src/climasus/utils/data.py</code> <pre><code>def system_family(system: str) -&gt; str:\n    \"\"\"Extract the family prefix from a SUS system identifier.\n\n    Args:\n        system: Full system name, e.g. ``\"SIM-DO\"`` or ``\"SIH-RD\"``.\n\n    Returns:\n        Family prefix string, e.g. ``\"SIM\"`` or ``\"SIH\"``.\n\n    Example:\n        &gt;&gt;&gt; system_family(\"SIM-DO\")\n        'SIM'\n        &gt;&gt;&gt; system_family(\"SIH-RD\")\n        'SIH'\n    \"\"\"\n    return system.split(\"-\")[0]\n</code></pre>"},{"location":"reference/climasus/utils/encoding/","title":"<code>climasus.utils.encoding</code>","text":""},{"location":"reference/climasus/utils/encoding/#climasus.utils.encoding","title":"climasus.utils.encoding","text":"<p>UTF-8 encoding fixes for DATASUS data.</p> <p>Mirrors R: utils-encoding.R \u2014 fixes mojibake from latin1\u2192utf8 double-encoding.</p>"},{"location":"reference/climasus/utils/encoding/#climasus.utils.encoding-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/encoding/#climasus.utils.encoding.fix_encoding","title":"fix_encoding","text":"<pre><code>fix_encoding(text: str) -&gt; str\n</code></pre> <p>Fix mojibake caused by UTF-8 text mistakenly decoded as Latin-1/CP-1252.</p> <p>Many DATASUS <code>.dbc</code> files contain UTF-8 strings that were read with a Latin-1/CP-1252 codec, producing garbled characters (mojibake). The fix re-encodes the mangled string as CP-1252 then decodes it as UTF-8, reversing the double-encoding.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>String that may contain mojibake characters.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Corrected UTF-8 string, or the original text unchanged if</p> <code>str</code> <p>the re-encoding fails (e.g. the text was already valid UTF-8).</p> Example <p>fix_encoding(\"S\u00c3\u00a3o Paulo\")  # mojibake for \"S\u00e3o Paulo\" 'S\u00e3o Paulo' fix_encoding(\"already fine\") 'already fine'</p> Source code in <code>src/climasus/utils/encoding.py</code> <pre><code>def fix_encoding(text: str) -&gt; str:\n    \"\"\"Fix mojibake caused by UTF-8 text mistakenly decoded as Latin-1/CP-1252.\n\n    Many DATASUS ``.dbc`` files contain UTF-8 strings that were read\n    with a Latin-1/CP-1252 codec, producing garbled characters\n    (mojibake). The fix re-encodes the mangled string as CP-1252 then\n    decodes it as UTF-8, reversing the double-encoding.\n\n    Args:\n        text: String that may contain mojibake characters.\n\n    Returns:\n        Corrected UTF-8 string, or the original *text* unchanged if\n        the re-encoding fails (e.g. the text was already valid UTF-8).\n\n    Example:\n        &gt;&gt;&gt; fix_encoding(\"S\\u00c3\\u00a3o Paulo\")  # mojibake for \"S\u00e3o Paulo\"\n        'S\u00e3o Paulo'\n        &gt;&gt;&gt; fix_encoding(\"already fine\")\n        'already fine'\n    \"\"\"\n    try:\n        return text.encode(\"cp1252\").decode(\"utf-8\")\n    except (UnicodeDecodeError, UnicodeEncodeError):\n        return text\n</code></pre>"},{"location":"reference/climasus/utils/explore/","title":"<code>climasus.utils.explore</code>","text":""},{"location":"reference/climasus/utils/explore/#climasus.utils.explore","title":"climasus.utils.explore","text":"<p>Metadata exploration \u2014 browse disease groups, systems, regions.</p> <p>Mirrors R: explore.R</p>"},{"location":"reference/climasus/utils/explore/#climasus.utils.explore-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/explore/#climasus.utils.explore.sus_explore","title":"sus_explore","text":"<pre><code>sus_explore(what: str = 'systems') -&gt; dict\n</code></pre> <p>Browse climasus-data metadata interactively.</p> <p>Returns the requested metadata dictionary from climasus-data, useful for discovering available systems, disease groups, regions, and state codes before building a pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>what</code> <code>str</code> <p>Topic to browse. Accepted values:</p> <ul> <li><code>\"systems\"</code> \u2014 available SUS systems and their   properties.</li> <li><code>\"groups\"</code> \u2014 disease group definitions (core and   climate-sensitive).</li> <li><code>\"regions\"</code> \u2014 Brazilian macro-regions and member   states.</li> <li><code>\"uf\"</code> \u2014 2-letter state codes and names.</li> </ul> <code>'systems'</code> <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with the requested metadata. Structure depends on</p> <code>dict</code> <p>the what argument.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If what is not one of the accepted values.</p> Example <p>import climasus as cs systems = cs.sus_explore(\"systems\") list(systems.keys())[:3] cs.sus_explore(\"groups\")[\"climate_sensitive\"]</p> Source code in <code>src/climasus/utils/explore.py</code> <pre><code>def sus_explore(\n    what: str = \"systems\",\n) -&gt; dict:\n    \"\"\"Browse climasus-data metadata interactively.\n\n    Returns the requested metadata dictionary from climasus-data,\n    useful for discovering available systems, disease groups, regions,\n    and state codes before building a pipeline.\n\n    Args:\n        what: Topic to browse. Accepted values:\n\n            - ``\"systems\"`` \u2014 available SUS systems and their\n              properties.\n            - ``\"groups\"`` \u2014 disease group definitions (core and\n              climate-sensitive).\n            - ``\"regions\"`` \u2014 Brazilian macro-regions and member\n              states.\n            - ``\"uf\"`` \u2014 2-letter state codes and names.\n\n    Returns:\n        Dictionary with the requested metadata. Structure depends on\n        the *what* argument.\n\n    Raises:\n        ValueError: If *what* is not one of the accepted values.\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; systems = cs.sus_explore(\"systems\")\n        &gt;&gt;&gt; list(systems.keys())[:3]\n        &gt;&gt;&gt; cs.sus_explore(\"groups\")[\"climate_sensitive\"]\n    \"\"\"\n    if what == \"systems\":\n        return load_systems()\n\n    elif what == \"groups\":\n        core = load_json(\"disease_groups/core.json\")\n        sensitive = load_json(\"disease_groups/climate_sensitive.json\")\n        return {\"core\": core, \"climate_sensitive\": sensitive}\n\n    elif what == \"regions\":\n        return load_regions()\n\n    elif what == \"uf\":\n        return load_uf_codes()\n\n    else:\n        raise ValueError(f\"Unknown topic: {what}. Use systems, groups, regions, or uf.\")\n</code></pre>"},{"location":"reference/climasus/utils/quality/","title":"<code>climasus.utils.quality</code>","text":""},{"location":"reference/climasus/utils/quality/#climasus.utils.quality","title":"climasus.utils.quality","text":"<p>Data quality profiling.</p> <p>Mirrors R: quality.R</p>"},{"location":"reference/climasus/utils/quality/#climasus.utils.quality-functions","title":"Functions","text":""},{"location":"reference/climasus/utils/quality/#climasus.utils.quality.sus_quality","title":"sus_quality","text":"<pre><code>sus_quality(data: DuckDBPyRelation | DataFrame) -&gt; dict\n</code></pre> <p>Calculate data quality metrics for a SUS dataset.</p> <p>Computes row count, column count, and per-column completeness rate (percentage of non-null values). Works with both lazy DuckDB relations and materialised <code>pandas.DataFrame</code> objects.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>DuckDBPyRelation | DataFrame</code> <p>Dataset to profile \u2014 a lazy <code>DuckDBPyRelation</code> or a <code>pandas.DataFrame</code>.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>Dictionary with the following keys:</p> <code>dict</code> <ul> <li><code>total_rows</code> (int): total number of rows.</li> </ul> <code>dict</code> <ul> <li><code>total_cols</code> (int): total number of columns.</li> </ul> <code>dict</code> <ul> <li><code>completeness</code> (dict[str, float]): per-column mapping of column name to percentage of non-null values (0\u2013100).</li> </ul> Example <p>import climasus as cs metrics = cs.sus_quality(rel) metrics[\"total_rows\"] 334303 isinstance(metrics.get(\"completeness\"), dict) True</p> Source code in <code>src/climasus/utils/quality.py</code> <pre><code>def sus_quality(\n    data: duckdb.DuckDBPyRelation | pd.DataFrame,\n) -&gt; dict:\n    \"\"\"Calculate data quality metrics for a SUS dataset.\n\n    Computes row count, column count, and per-column completeness rate\n    (percentage of non-null values). Works with both lazy DuckDB\n    relations and materialised ``pandas.DataFrame`` objects.\n\n    Args:\n        data: Dataset to profile \u2014 a lazy ``DuckDBPyRelation`` or a\n            ``pandas.DataFrame``.\n\n    Returns:\n        Dictionary with the following keys:\n\n        - `total_rows` (int): total number of rows.\n        - `total_cols` (int): total number of columns.\n        - `completeness` (dict[str, float]): per-column mapping\n            of column name to percentage of non-null values (0\u2013100).\n\n    Example:\n        &gt;&gt;&gt; import climasus as cs\n        &gt;&gt;&gt; metrics = cs.sus_quality(rel)\n        &gt;&gt;&gt; metrics[\"total_rows\"]\n        334303\n        &gt;&gt;&gt; isinstance(metrics.get(\"completeness\"), dict)\n        True\n    \"\"\"\n    conn = get_connection()\n\n    if is_relation(data):\n        columns = data.columns\n        total_rows = data.aggregate(\"count(*)\").fetchone()[0]\n\n        completeness = {}\n        for col in columns:\n            non_null = conn.sql(\n                f'SELECT COUNT(\"{col}\") FROM data WHERE \"{col}\" IS NOT NULL'\n            ).fetchone()[0]\n            completeness[col] = round(non_null / max(total_rows, 1) * 100, 1)\n    else:\n        total_rows = len(data)\n        columns = list(data.columns)\n        completeness = {\n            col: round(data[col].notna().sum() / max(total_rows, 1) * 100, 1)\n            for col in columns\n        }\n\n    return {\n        \"total_rows\": total_rows,\n        \"total_cols\": len(columns),\n        \"completeness\": completeness,\n    }\n</code></pre>"}]}